# 12. Open decisions

## Status
- Owner: the platform owner
- Last reviewed: 2026-09-15
- 2026-09-15: P144–P191 added in [§6a](#6a-proposed-by-the-setup-procedures-p144p191) — the 48
  decisions the setup procedures ([setup/README.md](setup/README.md)) proposed to close the
  findings of [13-setup-procedure-review.md](13-setup-procedure-review.md); each is
  `proposed by the setup procedures, pending the owner's signature` and is signed as a dated
  record under [setup 03 §11](setup/03-decisions-and-people.md#11-the-checklist-every-decision-its-gate-and-its-signatories).
- Maturity: the register of record for the platform set, written 2026-09-13. It holds every
  platform decision in one numbered sequence **P1–P191**: P1–P34 are the HLD's own
  ([01-hld.md](01-hld.md) §17, repeated here so a reader has one list) and P35–P143 are the
  decisions the ten detailed pages [02](02-landing-zone-and-tiers.md)–[11](11-tisax.md) proposed
  and recorded. An id on any page of this set is the id here. Nothing is built; every row is a design decision
  about something that does not yet exist.
- What a row is: a choice the platform must make once, named so it is not re-argued per agent,
  per auditor or per page. Each row names the decision, why it matters (what breaks or what
  cannot be verified if it stays undecided), the recommendation or value a page proposed, the
  owner (a role, never a person, except where the objective's author is the owner), the gate it
  blocks, where it is recorded, and the agent-set decisions it supersedes or depends on
  (Wall-E's 1–52 in [../wall-e/09-open-decisions.md](../wall-e/09-open-decisions.md) and
  [../project-topology.md](../project-topology.md) §8 for 42–52; Eve's E-1..E-20 in
  [../eve/09-open-decisions.md](../eve/09-open-decisions.md); Mo's M-1..M-11 in
  [../mo/08-open-decisions.md](../mo/08-open-decisions.md)).
- Grouping: by the gate the decision blocks, in the order the gates come — §2 before the
  folder exists, §3 before any Tier W agent writes, §4 before the super-admin grant (which is
  also the Tier P gate, HLD §0.4), §5 before Wall-E's Stage 1 and its later stages, §6 later
  (the TISAX assessment order, Tier X, continuous triggers, revisit dates). A decision that
  blocks two gates sits in the earliest and says so.
- Conventions: `Assumption:` marks inferred facts; *tbd* marks values nobody has decided; no
  company names, no secrets, no person named for a role. This page introduces no Google or
  regulatory fact of its own: every product, role, constraint, article and date in a row is the
  one its recording page verified, and the "Recorded in" column is where the source URL is.
- Standing constraints: this register cannot loosen the constraints of
  [01-hld.md Status](01-hld.md#status).

---

## 0. The register in one screen

| | Count | Of which decided or closed | Proposed by a page (stands unless the owner overturns) | Open (nobody has decided; no page proposes beyond the HLD) | Spike (answered by a test on a throwaway resource) |
|---|---|---|---|---|---|
| HLD decisions P1–P34 | 34 | 3 (P2 closed by P35, P9 closed by P60; P33 decided by the owner) | 9 (P1, P15, P16, P20 via P133, P24 for the tenant app, P27, P28's content via P125, P30, P34) | 18 | 4 (P3, P4, P6 via P57, P8 via P61) |
| Setup-procedure decisions P144–P191 (§6a, added 2026-09-15) | 48 | 0 | 48 — every row `proposed by the setup procedures, pending the owner's signature`; the procedures refuse the gated step until its record is signed | 0 | 0 (P165 relies on the P8 spike, P189 on the twin) |
| Page decisions P35–P143 | 109 | 0 (P60 closes P9 but is itself a proposal) | 107 — of which 11 carry an `open` dependency or an `Assumption:` value the owner must confirm (P31's amounts in P39; P13 in P52, P106, P113; P24 in P64; P10 in P92; P20 in P119, P133; P18 in P126; P83's SLO; P117's thresholds) | 0 | 2 (P57, P61); P42 carries three sub-spikes, and P89 and P90 wait on P3's spike 1 (P89's template is also what makes spike 2's backstop possible) |

| Gate (§) | Rows | What opens when the group is green |
|---|---|---|
| §2 Before the folder exists | P1, P2, P4, P5, P8, P9, P11, P21, P31, P35–P39, P41–P45, P47–P49, P51, P53–P55, P58, P59, P61, P62, P65, P67, P70–P73, P78, P80–P82, P84–P86, P88, P94, P95, P104, P105, P108, P109, P111, P112, P118, P121, P142 | `fld-agentic-platform`, the factory's first run, Tier C (the tenant app baseline) and Tier R (read-tool agents) |
| §3 Before any Tier W agent writes | P3 (Tier W half), P13, P22, P24, P25, P30, P40, P46, P50, P52, P56, P57, P63, P64, P74, P76, P77, P79, P83, P87, P89, P96 (PL-*), P98, P99, P100, P103, P106, P110, P113, P115–P117, P120, P129, P130, P137 | Tier W and the first write of the first agent that writes (Stage 1 of that agent) |
| §4 Before the super-admin grant (Tier P / P-SA) | P7, P10, P14, P16, P29, P33, P60, P66, P68, P69, P90, P92, P93, P97, P101, P102, P131, P136, P138, P139, P143 | Tier P and the super-admin credential on `walle@`; the day "the four owner groups are one person" expires |
| §5 Before Wall-E's Stage 1 and its later stages | P17, P18, P19, P28, P34, P75, P107, P114, P125–P128, P132 | Wall-E's first super-admin write; F5/F7 above L3; `eve-advisor` |
| §6 Later | P6, P12, P15, P20, P23, P26, P27, P32, P91, P119, P122–P124, P133–P135, P140, P141 | the TISAX assessment order, the perimeter backstop, Tier X, the reclassification calendar |
| §6a Proposed by the setup procedures | P144–P191 | the setup step each row names; the records of [setup 03 §11](setup/03-decisions-and-people.md#11-the-checklist-every-decision-its-gate-and-its-signatories) |

Total rows: 191, each id once (P144–P191 added 2026-09-15). P3's row is in §3 (its spike 2 is the Tier W perimeter); its spike 1 also blocks the grant, through P90, and §4 says so there. P89 and P90 follow [06](06-gateways-model-armor-perimeter.md) §7's numbering (P89 = gateways born perimeter-ready, Tier W; P90 = the `run.allowedIngress` folder rule, the super-admin grant).

---

## 1. How this register works

**States.** `decided` — the named owner decided, on the date given. `closed` — answered by
a later row or by a verified fact; the row stays so the closing reference is not lost.
`proposed` — a detailed page proposed a value and recorded it; it stands, and the factory, the
validator and the runbooks are written to it, until the owner named in the row overturns it in
writing. `open` — nobody has decided and no page proposes beyond the HLD's recommendation; the
gate it blocks stays red. `spike` — decided by a test on a throwaway resource in nonprod, with
the pass criteria on the recording page; the spike's artefact closes the row.

**Who changes a row.** The owner of the row, in a pull request on this page, with the change
dated in the row. A row whose gate has passed is not reopened by editing it: it is superseded by
a new row that names it. Loosening a fleet default (P77) is a platform decision the security
reviewer signs, never a page edit.

**What "why it matters" answers.** The one thing that cannot be built, verified or defended
while the row is open — a factory input the pipeline refuses without, a safety case that rests
on an unverified name, an audit question with no evidence. If a row's "why" cannot be stated
that way, the row is a preference, not a decision, and it does not belong here.

**How a gate closes.** A gate is green when every row in its group is `decided`, `closed`,
`proposed` or a passed `spike`; an `open` row keeps the gate red. The admission gate (HLD
§5.3, [05](05-registry-and-autonomy-contract.md) §7) reads the tier gate's state, so a row left
open here is visible as a refused register row there, not as a forgotten paragraph.

**Numbering.** P1–P34 are the HLD's ids, unchanged. P35–P141 continue them in page order
(02: P35–P47; 03: P48–P59; 04: P60–P70; 05: P71–P80; 06: P81–P91; 07: P92–P103; 08:
P104–P114; 09: P115–P124; 10: P125–P132; 11: P133–P141). One HLD decision, P3, was refined by
page 06 without a new id; it keeps P3. P142 (factory and deploy identities) and P143 (owners
and gates for the HLD §18 propagation stage) follow P141. P144–P191 are the decisions the
setup procedures proposed on 2026-09-15, in SD order ([§6a](#6a-proposed-by-the-setup-procedures-p144p191)).
New decisions append at P192; ids are never reused.

**Where a decision is recorded.** Two places, doing different jobs, for every id alike — P rows
here, Wall-E's numbered decisions, Eve's `E-` rows and Mo's `M-` rows. (1) The reasoning lives on
the page named in the row's "Recorded in" (or, in an agent set, last) column; that page is what is
edited when the answer lands, and the row is an index, not the argument. (2) The answer lives in
[../../decisions/](../../decisions/) as a dated file `YYYY-MM-DD-<title>.md` carrying its id in
the first line. That directory is append-only: a later decision supersedes an earlier one and never
rewrites it. On 2026-09-14 it holds two records, P33's
([2026-09-13-wall-e-holds-super-admin.md](../../decisions/2026-09-13-wall-e-holds-super-admin.md))
and P34's ([2026-09-13-eve-reporting-path-may-reason.md](../../decisions/2026-09-13-eve-reporting-path-may-reason.md));
no numbered Wall-E decision and no `E-` or `M-` row has its own file yet. No program answers a
row: raising a level, widening a scope and answering any row are human acts, recorded by hand.

**Cross-references.** "Supersedes" means the platform row replaces the agent-set decision for
every agent, Wall-E included, and the propagation stage (HLD §18) adds the dated pointer line
in the agent set. "Depends on" means the platform row cannot pass its gate until that agent-set
decision is answered. "Generalises" means the agent-set decision stays true for its agent and
becomes the fleet rule.

---

## 2. Before the folder exists

The gate: `fld-agentic-platform` and its children, the factory's first run, Tier C (the tenant
app's baseline) and Tier R (read-tool agents). Nothing in this group needs a person who does not
exist on 2026-09-13; what it needs is a decision the factory can read. Rows are in id order.

| Id | State | Decision | Why it matters | Recommendation / value | Owner | Gate it blocks | Recorded in | Agent-set decisions |
|---|---|---|---|---|---|---|---|---|
| P1 | proposed (2026-09-13) | Adopt the tier model, the tier gate and the lettered tiers C/R/W/P(-SA)/X as the platform's | Every folder, factory module, monitoring baseline, metric pack, staffing minimum and compliance gate is keyed on the tier; without it "hundreds of agents" has no unit | as HLD §0.4 and §11; page 02 §1 fills every cell | the platform owner | everything | HLD §17; [02](02-landing-zone-and-tiers.md) §1 | generalises Wall-E 27 (bands), 37 (security reviewer); E-2 (owner groups per tier) |
| P2 | closed by P35 | Factory tooling | The factory is the mechanism behind PR1 and D1; no factory, no project-per-agent | Fabric modules with thin wrappers (P35); the HLD's options were the Cloud Foundation Fabric FAST project factory (its recommendation), bespoke modules and Config Controller | platform owner | Tier R | HLD §17; [02](02-landing-zone-and-tiers.md) §3.2 | — |
| P4 | spike (half closed by P42) | Which custom constraints the resource types support (`spec.identityType` on `ReasoningEngine`; Binary Authorization on `run.googleapis.com/Service`) | Decides whether D2 ("Agent Identity, bound to a gateway") is enforcement-grade or a CI check | `AuthProvider` is answered (supported, Preview, HLD §3.3); `secretmanager.googleapis.com/Secret` is a supported resource type and GA (control SK-1, [09](09-supply-chain-secrets-recovery.md) §2.6); CC-3 answers the Cloud Run half (P42); CC-8 on `ReasoningEngine` stays a spike — `ReasoningEngine` was not a custom-constraint resource type on 2026-09-13 ([09](09-supply-chain-secrets-recovery.md) P121) | platform owner | D2's enforcement grade | HLD §17; [02](02-landing-zone-and-tiers.md) §4.3 | depends on Wall-E 19 (Agent Identity adopted) |
| P5 | open (verify GA) | Cloud Run Agent Identity in prod when GA (Preview on 2026-09-10); nonprod now; dated service-account exceptions until then | Tier R prod projects carry a dated exception per Cloud Run workload until the stage changes; the drift job counts them | verify the launch stage at each factory release | platform owner | Tier R prod exceptions | HLD §17; [04](04-identity-and-privileged-access.md) §2 | generalises Wall-E 19 |
| P8 | spike (narrowed by P61) | Deny-policy permission names and the `principalSet://agents…` form | KF-2 of the fleet kill switch and rules R1–R5 rest on names Google's deny-supported list must contain | names verified on 2026-09-13 (P61); the agent principal-set spelling is proven on a throwaway engine before the folder policy is applied | platform owner | §4.5 of the HLD; K7 lever KF-2 | HLD §17; [04](04-identity-and-privileged-access.md) §3 | supersedes the per-project deny policy of Wall-E 12 §1.4 (promoted, not copied) |
| P9 | closed by P60 | PAB blocked-permission list: are `reasoningEngines.query` and `run.routes.invoke` blockable | Whether the PAB counts in any kill-switch table or safety case | enforcement version 4 blocks every `aiplatform` permission and has no effect on `run.routes.invoke`; KF-4 counts for engine queries only (P60) | platform owner, security reviewer | K7 lever KF-4; trust boundary B7 | HLD §17; [04](04-identity-and-privileged-access.md) §4 | — |
| P11 | open | SCC Premium funding and ownership | Tier C's detection desk is Google-run SCC findings; without the tier, no organisation-level inventory or ETD | Premium at organisation level, `eu` residency (P94); funding `Assumption:` platform line until IT security takes it | organisation IT security | Tier C | HLD §17, §0.5; [07](07-monitoring-detection-incident-response.md) §3 | — |
| P21 | open | Which public agent-threat taxonomy the detection catalogue is scored against | Coverage (a SOC metric, P103) has no denominator without it | OWASP Agentic / MAESTRO / other — verify currency before choosing | security reviewer | HLD §7.3 coverage line | HLD §17; [07](07-monitoring-detection-incident-response.md) §6 | — |
| P31 | open (`Assumption:` values stand) | Quota register and per-tier budget numbers | The factory writes a budget per project (P39) and the quota alerts (P83) fire on numbers; the numbers are guesses until someone owns them | keep HLD §3.1 and §3.4's `Assumption:` numbers; P39's mechanism does not depend on them | platform owner | Tier R | HLD §17; [02](02-landing-zone-and-tiers.md) §3.5 | generalises Wall-E 16 (budgets and caps) |
| P35 | proposed | Factory tooling: Cloud Foundation Fabric `project-factory`, `folder`, `organization`, `billing-account` and `agent-gateway` modules pinned at v58.0.0 (re-pinned only by pull request), wrapped by `agent-project` / `verifier-project` / `improver-project`; run by Cloud Build in `CICD_PROJECT`; FAST stages, Config Controller, Infrastructure Manager not adopted | The factory is the one thing that makes a project-per-agent affordable at hundreds; a fork of Fabric would be a second factory to maintain | adopt; where Fabric YAML cannot express an input, a resource in the wrapper — never a fork. Closes P2. Rejected: FAST stages end to end, a bespoke factory, Config Controller, Infrastructure Manager as runner | platform owner | Tier R | [02](02-landing-zone-and-tiers.md) §3.2, §7 | supersedes Wall-E SETUP Phase 6 (becomes a factory call, HLD §18 item 5) |
| P36 | proposed (narrowed by P142 on 2026-09-13: conditioned `projectIamAdmin`, no standing role on P-SA and controller folders, per-agent deployers, deny rule R6) | CI identity: `factory-apply@CICD_PROJECT` impersonated through WIF pool `wif-factory` with numeric-id attribute conditions, `main` only; two-phase apply — routine by the service account, privileged by a human through PAM (security reviewer approves at W+); §3.4's standing-role table is the ceiling; `pab-core-ci` bounds it to the folder | The factory identity can make every project; if it held `orgpolicy.policyAdmin` or `iam.denyAdmin` standing, a compromised pipeline could lift the fleet's own boundaries | impersonation over direct access; no org-policy, deny-admin or PAB-admin role ever standing on it. Rejected: direct resource access, a standing human deployer, a factory service account with `orgpolicy.policyAdmin` | platform owner; security reviewer | Tier R | [02](02-landing-zone-and-tiers.md) §3.4 | depends on P22 (git host); supersedes M-7's "admin-bypass" question for the platform repositories |
| P37 | proposed | Naming: `fld-<purpose>[-<tier>][-<env>]` folders, `agp-<tiercode>-<agent>-<env>[-<4 hex>]` project ids, the service-account, group, gateway, dataset, secret and tag-key patterns of §3.6; enforced by custom constraints CC-5 and CC-9 (Preview) after a 14-day dry-run | Reconciliation (P73) and every SIEM rule key on names; a free-form name is a row the drift job cannot attribute | adopt; existing handles (`WALLE_PROJECT`, …) stay as variables the register maps. Rejected: free-form names with a CI regex only | platform owner | Tier R | [02](02-landing-zone-and-tiers.md) §3.6 | supersedes Wall-E 2 (names) and topology §6 for new resources |
| P38 | proposed | Label taxonomy of §3.6 (thirteen keys, slug values, the register holds the exact values), CI-enforced and reconciled because no constraint can require labels; tags `agp-tier`, `agp-env`, `agp-tisax-scope` bound at folders for policy conditions; `tisax_scope` is a tag, not a label | Billing views, log views and inherited policy conditions all select by these keys; a project without them is invisible to the tier's controls | labels for billing and log views, tags for inherited policy conditions. Rejected: labels only; tags only | platform owner | Tier R | [02](02-landing-zone-and-tiers.md) §3.6 | — |
| P39 | proposed | Budget mechanism: one `google_billing_budget` per project at the tier default (50/90/100 % actual, 100 % forecast), one aggregate per tier by label at the billing account, channel `platform-budgets` plus the owner group, `budget-events` topic in `LOGGING_PROJECT`, no automatic billing disable | A runaway agent is a cost incident before it is a security one; but billing-disable would be a kill switch nobody drilled — the levers are K0–K7 | adopt; amounts stay P31 | platform owner | Tier R | [02](02-landing-zone-and-tiers.md) §3.5 | generalises Wall-E 16; topology 52 (budgets per project) |
| P41 | proposed | Baseline additions beyond HLD §3.3: `storage.publicAccessPrevention`, `gcp.detailedAuditLoggingMode`, `compute.skipDefaultNetworkCreation`, `sql.restrictPublicIp`, `iam.managed.workloadIdentityPoolProviders` (core); the three KMS constraints of B21 ([09](09-supply-chain-secrets-recovery.md) P118) and `gcp.restrictTLSVersion` (B22, [08](08-data-logging-retention-sovereignty.md) §7.1); the managed key-upload constraint fleet-wide; legacy `iam.allowedPolicyMemberDomains` kept until Google retires it; `iam.managed.disableAccessPolicyBinding` enforced at the platform folder and lifted at the agent and controller folders; a 14-day dry-run for every new constraint; CMEK constraints held | The folder baseline is the trust boundary the agent cannot redraw (HLD CP1); each missing constraint is a class of misconfiguration the drift job would otherwise have to detect | adopt; the security reviewer approves changes | platform owner; security reviewer | Tier R | [02](02-landing-zone-and-tiers.md) §4.1 | supersedes topology §5's per-project checklist |
| P42 | proposed, with three spikes | Custom constraints now: CC-1, CC-2 (Google's gateway constraints on `ReasoningEngine`), CC-3 (`custom.runBinaryAuthorizationRequired` via the v1 annotation — the Cloud Run half of P4), CC-5/CC-9 naming (Preview), CC-7 `AuthProvider`; held: CC-4 internal ingress with B16 until P3; spikes: CC-6 AllowPolicy (PAM interaction, agent-principal eligibility), CC-8 `identityType` (the other half of P4), CC-11 LogSink actor exclusions; CC-10 label enforcement impossible | Each verified constraint moves a control from detection to enforcement; each unverified one left in the baseline would be a paper control | adopt the verifiable set; run the three spikes on throwaway resources in nonprod | platform owner | D2's enforcement grade | [02](02-landing-zone-and-tiers.md) §4.3 | promotes Wall-E 13 §7's CC-1/CC-2 facts; depends on P4 |
| P43 | proposed | A tier is a folder; a tier change is a new register row, a new project in the new folder and a `revoke` of the old; projects are never moved after creation except the `GEMINI_PROJECT` import; `roles/resourcemanager.projectMover` only through PAM with the security reviewer; only humans write `tier` (machines may only set `status: halted`) | Moving a project changes every inherited policy at once with no dry-run; "humans raise, machines lower" applies to tiers as much as to ladder cells | adopt; `MoveProject` inside the folder is a SIEM rule. Rejected: project moves with a change window | platform owner | — (a rule from the first project) | [02](02-landing-zone-and-tiers.md) §1.4 | — |
| P44 | proposed | `gcp.restrictServiceUsage` in allow-list mode on every folder with §4.2's lists; fleet-kill lever KF-1 is the pre-written replacement policy lacking `aiplatform` and `run` (allow and deny modes are mutually exclusive, so it is a replacement, not an addition); the P-SA folder's list is generated from Wall-E's register row and the factory refuses a P-SA manifest needing a service outside it | KF-1 is the fastest fleet stop (< 60 s, P99) and the one that needs no name verification; an allow-list is also how Tier R has no write path (no `secretmanager`) | adopt; exact lists confirmed by the first nonprod factory run. Rejected: denylist mode | platform owner; security reviewer | Tier R; K7 | [02](02-landing-zone-and-tiers.md) §4.2 | promotes Wall-E PREREQUISITES §4's API lists; generalises Wall-E 13's "Required APIs" |
| P45 | proposed | The shared / per-agent split of §5: register of record, shared Agent Registry, evidence lake, ladder-state page, K7 job, reconciliation and drift jobs in `CORE_PROJECT`; sink destination, log buckets, views, billing export, `budget-events` in `LOGGING_PROJECT`; supply chain in `CICD_PROJECT`; validator custodian in `VALIDATOR_PROJECT`; the template standard, monitoring baseline, `audit.schema`, `ladder.schema` and manifest schema as repository modules instantiated per project; engines, gateways, action services, secrets, Firestore, `ladder.yaml`, audit datasets, content bucket, templates, verifier keys and behavioural baselines per agent | What is shared is what a compromised agent must not reach; what is per agent is what its teardown must remove (D10) | adopt; the review's A3 shared-candidate list rejected where it differs (for the approval surface see P46) | platform owner | Tier R | [02](02-landing-zone-and-tiers.md) §5 | generalises topology "the one rule" and §3's grant rows as factory inputs; topology 45 (subscriptions in the consumer's project), 48 (carve-outs), 50 (CI reader) |
| P47 | proposed | Promotion map of §8: `wall-e/11`, `12`, `13` and `project-topology.md` are seeds the platform pages reference (rule at platform level, mechanism and verification steps in the chapter); per-agent content stays in the agent sets with one framing line; nothing duplicated | Two copies of a security chapter drift; a reviewer must find one rule in one place | reference, do not copy | platform owner | the documentation reconciliation that precedes the S1 documentation tag (P130) | [02](02-landing-zone-and-tiers.md) §8 | frames Wall-E 11, 12, 13 and topology 42–52 (HLD §18 item 8) |
| P48 | proposed | `GEMINI_PROJECT` imported in place under `fld-gemini-enterprise`; the folder gains `discoveryengine.managed.allowedDataSources` and `allowedEgressFqdns` (both with `enforcedProjects` = `GEMINI_PROJECT_NUMBER`, since neither binds a non-VPC-SC project otherwise), the custom-MCP block kept enforced, `custom.geDataStoreAclRequired` in dry-run; the project hosts the app, `gemini-egress`, `gemini-registry`, `ge-console-standard`, the CMEK registration and nothing of any agent's | The tenant app is the one shared front door; if it lived in an agent's project, that agent's teardown or compromise would reach every user's assistant | import and move; fallback (dated) = stay outside the folder with project-level re-application | platform owner; Gemini Enterprise admin | Tier C | [03](03-gemini-enterprise-environment.md) §3 | supersedes topology 52 (the Gemini project's identity) for placement |
| P49 | proposed | Administration model: `ge-admins@` holds `roles/discoveryengine.agentspaceAdmin` only through PAM (1 h, security-reviewer approval); `ge-readers@` standing viewer; the CI identity standing `editor` as the one foreign principal; no basic roles; no project-level `agentspaceUser` | A standing app admin can publish any agent to every user with no record; PAM makes each publication a windowed, approved, logged act | PAM, not a standing admin; a standing admin for one person rejected (no record, no window) | platform owner | Tier C | [03](03-gemini-enterprise-environment.md) §4 | — |
| P51 | proposed (P24 stays open for other populations) | Identity provider Google Identity for the `eu` location (`wall-e/12` §5 case (a)); any change is a decision record stating the chat-history loss; P24 answered "no" for the tenant app | Changing the provider later deletes conversation history and breaks every `agentspaceUser` binding; it must be chosen before the first user | Google Identity; Workforce Identity Federation rejected (no live group re-check on the write path, two-step removal, K5 needs a Google identity anyway) | Gemini Enterprise admin | Tier C | [03](03-gemini-enterprise-environment.md) §6; [../gemini-enterprise.md](../gemini-enterprise.md) | promotes Wall-E 12 §5 (operator identity cases a/b); answers Wall-E 20 for the front door |
| P53 | proposed | Console Model Armor on, per app, template `ge-console-standard` in `GEMINI_PROJECT` from the Tier C row of the template standard, failure mode Block; a flip to Allow is a PAM act with an incident reference | Tier C agents have no gateway; the console setting is their only prompt screen, and fail-open would make it decorative | on, Block; Allow on failure rejected (HLD §6.1's rule) | Gemini Enterprise admin; template by platform owner | Tier C | [03](03-gemini-enterprise-environment.md) §9; [06](06-gateways-model-armor-perimeter.md) §3.5 (P87) | promotes Wall-E 11 §2's "console setting covers Tier C only" |
| P54 | proposed | Feature Management baseline of §8 as a committed file; sharing requires admin approval; sharing to groups; skills, memory, session sharing, model selector, image and video generation off | Every toggle is a data path; a console habit without a committed file has no drift check | as §8 | Gemini Enterprise admin | Tier C | [03](03-gemini-enterprise-environment.md) §8 | — |
| P55 | proposed | Population = three gates: the OU service toggle (robot OU off), app-level `agentspaceUser` to `ge-users@`/`ge-builders@`, automatic licence assignment within those gates with a quarterly review | The robot accounts must never hold a front-door licence (a second path into the assistant under a super-admin identity); licences by hand at scale have no record | automatic licences within the gates; manual licences per person rejected (a console habit without a record) | Gemini Enterprise admin; Service Settings admin | Tier C | [03](03-gemini-enterprise-environment.md) §7 | generalises Wall-E 5 (OUs) for the front door |
| P58 | proposed | Connector allow-list as organisation policy: first-party Workspace sources only at first; custom MCP data stores blocked; egress FQDNs restricted; no connector holds a write credential; every connector is a supplier row | A connector is a credentialed data path into the tenant; a console allow-list is not enforced and not reviewed | org policy, not a console list | platform owner; IT security | Tier C | [03](03-gemini-enterprise-environment.md) §12; [11](11-tisax.md) §8 (P140) | — |
| P59 | proposed | No second production app; one throwaway app per spike, deleted at the end; a sandbox-tenant app is decided with decision 29's sandbox tenant | A permanent nonprod app in the production tenant is a second front door with the same licences and audience | no permanent nonprod app in the production tenant | platform owner | — (a rule from Tier C) | [03](03-gemini-enterprise-environment.md) §3 (row "The app") | depends on Wall-E 29 (sandbox tenant, reversed to a tenant in HLD §3.1) |
| P61 | spike (names verified; spelling to prove) | Folder deny policy `deny-agents-platform` rules R1–R5 (+R3b `actAs` exception for the deployer; R6, the `factory-apply@` credential fence, added by P142 on 2026-09-13) with every permission verified on the deny-supported list; `reasoningEngines.streamQuery` dropped for the `reasoningEngines.*` wildcard; `cloudscheduler.*` and `iam.denypolicies.*` recorded as not deniable; P8 narrowed to the agent principal-set spelling, proven on a throwaway engine | The deny policy is the folder's enforcement-grade ceiling on every agent principal (HLD CP3); one wrong name is a rule that silently never matches | adopt; the factory emits `attribute.platformContainer/aiplatform/projects/N` for allow and deny and `attribute.container/projects/N` for PAB until the spike says otherwise | platform owner | the folder baseline (Tier R) | [04](04-identity-and-privileged-access.md) §3 | supersedes Wall-E 12 §1.4's per-project deny policy; closes topology's per-project copies |
| P62 | proposed | PAM catalogue without `roles/owner` (PAM does not support basic roles): `ent-project-repair` is a predefined-role bundle, `roles/admin` deferred until GA; org-policy, deny-admin and PAB-admin entitlements at the organisation (forced by those roles' lowest grant level; `roles/iam.denyAdmin` is organisation-level, so deny-policy edits sit in `ent-platform-policy`); `ent-k7-human` and `ent-k7-executor` activate without approval and page; `roles/privilegedaccessmanager.admin` is the one standing administrative role; grants no shorter than the 30-minute floor | "No standing owner anywhere" is the TISAX 4.1.x evidence and the reason a stolen operator session cannot repair a project; the K7 entitlements must not wait for an approver at 03:00 | adopt; `roles/admin` now rejected (Preview); a folder-level org-policy entitlement is impossible | platform owner, security reviewer | Tier R (no standing owner); K7 | [04](04-identity-and-privileged-access.md) §5 | generalises Wall-E 18 (split control plane) on the human side |
| P65 | proposed | `factory-groups@` (a service account with the Workspace Groups Admin role — with `walle@` and `eve@` the only machines holding a Workspace admin role, recorded as privilege `workspace_role:groups_admin`) makes agent groups from the register; control groups are on a hand-managed two-human-merged list the job refuses in code; every platform group is a security group; `<agent>-owners@/-operators@/-readers@/-users@` | Groups by hand do not scale to hundreds; a Groups Admin robot that could edit `eve-owners@` would be a path around Eve's independence, hence the refused list | adopt; groups by hand for everything rejected at hundreds of agents; one service account with Super Admin refused by fact and by design | platform owner | Tier R factory | [04](04-identity-and-privileged-access.md) §2.4 | depends on Wall-E 2 (group names); generalises topology 52 (owner groups) |
| P67 | proposed | K7 executor job `k7-executor` in `CORE_PROJECT` (attested image, four committed lever files, identity `k7-executor@` requesting its own PAM grant — a verified requester type); drill calendar: monthly nonprod full drill on a canary engine, quarterly manual and SIEM-path drills, quarterly KF-3 in prod on `fld-agents-r/prod`, semi-annual KF-1 in prod on `fld-agents-r/prod` only, no production P-SA drill; the WIF-principal / standing-custom-role contingency kept for the first drill; a drill older than 30 days freezes raises | A kill switch that was never pulled is a hypothesis; a drill in prod on the top tier is an outage the tiers below P do not need | adopt; monthly prod drills rejected (a monthly fleet stop in prod is an availability cost the tiers below P do not need) | platform owner, security reviewer | Tier R (K7 mandatory from R); Tier X's six-month drill history | [04](04-identity-and-privileged-access.md) §9 | depends on E-15 (where drills run) |
| P70 | proposed | K7's scope excludes `fld-controllers`, `fld-platform-core` and `fld-gemini-enterprise`; lever order KF-1, KF-3, KF-4, KF-2; K4 is pulled before K7 in the P-SA runbook (KF-1 makes the K4 endpoint unreachable); lifting K7 is a two-human pull request through `ent-platform-policy` that re-asserts K0 `no_writes` on Tier P and K1 on Tier W | Eve must keep paging under K7 (CP5 primitive); lifting the fleet stop must not silently restore autonomy | adopt; including the controllers in K7's scope rejected | platform owner | the incident runbooks ([07](07-monitoring-detection-incident-response.md) §11) | [04](04-identity-and-privileged-access.md) §9.1, §9.5, §9.7 | depends on E-5 (Eve-down timer ownership) |
| P71 | proposed | One shared Agent Registry in `CORE_PROJECT`, `europe-west1`; `agentregistry.googleapis.com` excluded from every tier folder's `restrictServiceUsage` allow-list so no local registry can exist; a nonprod spike confirms automatic registration has nowhere to land and that an engine and its bound egress gateway still work with the API disabled in the engine's own project — if not, a per-agent-project working-set registry (the `GEMINI_PROJECT` pattern) is recorded by a new dated row overturning the exclusion; quota increase at 60 % occupancy; per-tier-folder sharding as the recorded fallback | "Registered ⇒ feeding the SIEM" (P74) is only checkable if there is one place to be registered | (a) shared, over (b) per agent and (c) per tier folder | platform owner | Tier R (a Tier R gate item) | [05](05-registry-and-autonomy-contract.md) §2 | promotes Wall-E 13 §2 (registration never authorises); extends topology 43 (no `agentregistry.viewer` for agent principals; `eve-owners@` humans may hold it) |
| P72 | proposed | Card metadata carried by the register row, projected into project labels and a fixed-format first line of the registry entry description (`meta: agent_id=… tier=… owner=… risk_class=… ai_act_class=… tisax_class=… status=… register_sha=…`); HLD §3.4's "labels on every registry card" corrected — the `Service` resource has no labels or annotations | Reconciliation and the compliance exports need the classification on the object the registry actually stores | description line, over A2A card metadata or a sidecar file | platform owner | Tier R | [05](05-registry-and-autonomy-contract.md) §3 | supersedes Wall-E 13 §3's card content rule for metadata |
| P73 | proposed | Reconciliation as a second schedule of the drift job under `platform-drift@CORE_PROJECT` (HLD §18 item 25 exception extended: `cloudasset.viewer` on the folder, `agentregistry.viewer`, `bigquery.jobUser`, WRITER on `platform_registry`, `run.invoker` on each W+ `/v1/control/halt`, `securitycenter.findingsViewer` at the organisation); daily full run 06:00 Europe/Paris plus feed-driven incremental within 15 min; five sources, S4 SCC (AI Protection's inventory, the cross-check the platform does not build) a weekly human cross-check until export is proven, S5 method *tbd*; severities per tier ([05](05-registry-and-autonomy-contract.md) §6.2); "registered ⇒ feeding" evaluated every run | An agent that exists without a register row, or a row without an agent, is the inventory failure every assessor asks about first (TISAX 1.3.1) | reuse the drift job's principal rather than a second folder-level principal; hourly full runs rejected | platform owner; security reviewer for the exception | Tier R | [05](05-registry-and-autonomy-contract.md) §6 | supersedes Wall-E 46 (closed by the drift job, HLD §4.7); topology 46 |
| P78 | proposed (bindings at R; writes at W) | Fleet peer rule as code: `agent` ceiling column fixed by the schema (`code: true`, L5 READ / L0 every write); the action service sets `tainted` at run open and refuses writes from agent principals with `p:actor_not_authorised`; `iap.egressor` bindings generated from `peers[]` only, whole-registry targets refused, `DRY_RUN` 30 days then `ENFORCE`; drift-job assertion daily | D6 (every peer is L0 for writes, tainted on receipt) must be a schema fact and a binding, not a review comment | generated bindings over hand-made bindings with review | platform owner | Tier R (bindings); Tier W (writes) | [05](05-registry-and-autonomy-contract.md) §9.7 | promotes Wall-E 13 §5 (peer trust, the `agent` column); answers Wall-E 23 (A2A) for the fleet |
| P80 | proposed | `ADMIN_READ` Data Access audit logs for `agentregistry.googleapis.com` in `CORE_PROJECT` | Who read the fleet inventory is an evidence question after any incident; the registry is small and the cost negligible | on; off (cost) rejected | platform owner | Tier R | [05](05-registry-and-autonomy-contract.md) §10 | — |
| P81 | proposed | Gateway binding standard: egress gateway for every engine above Tier C; ingress gateway whenever the register row's invoker list is non-empty; the essential-endpoint set as one Terraform local in `CORE_PROJECT` spliced into every policy; nonprod gateways in `DRY_RUN`, prod enforcing from the first deploy (the tenant app's 30-day dry-run the one exception); the front door's `streamQuery` method measured in nonprod | PR3 (default-deny egress, screened ingress) is the promise every agent gets; a prod dry-run per agent would be an unscreened window per agent | as [06](06-gateways-model-armor-perimeter.md) §2.1–§2.3; rejected: 14-day prod dry-run per agent | platform owner | Tier R (first factory-made prod project) | [06](06-gateways-model-armor-perimeter.md) §2 | promotes Wall-E 13 §7 (gateway facts, binding); supersedes Wall-E 21 for the gateway half; E-19 (Eve's gateway) |
| P82 | proposed (adopted now) | Gateway over Agent Platform Threat Detection at every prod tier (mutually exclusive on a bound engine); nonprod rotation of at most one unbound engine per tier folder for at most 30 days, opened and closed by a factory input; reconciliation pages at two; revisit on compatibility or GA | The two products cannot coexist on one engine; the platform must say which one the safety case rests on | as [06](06-gateways-model-armor-perimeter.md) §2.4 | platform owner; security reviewer at Tier P | none — adopted now | [06](06-gateways-model-armor-perimeter.md) §2.4; [02](02-landing-zone-and-tiers.md) P40 | — |
| P84 | proposed (redesigned 2026-09-13: Google defines template conformance at organisation/folder level and inline enforcement at project level only; project floors override folders and can disable them) | Floor hierarchy: organisation floor (IT security) = prompt-injection/jailbreak at `HIGH` or stricter + malicious URL + `enableCloudLogging` + multi-language; `fld-agentic-platform` adds Responsible AI at `MEDIUM_AND_ABOVE`; tier floors (W, P, P-SA, controllers) tighten to the measured level — all template conformance only; a `Custom` project floor generated by the privileged phase in every agent project carries inline enforcement (`INSPECT_ONLY` at R and before measurement, `INSPECT_AND_BLOCK` at W/P/controllers after, always at P-SA); any other project floor write, `Inherit` or `Disable` is severity-1 drift; `floorSettingsAdmin` at project level only via PAM | The floor is the one Model Armor setting an agent's owner cannot lower; `INSPECT_AND_BLOCK` at the organisation would buy no safety case (fail-open path) and cost organisation-wide refusals; and because a project floor overrides its parents, "never looser" can only be a property of who writes project floors and how fast a write is seen | as [06](06-gateways-model-armor-perimeter.md) §3.2 | IT security (organisation floor); platform owner (folders) | Tier R (first project); IT security handover date *tbd* | [06](06-gateways-model-armor-perimeter.md) §3.2 | promotes Wall-E 11 §2 (the layer model) |
| P85 | proposed | Template standard per tier including SDP: basic SDP at R; advanced SDP with a fleet-wide de-identify template at W+; custom dictionary/regex infoTypes for the hard-denied vocabulary on the P-SA response template (detection-grade, behind the action service's list); `STABLE` filter version; `INSPECT_ONLY` in nonprod, flip to `INSPECT_AND_BLOCK` by pull request once the tier's benign-corpus false-block rate is below a measured threshold (`Assumption:` 1 %); `logSanitizeOperations` only after the content bucket and sink exist | A template that blocks before its false-block rate is measured stops legitimate work and teaches operators to route around it; a template that never blocks is detection only | as [06](06-gateways-model-armor-perimeter.md) §3.3 | platform owner; security reviewer for P-SA | Tier R; the P-SA threshold blocks the super-admin grant | [06](06-gateways-model-armor-perimeter.md) §3.3 | supersedes Wall-E 24 (blocking threshold) with a measured rule; promotes Wall-E 11 §4 |
| P86 | proposed | Alerting on floor and template writes: Admin Activity logs for `modelarmor.googleapis.com` in the aggregated sink, a log-based alert in `LOGGING_PROJECT`, a SIEM rule from Tier P, PAM audit on `floorSettingsAdmin` grants; a floor write outside a PAM grant with a ticket = severity 2; any write by a non-Terraform principal = severity 1 at Tier P; the SCC path for floor writes *tbd* pending the resource-type check; `wall-e/11` §6's alerts promoted into the monitoring baseline module | The floor is only a boundary if lowering it is seen | as [06](06-gateways-model-armor-perimeter.md) §3.4 | platform owner; security reviewer owns rule content | Tier R | [06](06-gateways-model-armor-perimeter.md) §3.4 | promotes Wall-E 11 §6 |
| P88 | proposed | Tool-result screening per tier: Tier R read tools go over MCP through the egress gateway so Model Armor screens `tools/call` responses fail-closed (enforcement), with CEL on `mcp.toolName` and `mcp.tool.isReadOnly`; a REST read API needs a supplier row and a reason; Tier W+ action services screen attacker-writable fields themselves (`wall-e/11` option B) with `sanitizeUserPrompt`, correlation id = audit id, `MATCH_FOUND` sets the taint bit and the ceiling module caps the run; option A (action service as an MCP server) per agent at Stage 3, never a platform default | "Tool results are not screened by the gateway" is the injection path every read tool opens; the platform must say per tier who screens them | as [06](06-gateways-model-armor-perimeter.md) §2.6 | platform owner; agent owner for option A | Tier R | [06](06-gateways-model-armor-perimeter.md) §2.6 | supersedes Wall-E 22 (MCP server) as a per-agent Stage 3 choice; promotes Wall-E 11 §3, 13 §6.3 |
| P94 | proposed | SCC Premium at organisation level with ETD (incl. the eight Workspace findings), SHA + custom modules, AI Protection, Sensitive Actions, Agent Platform Threat Detection nonprod only; Enterprise not chosen (deprecated 2026-05-21, shutdown 2027-05-21); SCC residency `eu` (Model Armor, AI Discovery and Gemini inventory are restricted only in KSA; `eu` loses the Compliance page's resources-scanned counts, some Cloud Run/Container Threat Detection detectors, and Data Location terms for Preview services), with the disabled-detector list confirmed before activation; `_Required`/`_Default` never redirected; one org-level notification config to Pub/Sub in `CORE_PROJECT` | Tier C's detection desk is SCC; choosing the tier that Google is retiring would be a migration during Tier P | Premium, `eu` | organisation IT security | Tier C | [07](07-monitoring-detection-incident-response.md) §3 | funds P11 |
| P95 | proposed | Monitoring baseline module variants (`read` / `write` / `credentialed`), heartbeat cadences (daily / hourly / 5 min), absence windows (— / 2 h / 15 min heartbeat; — / 4 h / 60 min audit rows), admission steps A1–A6, unpublish-on-silence with silence budgets 24 h R / 4 h W / 15 min P (aligned with P74) | PR4 ("somebody is paged if your logs stop") is a module instantiated per project, or it is a promise | as [07](07-monitoring-detection-incident-response.md) §4 | platform owner (Eve owner for P windows) | Tier R (module); Tier P (SIEM invariant) | [07](07-monitoring-detection-incident-response.md) §4 | depends on E-10 (how Eve availability is measured); E-18 (windows) |
| P104 | proposed | Central logging topology: `S-org` (organisation sink, no `includeChildren`, organisation-level Workspace and org audit entries) and `S-folder` (`fld-agentic-platform`, `includeChildren`, intercepting, five `cloudaudit` families) into `LOGGING_PROJECT` as a project destination; fan-out sinks to the locked evidence bucket `platform-evidence-logs`, the identity bucket `platform-identity-logs`, BigQuery `platform_logs` and per-agent trigger topics; per-agent access by log views and authorised views; Wall-E's two organisation sinks deleted and re-homed; Eve's organisation sink kept independent | CP6 (evidence the judged thing cannot forge or silence) is two sinks and a locked bucket, or it is nothing; N sinks at the organisation is N `configWriter` grants to review | adopt; rejected: one sink per destination at organisation level (N destinations, N `configWriter` grants), no interception (N+1 billed, shortenable copies), interception of all families (would pull content logs out of the agent's restricted bucket) | platform owner | Tier R (the factory's `platform-core` module) | [08](08-data-logging-retention-sovereignty.md) §3 | supersedes Wall-E SETUP Phase 11's organisation sink (HLD §18 item 5); topology rows 22–23 retired |
| P105 | proposed | Data Access `auditConfigs` at `fld-agentic-platform` for `secretmanager`, `cloudkms`, `iap`, `firestore`, `aiplatform`, `discoveryengine`, `sts`, `logging` (`DATA_READ`), `agentidentity`; `iam`/`orgpolicy` at `ADMIN_READ`; `storage` `DATA_READ/WRITE` at project level on `LOGGING_PROJECT`, `CORE_PROJECT`, `EVE_PROJECT`, `EVE_WITNESS_PROJECT`; no `exemptedMembers`; cost on `LOGGING_PROJECT`'s platform line; a daily Data Access canary job | Who read a secret or queried an engine is the forensic question; a per-project config can be switched off by the project's owner, a folder one cannot | adopt; `allServices` rejected (cost without a consumer) | platform owner (payer); security reviewer for the list | Tier R | [08](08-data-logging-retention-sovereignty.md) §4 | supersedes topology 47's per-page assertion (the actor-exclusion check reads the authorised view) |
| P108 | proposed (discovery at R; de-identify job at W) | Sensitive Data Protection discovery at the folder over BigQuery and Cloud Storage, service-agent container `LOGGING_PROJECT`, `secret`-class stores excluded by register label, publish to SCC/BigQuery/Pub/Sub, a declared-versus-discovered reconcile job; one de-identify template `platform-log-deid-v1` (deterministic encryption for identifiers, redaction for credentials; + `platform-log-inspect-v1`) in `CORE_PROJECT` applied daily `content_raw` → `content_deid` by a Cloud Run job and referenced by the tier Model Armor templates; content buckets with restricted fields, one generated view, `viewAccessor` + `fieldAccessor` for `<agent>-operators@` and `platform-security@` only, never a linked dataset | Content logs hold employee data (Wall-E 25); a declared data class nobody checks against what the stores actually contain is the finding that fails 1.3.2 | adopt; a de-identify pass on the log bucket is impossible; SDP as a Model Armor filter only (status quo) rejected | platform owner (template); agent owner (bucket, job) | Tier R (discovery); Tier W (de-identify job) | [08](08-data-logging-retention-sovereignty.md) §6 | supersedes Wall-E 25 (telemetry content) with a de-identified copy; promotes Wall-E 11 §4/§6's SDP *tbd* |
| P109 | proposed | Five platform data classes (`evidence`, `content`, `control`, `record`, `secret`, `ops`) as the per-store vocabulary; every store classified in §2.2; `walle_metrics_private` class `secret`; asset, data, custodian and reviewer roles per project including `KMS_PROJECT`; mapping to the organisation's scheme *tbd* by the ISMS | Retention, CMEK, access and the TISAX label all key on the class; without a per-store class the schedule (P106) has nothing to apply to | adopt until the ISMS mapping exists; reusing the organisation's scheme directly is blocked while it is *tbd* | platform owner; ISMS for the mapping | Tier R; the TISAX assessment order for the mapping | [08](08-data-logging-retention-sovereignty.md) §2, §8 | generalises topology 52 (owners per project); M-3 (who reads `walle_metrics`) |
| P111 | proposed (evidence projects at R; the rest at P) | Access Transparency verified enabled at Stage 0 and routed to S12 and the SIEM (quarterly manual check, no API found); Access Approval on `fld-agents-p`, `fld-controllers`, the witness, plus `LOGGING_PROJECT` and `CORE_PROJECT`, and (because Access Approval covers Agent Runtime and Agent Identity at GA) `fld-agents-w`; approvers `platform-security@` + platform owner; Pub/Sub to the SIEM; auto-approved requests a severity-2 rule; the setting disabled a severity-1 rule; Google-managed signing key at Stage 0 | Google-personnel access to the evidence and the super-admin projects is the 5.3.x shared-responsibility line an assessor asks for | adopt; organisation-wide Access Approval rejected (slows every support case outside the platform; IT security may still choose it) | IT security | Tier P per the HLD; the evidence projects at Tier R | [08](08-data-logging-retention-sovereignty.md) §7.2 | — |
| P112 | proposed | Data-at-rest for page 08's stores: CMEK with an explicit HSM key `platform-logs-europe-west1` in `KMS_PROJECT` (ring `logging`) on `platform-evidence-logs` and `platform-identity-logs`, because Cloud Logging is not on Autokey's list and CMEK cannot be added or removed after bucket creation; Autokey elsewhere (P118); Eve's evidence bucket and datasets on an explicit HSM key `eve-evidence` in `EVE_PROJECT` ring `eve` (not Autokey, for independence); the witness never on a tenant key; content buckets Google-managed at R, CMEK from W; no Key Access Justifications. Resolved against P119 exception 2 on 2026-09-13 — see §7 row 2 | The choice is made once at bucket creation; the evidence class at the Confidential label is the one place where "can't add CMEK later" is a permanent finding | adopt; Google-managed on the central buckets (P119's position) rejected for the evidence class | platform owner | Stage 0 (the buckets are created once) | [08](08-data-logging-retention-sovereignty.md) §9 | supersedes Wall-E 31's evidence-copy encryption line; E-14 |
| P118 | proposed | HSM everywhere and a key project: `constraints/cloudkms.allowedProtectionLevels = HSM`, `cloudkms.disableBeforeDestroy` enforced, `cloudkms.minimumDestroyScheduledDuration` of 30 days (no key destroyed sooner) at `fld-agentic-platform`; Autokey configured at the folder with a dedicated key project `KMS_PROJECT` under `fld-platform-core` — a fifth core project holding keys only | One rule instead of a per-class argument; the approval keys (Eve's signature) and the evidence keys must not sit in a project an agent's teardown or owner reaches | adopt; HSM cost per key version accepted, small at this fleet size (amount *tbd*); HSM for approval keys only with SOFTWARE elsewhere (HLD §8.3's first position) rejected | platform owner | Tier R (precedes the first factory run) | [09](09-supply-chain-secrets-recovery.md) §2 | supersedes Wall-E 36 (Stage 0 provisions the KMS key) for placement; E-3/E-16's key location |
| P121 | proposed | No code execution below Tier X, enforced per runtime: GKE by `restrictServiceUsage` (no `container.googleapis.com` outside `fld-agents-x`); Agent Runtime sandboxes by the absence of any create permission on agent identities plus a CI ban on the sandbox SDK and the ADK code-execution tool, a severity-1 log alert and a daily sandbox listing; Cloud Run sandboxes by image scan for the `sandbox` binary until the Pre-GA field is verified; `capabilities.code_execution: false` as the manifest default the validator enforces. Records that no organisation-policy constraint for Agent Runtime code execution exists and that `ReasoningEngine` is not a custom-constraint resource type on 2026-09-13 | Code execution is the capability that turns an injected prompt into an arbitrary program; below Tier X it must be impossible, not merely unconfigured | adopt | platform owner | Tier R (folder allow-lists) | [09](09-supply-chain-secrets-recovery.md) §4 | — |
| P142 | proposed (2026-09-13) | **Factory and deploy identities, one model for every page.** The routine identity `factory-apply@CICD_PROJECT` creates projects and non-credential infrastructure, is the only standing registry writer, holds in-project IAM power only as `modifiedGrantsByRole`-conditioned bindings and nothing on `fld-agents-p-sa-*` or `fld-controllers-*`; credential-path bindings are applied in the privileged phase by a human through PAM; P-SA and controller projects are applied end to end under `ent-factory-singleton` (a dated, time-boxed exception on topology decision 48 and HLD §18 item 25); each agent project has its own `<agent>-deployer@` and `platform-deployer@CICD_PROJECT` is retired; deny rule R6 and a factory negative test at every release fence the factory. The model: [02 §3.4](02-landing-zone-and-tiers.md#34-the-ci-identity-and-the-two-phase-apply-p36) (phases, roles, condition, negative test), [04](04-identity-and-privileged-access.md) §3 and §5.2 (R6, `ent-factory-singleton`), [09](09-supply-chain-secrets-recovery.md) §1.7 (the deployer), [../project-topology.md](../project-topology.md) §3 row 37 | Three pages had three deployers (09: one CI-project deployer; 02: `factory-apply@` making engines and holding the registry; 04: a per-agent deployer), so the deny-policy exceptions, the PAB, the drift job's expected set and the registry's "only CI writes" control rested on different principals; and an unconditioned folder-level `projectIamAdmin` inherited into `WALLE_PROJECT` could bind itself the super-admin secret, which HLD §4.4 allows only through a 30-minute approved PAM grant | as stated; rejected: an unconditioned standing `projectIamAdmin` with detection only, and one CI-project deployer for every agent (one compromise deploys the fleet) | platform owner; security reviewer signs R6 and the condition lists | Tier R (the factory's first run); `ent-factory-singleton` before the first controller project (Tier W) | [02](02-landing-zone-and-tiers.md) §3.3, §3.4; [04](04-identity-and-privileged-access.md) §2.3, §3, §5.2; [05](05-registry-and-autonomy-contract.md) §4; [09](09-supply-chain-secrets-recovery.md) §1.7; HLD §4.3, §18 item 25 | closes P22's deployer half; extends P36; dated exception on topology 48; generalises `wall-e/12` §6 (deployer pattern) |

---

## 3. Before any Tier W agent writes

The gate: Tier W opens (HLD §0.4: autonomy contract, platform verifier, validator custodian,
nonprod folder, one restore drill) and the first agent that writes to a system of record takes
its first write — Stage 1 of that agent. T0–T3 are the ladder's trigger classes, not tiers, so
the tier that first *acts* is Tier W. It
needs the second operator, the part-time security reviewer and the blind grader (HLD §0.3).

| Id | State | Decision | Why it matters | Recommendation / value | Owner | Gate it blocks | Recorded in | Agent-set decisions |
|---|---|---|---|---|---|---|---|---|
| P3 | spike (refined by page 06, id kept) | Perimeter model, two spikes on a throwaway engine: spike 1 (engine reach through a regional internal Application Load Balancer via serverless NEG, fallback PSC endpoint; ID-token claims intact; direct `run.app` refused; dry-run log shows only the registered destination), after which (b)'s folder policy is applied; spike 2 (dry-run then enforced VPC-SC perimeter over the nonprod project; pass = the Unified Access Policy still refuses an unregistered hostname while a deliberate out-of-perimeter egress produces a violation), after which (a) is the backstop; the reading that Google's two pages describe different objects (template-less vs `ALL_TRAFFIC`-template gateway) is a hypothesis spike 2 confirms or refutes | Whether "credential holders are never internet-reachable" (P90) is enforcement or IAM-only; whether a VPC-SC backstop is possible at all on a gateway-bound engine | as [06](06-gateways-model-armor-perimeter.md) §4.3 | platform owner, security reviewer | Tier W perimeter (spike 2); the super-admin grant (spike 1) | HLD §17, §8.1; [06](06-gateways-model-armor-perimeter.md) §4.3 | supersedes Wall-E 9 (Cloud Run ingress) and 21 (gateway or perimeter); closes E-19 |
| P13 | open (DPO) | Retention floor and ceiling per store; conversation retention | The locked stores are locked on the day the ceiling is recorded (P106) — not before, because a lock cannot be shortened; until the DPO decides, `evidence` cannot be made immutable | the numbers of [08](08-data-logging-retention-sovereignty.md) §5.2 (P106) as the proposal; conversation history 30 days interim (P52 and P113 aligned on 2026-09-13) — the DPO settles the number | DPO | Stage 1 of any Tier W agent | HLD §17, §7.5; [08](08-data-logging-retention-sovereignty.md) §5 | supersedes Wall-E 17 (audit retention), 7 (retention after licence removal); E-14; M-5 |
| P22 | open (deployer half closed by P142, 2026-09-13) | Git host and admin-bypass audit; the CI deployer identity is P142 | Branch protection is the enforcement behind D11 and "Mo reaches production only through a merged pull request"; an unaudited admin bypass is a write path around every reviewer | — | platform owner | Tier W | HLD §17; [09](09-supply-chain-secrets-recovery.md) §1 | supersedes M-7 (git host); depends on Wall-E 37 (second pair of hands) |
| P24 | proposed for the tenant app (no, P51); open for operators | Whether the operator population needs a workforce pool | A pool changes the IAP audience of every control surface (P64) and cannot carry device-posture access levels | only if non-Google operators exist; when made, one organisation-level pool per P64's rules | platform owner | HLD §4.2 | HLD §17; [04](04-identity-and-privileged-access.md) §2.5 | promotes Wall-E 12 §5; Wall-E 20 |
| P25 | open | The Tier W cap by named grading capacity, as a number of agents per grader | What limits "hundreds of agents" is blind-grading hours; a Tier W row without a named grader with capacity does not pass admission | *tbd*; measured after the first Tier W agent's first quarter | Mo owner, ISMS | the second Tier W agent | HLD §17, §11.1 | depends on M-4 and E-13 (the second grader); Wall-E 11 |
| P30 | proposed | Validator custodian's `READER` on `eve_quality` | Without it Eve's proposals to Mo stay advisory (`incident_note` only) and the verifier's quality pack cannot be re-derived by the gate | now (recommended) | Eve owner | Eve proposals beyond `incident_note` | HLD §17, §13.3 | depends on E-9 (Eve's BigQuery grant); M-11 (cross-project reads) |
| P40 | proposed | Non-production split: nonprod optional at R, mandatory from W and for controllers and improvers (folders `fld-controllers-nonprod`, `fld-improvers-nonprod` added); no production credential in nonprod; a sandbox Workspace tenant for P/P-SA nonprod (never an OU); promotion by Binary Authorization attestation or recorded bundle SHA-256; Agent Platform Threat Detection on in nonprod one unbound agent at a time; nonprod budgets 50 % | A gate change that first executes as the production credential holder is the failure Wall-E 29 named; a nonprod OU in the production tenant shares its super admins | adopt; Eve drilled against the sandbox tenant before the super-admin grant. Rejected: nonprod mandatory at R; nonprod as an OU (decision 29's reversal) | platform owner | Tier W | [02](02-landing-zone-and-tiers.md) §2.3 | supersedes Wall-E 29 (where a gate change first executes; the OU reading reversed); E-15 |
| P46 | proposed | The approval surface (IAP page bound to the action service's request hash) is per agent, never a shared page; the routing library and IAP audience pattern are shared from `CORE_PROJECT`'s repository | A shared approval page would be a cross-project write path into every credential holder; one compromise, every agent | refuse the review's A3 shared-page candidate | platform owner; security reviewer | Tier W | [02](02-landing-zone-and-tiers.md) §5 | generalises Wall-E 14 (approval surface) and 20 |
| P50 | proposed | Location `eu`; CMEK with a `europe` multi-region key via explicit `CmekConfig` for everything created after GE-4; the imported app recorded as Google-managed encryption (it cannot be protected in place); Google Search grounding off; Enterprise web search off until a row needs it | Recreating the app for CMEK loses history and data stores; the exception must be recorded once, dated, and reopened only with the label (P20) | do not recreate; record the exception with P20 | platform owner; ISMS for the CMEK position | TISAX assessment order (recorded here because GE-4 runs at Tier C) | [03](03-gemini-enterprise-environment.md) §5 | — |
| P52 | proposed (aligned with P113 on 2026-09-13; DPO settles under P13) | Conversation retention **30 days** as `Assumption:` until the DPO decides, from Google's 1–180-day list; not the Art. 12 log; memory off | The setting is per app and silent; page 03 first proposed 90 and page 08 30; 30 is the one interim number runbook step GE-6 sets | 30 days (interim, both pages); 90 was page 03's first proposal; 60 (default) and 180 also considered | DPO | Stage 1 of the first Tier W agent | [03](03-gemini-enterprise-environment.md) §5.4, §14 | Wall-E 7; E-14 |
| P56 | proposed | The cross-project grant generalised: custom role `geEngineQuery` (`aiplatform.reasoningEngines.query` only) in every agent project bound on the engine to `service-GEMINI_PROJECT_NUMBER@gcp-sa-discoveryengine`, made by the agent project's CI; the documented project-level `roles/discoveryengine.serviceAgent` a per-agent dated fallback with a mutation detection; registration by CI (`editor` holds `agents.create`/`update`); sharing by `ge-admins@` via PAM because `editor` lacks `agents.setIamPolicy` (verified 2026-09-13) | The project-level role carries create/delete/update on every engine in the project; the engine-scoped role is the difference between "the front door can ask" and "the front door can replace" | engine-scoped role first, fallback dated | platform owner | every publication (first at the first Tier R agent; a Tier W agent's publication carries a write path) | [03](03-gemini-enterprise-environment.md) §10 | generalises Wall-E 42 / topology 42 (`walleEngineQuery`); promotes Wall-E 12 §7 |
| P57 | spike, staged | `gemini-egress` (`europe-west1`, Agent-to-Anywhere, default deny) bound to the tenant app in three stages: throwaway-app spike, 30-day `DRY_RUN`, enforce; `gemini-registry` in `europe-west1` as a CI-generated working set; the assistant joins the gateway availability domain; rollback by unbinding | "Secure all agents through it" is untrue while the tenant app can reach an unregistered engine; P6 was the HLD's spike and this is its protocol | bind, staged; not binding rejected ("secure all agents through it" is untrue without it), binding without dry-run rejected | platform owner; Gemini Enterprise admin | the register as publication enforcement (HLD P6); enforce before the first Tier W publication | [03](03-gemini-enterprise-environment.md) §11 | details P6; topology 52 |
| P63 | proposed | Every human control surface behind IAP directly on Cloud Run (`--iap`, IAP service agent as invoker) with access level `al-platform-operator` (device posture; Chrome Enterprise Premium licence *tbd*) — `al-platform-operator-lite` (IP + time) until the licence exists; security key carried by "Only security key" 2SV and Google Cloud session control at 1 h on the OUs | Operators approve L3 requests and pull K0/K1 on these surfaces; a stolen session on one is a stolen approval | adopt; add the CEP licence line to HLD §0.5; load-balancer IAP rejected (not needed for Cloud Run); no access level rejected | platform owner | Tier W control surfaces | [04](04-identity-and-privileged-access.md) §6 | supersedes Wall-E 20 (operator authentication) and 14's surface authentication; promotes Wall-E 12 §5.3 |
| P64 | proposed (conditional on P24) | Workforce pool only on P24 yes; when made: one organisation-level pool `wif-agentic-operators`, 1 h session, programmatic sign-in disabled, allowed services limited to the IAP surfaces, IP+time level only (device levels unavailable to workforce users), never on a Tier P surface, own IAP surface per population | A pool that could reach a Tier P surface would put a non-Google identity in front of a super-admin approval | adopt; one pool per agent rejected | platform owner | none until P24 | [04](04-identity-and-privileged-access.md) §2.5 | Wall-E 12 §5 case (b) |
| P74 | proposed | Register lifecycle gains `suspended`; "registered ⇒ feeding the SIEM" is a continuous invariant with per-tier silence budgets (24 h C/R, 4 h W, 15 min P) and automatic actions (`gemini-egress` entry removed, W+ `halt_all`, severity per tier); leaving suspension is a human pull request | A gate-only check catches an agent that never fed; only a continuous one catches an agent that stopped — and silence is what a compromised agent produces | continuous invariant over a gate-only check; delete on silence rejected | platform owner | Tier W | [05](05-registry-and-autonomy-contract.md) §5 | generalises E-5 (Eve down → `no_autonomous`) to every agent's feed |
| P76 | proposed | `contract_version` semver on all four contract artefacts (manifest, `ladder.schema`, `audit.schema`, `ladder.yaml`); validator, audit-schema check and factory accept N and N−1 major for 90 days; additive-only audit columns, removed columns deprecated for one major | Hundreds of agents cannot re-deploy on the day the schema changes; a pinned single version would freeze the contract, no window would break Mo's metric SQL | N/N−1 window over a pinned single version | platform owner, security reviewer | Tier W | [05](05-registry-and-autonomy-contract.md) §9.1 | supersedes E-20's `contract_version` question for the audit contract; M-2 (fingerprint scoping) |
| P77 | proposed | The Wall-E set's numbers (ceilings table, dwell, ratchet, Wilson ≥ 0.90 lower bound at n ≥ 35, severity table, 4-business-hour approval TTL, two-human rule) are the platform defaults; "tighten only" is a monotone comparison per parameter in the validator; loosening a default is a platform decision signed by the security reviewer | Per-agent negotiated numbers are the end of comparability between agents and of any fleet-wide statement about autonomy | fleet-wide defaults over per-agent numbers | security reviewer | Tier W | [05](05-registry-and-autonomy-contract.md) §9.5 | supersedes Wall-E 33 (promotion statistics — the Wilson gate becomes the default) and 16; M-10 (`unsure` cap, calendar) |
| P79 | proposed | Tier W uses the platform verifier with the second-implementation weakening accepted and written down (a second implementation of the platform's code, not a second author of the spec); four compensations (security-reviewer spec-review signature on `families[]`, blind human sample, monthly seeded-fault suite, differential test in CI) and the cap "L4 only with a seeded-fault record younger than 30 days"; Tier P keeps a hand-written Eve; Tier X has no verifier and every cell L0 | A hand-written verifier per Tier W agent is six engineer-days each and does not scale; a shared one measures itself unless compensated, and the page says so | accept for Tier W with compensations | security reviewer; Mo owner | the second Tier W agent | [05](05-registry-and-autonomy-contract.md) §9.8; HLD §12.4 | generalises E-6 (observe mode), E-13 (second grader), E-15 (seeded faults); Wall-E 34 |
| P83 | proposed (`Assumption:` value) | Availability-domain numbers for gateway plus Model Armor: SLO `Assumption:` 99.5 % monthly per region, measured by one nonprod probe engine per region every 5 minutes (benign plus known-hostile prompt); quota alerts at 70 % of 1,200 sanitize QPM and 600 ExternalProcessor QPM; `failOpen` never flipped to recover; outage runbook | The first fail-closed write agent is also the first agent an outage stops; the SLO is what the owner is told before that | value *tbd* by measurement over the first quarter | platform owner | Tier W (first fail-closed write agent) | [06](06-gateways-model-armor-perimeter.md) §2.5 | E-10 (availability measurement); Wall-E 41 (single region) |
| P87 | proposed | Console Model Armor: on for every Gemini Enterprise app, `failureMode FAIL_CLOSED`, a second template pair in the `eu` multi-region generated from the Tier C standard, applied by the Gemini Enterprise admin through PAM, drift-checked daily by enumerating apps and reading `customerPolicy.modelArmorConfig`; an app without it or with `FAIL_OPEN` is severity 2 and unpublished for external sharing; recorded as covering Tier C only | Same setting as P53, seen from the gateway page: the drift check is what turns a console toggle into a control | as [06](06-gateways-model-armor-perimeter.md) §3.5; `FAIL_OPEN` for availability rejected | Gemini Enterprise admin | Tier C (setting); Tier W (drift check, first custom agent not covered) | [06](06-gateways-model-armor-perimeter.md) §3.5 | — |
| P89 | proposed (decided at the first Tier W prod gateway; spike 1 on that configuration) | Gateways born perimeter-ready: every Tier W+ prod egress gateway created with an agent connectivity template in `ALL_TRAFFIC` mode in the agent project's own VPC (/28 subnet with Private Google Access, PSC network attachment, DNS peering, Cloud NAT) from the first deploy, provided spike 1 passes on that configuration; Tier R stays `PRIVATE_RANGES_ONLY`; if spike 1 fails on `ALL_TRAFFIC` but passes on `PRIVATE_RANGES_ONLY` the factory uses the passing one and (a) is recorded unreachable for that tier | A template cannot be added to a gateway later; "no template until spike 2" means deleting and recreating every gateway when the backstop is wanted | as [06](06-gateways-model-armor-perimeter.md) §4.2 | platform owner | Tier W (the choice is made at the first W gateway; the backstop it enables is later) | [06](06-gateways-model-armor-perimeter.md) §4.2 | Wall-E 21; E-19. |
| P96 | proposed | Detection catalogue: IT security code-owns `detections/`; the security reviewer owns content with the MDR partner; YARA-L detection-as-code deployed by CI via `google_chronicle_rule`; a fixture per rule as a merge condition; retrohunt before enable; quarterly review with Eve's threshold review; rule ids SA-01..09, SG-01..07, PL-01..15 (PL-10..15 from pages 03, 08 and 09), AG-*; the auto-K7 subset = SG-01..04, PL-02 (KF-1 + KF-3; KF-4 counted for engine queries), dry-run until the SIEM outbound identity exists; SA-* rules never fleet-kill | A detection without a fixture is untestable; a rule that can pull the fleet kill must have a false-positive record before it is armed | as [07](07-monitoring-detection-incident-response.md) §6 | security reviewer; platform owner | Tier W (PL-*); the super-admin grant (SA-*/SG-*) | [07](07-monitoring-detection-incident-response.md) §6 | supersedes Wall-E 37's reviewer scope for detections; E-18 (paging conditions 5–6 filled by SA-03/SA-04) |
| P98 | proposed | Platform reporting path RP-1..RP-6: every verifier has an owner outside the administration line of any agent it verifies (admission refuses otherwise); sev 1/2 pages in parallel to the verifier owner and the desk; reports about a human with authority go out of band from the witness to the verifier owner as sole recipient; the reasoning path is report-only, sev 2, budgeted; unacknowledged pages re-page secondary then incident commander; three-pages-a-week budget. Eve's HLD §13.2 contract is the first instance | "Report to the human anything wrong" is only true if the human is not the one the report is about | as [07](07-monitoring-detection-incident-response.md) §8 | Eve owner; security reviewer | Tier W (RP-1, 2, 6); the super-admin grant (RP-3, RP-5) | [07](07-monitoring-detection-incident-response.md) §8 | generalises E-2 (Eve's owner outside the line), E-13; supersedes Wall-E 35 (who receives per-person output) for reports |
| P99 | proposed | On-call: the organisation's tool with 24x7 paging, else PagerDuty; one service `agentic-platform` as the Terraform-managed channel everywhere; SMS + mobile app to individuals from the witness for sev 1; escalation L1 desk → L2 platform owner → L3 IT security, sev 1 parallel to the second human, auto-escalate at twice the target; acknowledgement sev 1 15/60 min, sev 2 4 business hours (`Assumption:`), C–W next business morning; containment K0 < 60 s, K5 ≤ 30 min of ack, K6 ≤ 60 min, K7 KF-1 < 60 s / < 5 min; `oncall.yaml` the platform rota file with a K5/K6 section | A page nobody is contracted to answer is a log line; the targets are what the SOC metrics (P103) measure | as [07](07-monitoring-detection-incident-response.md) §9.2 | incident commander role; platform owner | Tier P (24x7 line); Tier W (channel resource) | [07](07-monitoring-detection-incident-response.md) §9 | supersedes Wall-E 11 (on-call, second approver) and 15 (business hours) for paging |
| P100 | proposed | One incident record in the SIEM's case system (SecOps cases; the organisation's ITSM only if its cases cannot carry the mandatory fields), mandatory fields incl. correlation keys, levers with timestamps, whether Eve saw it and which detection fired first, regulatory flags; nightly reconciliation with `eve.incidents`; sev-1 copy to the witness within 24 h; monthly JSONL export to the locked evidence bucket; evidence freeze before remediation (Art. 73(6)) | Two incident records (Eve's and the desk's) that disagree are what a regulator finds; one record with Eve's view as a field is not | as [07](07-monitoring-detection-incident-response.md) §10 | incident commander role | Tier W (record); Tier P (witness copy) | [07](07-monitoring-detection-incident-response.md) §10 | E-11 (outcome codes), E-14 |
| P103 | proposed | SOC metric set (MTTD/MTTA per severity, time to K0/K5/K6/K7, precision per rule, page-budget adherence, coverage, dead rules, pipeline availability, seeded-fault catch rate, audit completeness, two-evaluator agreement SIEM vs Eve, regulatory-clock compliance) monthly; the desk produces platform-wide from the SIEM and cases, never from `MO_PROJECT`; Mo produces the Wall-E and Eve packs | The management review (P139) needs numbers the improver did not compute about itself | as [07](07-monitoring-detection-incident-response.md) §14; targets *tbd* after three months, `Assumption:` starting values | security reviewer | Tier W (first pack); Tier P (desk-produced) | [07](07-monitoring-detection-incident-response.md) §14 | M-5, M-8 (metric packs); Wall-E 38 (value baseline) |
| P106 | proposed (buckets at Stage 0; the lock at Stage 1) | Retention schedule: `evidence` 400 days floor, `record` 10 years, `content` 30 days ceiling, `ops` 30; the DPO's ceiling per class before Stage 1 of any Tier W agent; locked stores created unlocked at the floor and locked on the day the DPO's ceiling is recorded at max(floor, ceiling), never before; login/token/SAML streams in their own bucket with their own line; an interim compensating SIEM rule on any bucket or retention change in the three evidence projects | A lock is irreversible in the one direction the DPO can move; no lock leaves the super-admin case without a preventive copy | adopt | platform owner; the DPO for the ceilings | Stage 0 (buckets); Stage 1 of any Tier W agent (the lock) | [08](08-data-logging-retention-sovereignty.md) §5 | supersedes Wall-E 17, 31; E-14 (with the lock-trigger sentence in Eve Phase 9); M-5 |
| P110 | proposed (revisit per P12) | Assured Workloads EU Data Boundary for the agents folder: no, not now. Verified: the package lists Gemini Enterprise, Vertex AI, Cloud Logging, Model Armor, Cloud Run, BigQuery, Firestore, Discovery Engine but not Agent Gateway, Agent Registry or Agent Identity; it sets `gcp.restrictServiceUsage` to supported products, so the three would be refused or permanent violations; MCP servers unsupported; Key Access Justifications unavailable without it. Compensating set DL-7.1..7.6; adoption later by a new folder and move analysis; gap raised with Google via P32 | "Yes with three permanent acknowledged violations" is a paper control; "yes without gateway and registry" removes the design | no | platform owner; security reviewer | none (revisit per P12); recorded here because the compensating set must exist before the first Tier W agent | [08](08-data-logging-retention-sovereignty.md) §7.1–§7.2; [11](11-tisax.md) R-07 | — |
| P113 | proposed (P52 aligned to it on 2026-09-13; DPO settles under P13) | Gemini Enterprise conversation history 30 days (`Assumption:`; Google's list 1/30/60/90/120/180, default 60, Plus edition required; 60 stands where the edition hides the setting); the EU-DRZ model-pin rule as a CI check against a committed allow-list of models with EU at-rest data residency and ML processing | The chat is tenant content about named employees; the model pin is the residency claim every audit row carries (Wall-E 6) | 30 days; rejected: 1 day (operators lose the thread within a working day), the default 60 (twice the content ceiling), 90 (page 03's first proposal) | Gemini Enterprise admin; platform owner for the allow-list | Stage 1 | [08](08-data-logging-retention-sovereignty.md) §5.2 R8, §7.3 | supersedes Wall-E 6 (model pin) with a fleet allow-list |
| P115 | proposed | Admission model: per-agent-project Binary Authorization project-singleton policy generated by the factory (`REQUIRE_ATTESTATION` by `built-by-cloud-build` + `vuln-gated`, plus `promoted-to-prod` in prod; `admissionWhitelistPatterns` = the agent's repository only); `constraints/run.allowedBinaryAuthorizationPolicies = default` at `fld-agents-w`, `fld-agents-p`, `fld-controllers` and `fld-platform-core` (extends HLD §9's "W and P"); 30-day dry-run per folder; promotion = an attestation on the same digest after the tier's nonprod gate; rollback by digest; emergency change by `--breakglass` with a severity-1 case and a post-hoc reviewer | Eve's jobs and the K7 job are Cloud Run on the authority and kill paths; an unattested image there is worse than an unattested agent | as stated; `fld-agents-r` exempt (no Cloud Run); "W and P only" as the HLD first wrote rejected. Binary Authorization continuous validation is GKE-only (Preview), so post-deploy digest drift on Cloud Run is a drift-job / Security Health Analytics custom-module check, detection-grade | platform owner; security reviewer | Tier W (constraint live before the first Tier W prod deploy) | [09](09-supply-chain-secrets-recovery.md) §1 | generalises Wall-E 29 (promotion after the nonprod gate); D11 |
| P116 | proposed | Agent Runtime deploys from the attested container image (`container_spec.image_uri` = digest) for every engine from Tier W up and for controllers; source-based (pickled bundle) deploys at Tier R only with bundle SHA-256 and lockfile recorded; the drift job compares spec to the attestation set (detection-grade, stated); the service-agent read grant on `CICD_PROJECT`'s repository a topology row, *tbd* until the cross-project read is proven, per-project remote repository as fallback | A pickled bundle is code nobody attested; from the first write agent the reasoning layer must be the image the pipeline signed | as stated (verified 2026-09-13 that Agent Runtime accepts `container_spec.image_uri` from Artifact Registry); the bundle hash alone (HLD §9's first position) rejected from Tier W up | platform owner | Tier W | [09](09-supply-chain-secrets-recovery.md) §1.4 | supersedes Wall-E 40's bundle-hash probe for W+; M-11 (cross-project reads) |
| P117 | proposed (`Assumption:` thresholds) | Vulnerability gate: in-build on-demand scan failing on CRITICAL or a malicious package; `vuln-gated` attestation signed by a pipeline-only HSM key; automatic scanning on push with HIGH triaged within 7 business days; daily running-digest re-scan by the drift job; patch cadence R monthly / W fortnightly / P and controllers weekly; implemented as the platform's own build step because Google's Kritis Signer guide returned 404 and the tool is unmaintained | TISAX 5.2.x and Art. 15 both ask "how do you know the running image has no known critical flaw" — the answer must be a gate, not a scan report | thresholds `Assumption:`; ISMS confirms | platform owner; ISMS | Tier W | [09](09-supply-chain-secrets-recovery.md) §1.6 | — |
| P120 | proposed | Restore boots at `halt_all` by construction: the action service pins the Firestore `Database.uid`; a mismatch at start-up forces posture `RESTORED_UNCLEARED`, the verifier refuses independently on the same status, reconciliation replays halts and overrides from the insert-only BigQuery audit log, and only a human operator clears it (writing the new uid; CI updates the pin). Restore drill once before Stage 1, then in every promotion gate above L3 and quarterly; the validator refuses a raise without a fresh clean record. `Assumption:` `uid` changes on in-place restore — the first drill proves it, else the pin is `createTime` | A restore silently rolls back halts and overrides (Wall-E 30); a runbook step is forgotten, a uid is not | as stated over decision 30's prose | agent owner (code); validator custodian (gate) | Stage 1 of any Tier W agent | [09](09-supply-chain-secrets-recovery.md) §3.3 | supersedes Wall-E 30 (restore protocol) with a mechanism; E-15 (K6 drills) |
| P129 | proposed | Worker information and consultation moved to before Stage 1 (from before Stage 3), under GDPR Art. 88 and national law now and Art. 26(7) voluntarily now; an employee notice; the Art. 86 explanation path through HR using the redacted frozen plan; every request logged in `explanation_requests` and fed to Mo's Art. 72 loop; Wall-E decision 8 and prerequisite D7 re-dated | Art. 26(7) says before putting into service; a works council informed at Stage 3 has been bypassed for two stages | adopt; keeping "before Stage 3" rejected (Art. 26(7) and lead time) | HR/communications, DPO, legal | Stage 1 of any Tier W agent acting on employee accounts | [10](10-eu-ai-act.md) §4.10 | supersedes Wall-E 8 (DPIA and employee representatives) on timing |
| P130 | proposed | Documentation freeze and the Annex IV index: a git tag `compliance/<agent>/S<n>/<date>` and an evidence-bucket bundle per stage as the "documentation as put into service", 10 years, locked; [10](10-eu-ai-act.md) §4.3 as the Annex IV index with "frozen at" tags; the reconciled pages (P47) a precondition of the S1 tag; the autonomy ladder declared the Annex IV 2(f) pre-determined-changes record so promotions are not substantial modifications (Art. 43(4)) | Art. 11 needs the version put into service, not a live wiki; without 2(f) every L3→L4 promotion would be a re-assessment | adopt | platform owner; AI compliance owner | Stage 1 | [10](10-eu-ai-act.md) §4.3, §5 | depends on P47 (the pages reconciled) |
| P137 | proposed | Separation of duties as a counted minimum: the incompatibility matrix of [11](11-tisax.md) §7.1; 1 human at Stage 0 / Tier R (self-review recorded), 3 at Stage 1 / Tier W, 4 at the super-admin grant, 4 plus a bought 24x7 desk at Stage 3; a CI separation check in `CICD_PROJECT` evaluating the matrix against group memberships and the roster daily and on every merge, feeding the admission gate; security reviewer = Eve owner at the first grant only as a dated ISMS exception (five humans is the clean state; four with a dated exception is the floor); one training outline per role with completion records counted by the gate | "Every role is one person" is the finding most likely to stop a TISAX assessment (1.2.2); a promise of staffing without a count is unassessable | adopt; ISMS supplies names | ISMS (names); platform owner (the check) | Tier W; the super-admin grant | [11](11-tisax.md) §7; HLD §0.3 | supersedes Wall-E 37 (second pair of hands) with a count; E-2, E-13, M-4 |

---

## 4. Before the super-admin grant

The gate: the Tier P line of [HLD §0.4](01-hld.md#04-the-tier-gate-what-must-exist-before-a-tier-opens),
read as the checklist of [11 §6.3](11-tisax.md#63-compensations-as-preconditions--the-checklist-the-gate-reads).
It is the day `walle@` receives Super Admin (P33) and the day "the four owner groups are one
person" expires. Tier P generally opens on the same line, so the SIEM and desk rows sit here.

| Id | State | Decision | Why it matters | Recommendation / value | Owner | Gate it blocks | Recorded in | Agent-set decisions |
|---|---|---|---|---|---|---|---|---|
| P7 | open (verify with Google) | Context-Aware Access on the robot account: does the Admin console level apply to super admins; can API tokens be bound | Decides the grade of HLD §4.6 and §13.1 item 6 — whether CAA on `walle@` is a boundary or friction | adopt as detection-plus-friction meanwhile; never count it in the safety case (HLD §16) | platform owner | grade of HLD §4.6 and §13.1 item 6 | HLD §17; [04](04-identity-and-privileged-access.md) §8 | supersedes the "Google refuses at its end" lines of Wall-E 02 (HLD §18 item 1); Wall-E 39 |
| P10 | open (IT security) | SIEM: the organisation's existing instance vs an own Google SecOps instance; MDR partner | Tier P's detection desk is bought; without the answer the factory provisions P92's default | the contract S1–S9 (P92) is what either option must meet | IT security | Tier P | HLD §17; [07](07-monitoring-detection-incident-response.md) §2 | — |
| P14 | open (IT security) | The witness organisation: domain, Cloud Identity edition, billing linkage | Eve's evidence, incident record and pager leave the tenant's reach only if the witness exists outside it; every "outside the reach of the super admins" claim rests on it | — | IT security | the super-admin grant | HLD §17, §13.2 | depends on E-2 (Eve's owner); Wall-E 31 (evidence durability) |
| P16 | proposed (none now) | A machine-invocable account stop for `walle@` | K5/K6 are human acts; an automatic stop on the super-admin account would be an automatic lock-out path a compromised monitor could pull | none now; spike on OAuth-client disable invalidating refresh tokens and on delegated-admin suspend of a super admin; revisit with P15 | owner, security reviewer | — (revisit with P15) | HLD §17, §13.1 | Wall-E 39 |
| P29 | open (owner signs) | The two lists (hard-denied operations; protected principals), signed as decision 4 re-ratified | They are the Art. 6(3) boundary (P125) and the ceiling every control is calibrated against; unsigned, the deviation record (P136) has no scope | as HLD §13.1 | the platform owner | the super-admin grant | HLD §17, §13.1 | re-ratifies Wall-E 4 (the must-never list) and 27 (bands); depends on P33 |
| P33 | **decided** by the owner, 2026-09-13 | Wall-E holds Super Admin — the dated decision superseding decision 26 for Wall-E and re-ratifying decision 4 with the two lists; file `decisions/2026-09-13-wall-e-holds-super-admin.md`; the residual in the accepted-risks table; signed as a TISAX deviation (P136) and row R-01 of the risk register (P140) | The premise of the whole set: nothing in Workspace or GCP narrows a super admin, so the perimeter is custody, scope split, a split control plane, detection latency and the witness | designed around, not re-argued; the security reviewer signs the deviation | the platform owner (decides); security reviewer (signs) | the super-admin grant | HLD §17, §13.1; [11](11-tisax.md) §6 | supersedes Wall-E 26 (keyless SA admin principal — closed by fact); re-ratifies 4; E-16 (Eve's credential chapter contingent on 26) |
| P60 | closed P9; proposed | The PAB `pab-agents` (enforcement version 4, bound per project by the factory on `//agents.global.org-ORG_ID.system.id.goog/attribute.container/projects/N`; a stricter twin `pab-agents-p-sa` for the singleton) is enforcement-grade for every permission version 4 blocks — all `aiplatform.googleapis.com/*`, secrets, keys, `serviceAccounts.*`, storage, BigQuery, Pub/Sub — and has no effect on `run.googleapis.com/routes.invoke`; KF-4 counted for engine queries; B7's grade line rewritten | Whether the PAB counts in a safety case was a guess (P9); now it counts for what version 4 blocks and nothing else | adopt; keep the PAB out of the safety case for Cloud Run invocation (that rests on resource-level `run.invoker` plus the deny policy); keeping the PAB as a detection-grade backstop only rejected (the blocked list is now read) | platform owner, security reviewer | K7 lever KF-4; trust boundary B7 (counted at the grant) | [04](04-identity-and-privileged-access.md) §4 | promotes Wall-E 12 §1.9 (grades) and 13 §9 |
| P66 | proposed | Workspace multi-party approval ON for every covered setting, console and API, before the grant (role assignment and custom-role privilege updates, DWD, 2SV, session control, login challenges, account recovery, SSO, Context-Aware Access become Google-enforced two-person acts over the robot's credential); the robot never an approver; "MPA off" hard-denied and severity 1 with K0 `no_writes` on `walle-actions-super` | It is the only Google-enforced two-person rule over the robot's credential; without it a single compromised super-admin session changes 2SV for everyone | adopt; test `makeAdmin` coverage and the off-switch in the sandbox tenant first | platform owner | the super-admin grant; tenant edition *tbd* | [04](04-identity-and-privileged-access.md) §8.4 | supersedes Wall-E 02's "Google refuses" lines with a Google-enforced control (HLD §18 item 1); Wall-E 4 (the list gains "MPA off") |
| P68 | proposed | The roster: exactly three super admins in steady state — two human admin accounts (one outside the Wall-E administration line, the second human) and `walle@`; the robot never the only nor the recovery super admin; self-recovery Off at the top OU and drift-checked; custodians per [04](04-identity-and-privileged-access.md) §8.3; `role_assignment_added` and `role_assignment_missing` both paging classes; PAM admin, Organization Administrator and `factory-groups@` on the same quarterly roster review | A fourth standing super admin is a fourth target; a robot that is the recovery super admin is a tenant with no human recovery | adopt; four super admins with a spare human rejected (a fourth target; the hand-over exception covers it) | the platform owner; second human | the super-admin grant | [04](04-identity-and-privileged-access.md) §8.1 | supersedes Wall-E 39 (robot lifecycle: detection of another admin acting on the robot); 11 (second human) |
| P69 | proposed | Break-glass: two Cloud Identity accounts `brk-gcp-1@/2@` with standing Organization Administrator and PAM admin, sealed hardware keys with cross-line custodians, custody records in the witness bucket, sign-in = severity 1, quarterly drill; Workspace break-glass = the two human super admins on separate admin accounts with spare keys and admin-generated backup codes, self-recovery Off; a third super admin only as a dated hand-over exception | PAM cannot bootstrap itself; a platform with no standing owner anywhere (P62) needs a sealed way back in | adopt; PAM-only with no break-glass rejected | platform owner, second human | the super-admin grant | [04](04-identity-and-privileged-access.md) §7 | Wall-E 39 (custody and rebuild) |
| P90 | proposed (applied the day spike 1 passes) | "Credential holders are never internet-reachable" as a folder rule: `constraints/run.allowedIngress = internal-and-cloud-load-balancing` on `fld-agents-w`, `fld-agents-p`, `fld-agents-p-sa`, `fld-controllers` and `fld-platform-core`, held in Terraform; until then IAM-only invoke is the enforced boundary plus a Security Health Analytics custom module over `run.googleapis.com/Service` flagging ingress `all`; monthly direct `run.app` probe expecting refusal; Cloud Run jobs outside the rule by nature; security reviewer signs at Tier P | The action service holds the super-admin credential; an internet-reachable one is one IAM mistake from a tenant compromise | as [06](06-gateways-model-armor-perimeter.md) §4.3 | platform owner; security reviewer | the super-admin grant (with P3 spike 1) | [06](06-gateways-model-armor-perimeter.md) §4.3 | supersedes Wall-E 9 (Cloud Run ingress). |
| P92 | proposed (P10 open) | The SIEM contract S1–S9 (EU residency, retention ≥ the 400-day line, feeds F1–F4 without DWD or SA keys, detection-as-code, cases with correlation keys, 24x7 desk, REST outbound to the K7 job, four entity types, IT-security-administered readers); P10 is decided against it; the factory provisions the default if P10 is unanswered when the rest of the Tier P checklist is green | The grant must not wait on an organisational decision that has no owner yet; the contract is what any answer to P10 must satisfy | own Google SecOps instance per [07](07-monitoring-detection-incident-response.md) §2.2 | IT security (P10); platform owner (contract, default) | Tier P | [07](07-monitoring-detection-incident-response.md) §2 | — |
| P93 | proposed | Default SecOps instance location and retention: Europe multi-region (EU member states; excludes London/Zurich); retention ordered at the smallest term ≥ 400 days (term granularity *tbd*, follows P13); `europe-west3` as single-site fallback if the ISMS needs a nameable site | The SIEM copy is the evidence class's second home; a retention shorter than the alert/incident line is a gap an assessor measures in days | as stated | IT security | Tier P | [07](07-monitoring-detection-incident-response.md) §2.3 | E-14 |
| P97 | proposed | Four heartbeats: H-1 `eve_workspace_logs` (60 min business hours / 240 outside, alarm in `EVE_PROJECT` and the witness), H-2 SIEM canary pair every 15 min (45 min window, receipt watched from `CORE_PROJECT`), H-3 weekly synthetic SHA finding (8 days), H-4 P-tier 5-min heartbeats (`eve_last_seen` 15 min, witness push 26 h); `log_pipeline_silent` = `no_autonomous` for the verifier's agents and `halt_all` on the P-SA lanes, cleared by one human call, every clear a report about the administrator | Silence is what a super admin can manufacture; the halt must be the default state of silence, not an alert about it | as [07](07-monitoring-detection-incident-response.md) §7 | Eve owner; platform owner | the super-admin grant | [07](07-monitoring-detection-incident-response.md) §7 | generalises E-5 (Eve-down timer), E-10; supersedes Wall-E 40's contract-drift probe for pipeline silence |
| P101 | proposed | Art. 73 serious-incident taxonomy (mass wrongful suspension/deletion or reclaim as limb (c); health effect (a); widespread infringement in ≥ 3 Member States, 2 days; critical-infrastructure negative determination); assessor = incident commander + legal within 24 h; report owner = the AI compliance owner with legal; deadlines 15/10/2 days; operated voluntarily until any row is high-risk (2027-12-02). GDPR path with the DPO accountable: flag at case open, DPO assesses awareness within 4 business hours, decision within 48 h, filing ≤ 72 h, Art. 34 for high risk | A clock that starts at "awareness" needs a defined moment of awareness before the first incident, not after | as [07](07-monitoring-detection-incident-response.md) §12 | legal, DPO; incident commander operates | Stage 1 of any Tier W agent (DPO path); the super-admin grant (Art. 73 taxonomy live) | [07](07-monitoring-detection-incident-response.md) §12; [10](10-eu-ai-act.md) §4.8 | Wall-E 8 (DPIA) |
| P102 | proposed | Tabletops quarterly until four clean Tier P quarters then semi-annual, plus one before Stage 1 and one before the super-admin grant; run by IT security; participants incl. both K5/K6 rota humans, the desk, DPO, legal, HR/communications, employee representatives where required; the crisis scenario = abused super-admin robot credential (RB-01/RB-02) at least yearly; evidence in `evidence/tabletops/<date>/`; pass criteria | The runbooks of [07](07-monitoring-detection-incident-response.md) §11 are untested prose until humans have walked them with a clock | as [07](07-monitoring-detection-incident-response.md) §13 | IT security | Stage 1; the super-admin grant | [07](07-monitoring-detection-incident-response.md) §13 | Wall-E 39 (rebuild checklist → RB-01/02) |
| P131 | proposed | An AI compliance owner role (legal's designate, separate from platform and agent owners) added to the RACI, accountable for classifications, Art. 6(4) files, registrations, Art. 73 reports, the Art. 4 briefing content and the annual refresh; the Art. 4 measure per role with records; the GPAI one-liner (hosts third-party models, never trains, fine-tunes or modifies) and the Art. 27/26(8) negative determination recorded with entity and date at P23; the Art. 5 negative determination dated and re-run per family addition | The builder does not classify; a classification signed by the platform owner is self-assessment in the sense an auditor rejects | adopt; folding into the platform owner rejected | ISMS (names); legal | the super-admin grant (engaged); any `annex_iii_adjacent` prod row | [10](10-eu-ai-act.md) §2, §4.1 | — |
| P136 | proposed | The super-admin deviation record: one file (P33's) with the sections of [11](11-tisax.md) §6.2 and three signatories (platform owner decides, security reviewer signs, ISMS enters); the thirteen HLD §13.1 compensations plus the penetration test, the crisis tabletop and the witnessed key-custody record are gate conditions the grant runbook refuses to pass without; after the grant any red row is severity 2 with 30 days, rows 5/6/11 or two rows red is severity 1 and K6 until green; re-signed quarterly, at every Wall-E stage transition and on any severity-1 credential incident | A deviation with no expiry and no red-row rule is an exception that becomes the norm; 1.2.2 forbids self-signature | adopt; "before S1" items instead of gate conditions rejected by the HLD; self-signature by the platform owner rejected (1.2.2) | platform owner; security reviewer | the super-admin grant | [11](11-tisax.md) §6 | operationalises P33; Wall-E 4, 26; E-16 |
| P138 | proposed | One supplier procedure for every external agent, MCP server, Marketplace app, connector and model: declared on the register row first; assessed (TISAX label via the ENX share or an equivalent independent assessment at very high protection need; the same or an accepted risk assessment at high); ISMS acceptance and contract; technical admission under HLD rules (MCP only as a gateway-governed endpoint of a Tier R+ agent with explicit `tool_filter` and no elicitation callbacks; external agents L0 for writes and tainted; no write scope on connectors); the one-run admission gate; annual review with label-expiry tracking; exit per §8.4 with export-before-delete and a removal record per row, rehearsed annually on a throwaway project; Wall-E's no-MCP position promoted to the Tier P default | "Hundreds of agents" includes ones the organisation did not write; a third-party agent without a supplier row is HLD §16's refused case | adopt; admitting MCP servers as Gemini Enterprise data stores rejected (the managed constraint stays enforced); vendor questionnaires alone at very high protection need rejected (ISA2027) | platform owner; ISMS | the first external dependency (recorded here: the first connector beyond first-party sources, P58, is a Tier C act — the procedure must exist before the grant at the latest) | [11](11-tisax.md) §8 | supersedes Wall-E 22 (MCP) for Tier P; promotes Wall-E 13 §6.3 |
| P139 | proposed | Assurance cadence: penetration test before Stage 1 of any Tier P agent (for Wall-E before the grant) with the scope of [11](11-tisax.md) §9, a scoped test of the approval surface and action service before Stage 1 of the first Tier W agent, annually and on any new lane or front door, grey-box, bought, owned by IT security; internal audit annually by the ISMS's auditor (never the platform owner or the deviation's signatory), first before the assessment order and after any severity-1; management review quarterly (ladder state, risk register, deviation, SOC metrics, supplier *tbd* items, training) and annually into the ISMS's review, minutes kept ten years | The first write is the first exposure; a test only at the grant misses every Tier W agent before it | adopt; self-audit counted as internal audit rejected (1.5.2 wants independence) | IT security; ISMS; platform owner | Stage 1 (Tier W test); the super-admin grant (Wall-E's test); the assessment order (audit) | [11](11-tisax.md) §9 | Wall-E 37 |
| P143 | proposed (2026-09-13) | **The propagation stage of HLD §18 gets owners and gates.** Items 1–10 (Wall-E set): owner the agent owner of Wall-E, the security reviewer signs items 1–4; gate: the super-admin grant for items 1–5, Wall-E's Stage 1 for items 6–10. Items 11–18 (Eve set): owner the Eve owner; gate: the super-admin grant for items 11–16 (the observe-and-report layer and the witness), Wall-E's Stage 1 for 17–18. Items 19–24 (Mo set): owner the Mo owner; gate: Wall-E's Stage 1. Items 25–26 (shared pages): owner the platform owner; gate as each bullet of item 25 states (the factory's first run by default). Each item is closed by a dated pointer line in the agent set citing the platform row, and the reconciliation job reports an item still open when its gate is evaluated | HLD §18 lists 26 required edits with no owner and no gate, so the gap-register rows it defers (41 blocking and major ones, each with its item, owner and gate in [00 §4.1](00-objective-review.md#41-where-the-blocking-and-major-gaps-are-answered)) had no accountable role — the review's own rule that a deferral names an owner | as stated | platform owner (the stage); the three agent owners (their items) | the super-admin grant (the Wall-E-set and Eve-set blocking items); Wall-E's Stage 1 (the rest) | HLD §18; [00](00-objective-review.md) §4.1 (gap trace) | frames every Wall-E, Eve and Mo row of §8 |

---

## 5. Before Wall-E's Stage 1 and its later stages

The gate: Wall-E's first super-admin write, which follows the grant (the credential must exist
to write), and the stage transitions after it (F5/F7 above L3 at Stage 4; `eve-advisor`'s
build). Rows here are Wall-E-specific instances of platform rules; the platform rule is in §3
or §4 and the row says which.

| Id | State | Decision | Why it matters | Recommendation / value | Owner | Gate it blocks | Recorded in | Agent-set decisions |
|---|---|---|---|---|---|---|---|---|
| P17 | open | Workspace BigQuery export into the witness | Eve's reconciliation source (`eve_workspace_logs`) would then have a copy outside the tenant's super admins' reach; without it the witness holds Eve's findings but not the raw stream | yes / no | Eve owner | Eve S2 | HLD §17, §13.2 | E-9 (Eve's BigQuery grant); Wall-E 10 (Eve's own credential) |
| P18 | open (DPO + legal) | Is F7 inactivity reclaim profiling | If "profiling", `F7-inactive` leaves the catalogue (P126) rather than making Wall-E `high_risk`; until answered F7-inactive is capped at L3 | not profiling, on the mechanism of P126 (report-only F1; human-supplied targets) — legal decides | DPO + legal | F7 above L3 | HLD §17; [10](10-eu-ai-act.md) §3.1.3 | Wall-E 7 (retention after licence removal), 12 (playbooks) |
| P19 | open (legal) | `eve-advisor`'s AI Act class | Decides whether the reporting path may page at severity 2 and whether it needs a registration; P34 depends on it | — | legal | `eve-advisor` paging rights; any registration | HLD §17, §13.2; [10](10-eu-ai-act.md) §3.4 | depends on P34; E-7 (halting bias — moot for a report-only path) |
| P28 | proposed content (P125); signature open | Wall-E's intended purpose: catalogue plus bands B/C as execution and instructions (recommended) vs "any admin action" with full Chapter III | The purpose is the Art. 6 argument; "any admin action" collapses the narrow-task reading and has no harmonised standard to conform to | sign with legal the content P125 records | owner, legal | first write | HLD §17, §14.1; [10](10-eu-ai-act.md) §3.1.1 | re-ratifies Wall-E 27 (catalogue bands) |
| P34 | proposed | Eve's reporting path may reason — the dated reopening of `eve/09`'s "no model anywhere in Eve v1 or v2" row and `wall-e/14`'s "An LLM Eve — Rejected", limited to `eve-advisor` as a report-only path; file `decisions/2026-09-13-eve-reporting-path-may-reason.md` | "Report anything wrong" beyond a deterministic catalogue needs a path that can describe the unforeseen; it must never be an input to a verdict (HLD CP5) | as HLD §13.2; or no reporting path and the catalogue only | Eve owner, security reviewer | the `eve-advisor` build | HLD §17, §13.2 | reopens Wall-E 34 and E-7 for the reporting path only; P19, P123 depend on it |
| P75 | proposed | A row claiming the Art. 6(3) derogation (`annex_iii_adjacent`) or classed `high_risk` cannot carry `status: prod` without an `art_49_registration` id and, for derogations, an `art_6_4_assessment`; the TISAX asset register and the AI Act register are CI exports of the register, never hand-edited | Art. 49(2) registration before putting into service is a hard date per agent; a manual register kept by legal drifts from what runs | CI-enforced gate and generated exports; manual registers kept by the ISMS or legal rejected | legal, DPO, platform owner | first write of any such agent — Wall-E included (platform rule; Wall-E's instance is P125) | [05](05-registry-and-autonomy-contract.md) §8 | — |
| P107 | proposed | Daily newline-JSON export with SHA-256 manifests of `<agent>_audit.*` and `eve.findings/verdicts/incidents/pages` by `eve-export@EVE_PROJECT` into `exports/` of the verifier's locked bucket, then the witness; `walle_audit` plus the frozen plan designated the Art. 12 log; `walle_workspace_logs` becomes an authorised view `platform_logs_views.walle_workspace_logs` in `LOGGING_PROJECT`; Wall-E 31 and topology 51 closed | The Art. 12 log must survive the agent's teardown and its owner; the exporter must not be the identity that also reconciles (one identity per duty) | adopt; `eve-verifier@` as exporter rejected (it runs the reconciler; one identity per duty); a separate mirror project rejected by topology 51 | Eve owner; Wall-E owner for the `walle_audit` grant | Stage 1 | [08](08-data-logging-retention-sovereignty.md) §5.4 | closes Wall-E 31 and topology 51; changes topology 47 (the check reads the authorised view); M-5 |
| P114 | proposed | Log views per agent capped by Google's 30-per-bucket limit; one evidence bucket per tier folder once a folder passes about 25 agents, handled by a factory tier variable; security, SIEM, identity and `ge-requests` views per bucket | The limit is a hard stop that would otherwise arrive as the 31st agent's failed factory run | adopt; one bucket forever rejected by the limit; a bucket per agent rejected (N locks, N keys) | platform owner | the 26th agent in a tier (recorded here because the variable must exist before the buckets lock at Stage 1) | [08](08-data-logging-retention-sovereignty.md) §3.4 | — |
| P125 | proposed (content of P28) | Wall-E's Art. 6 path: the catalogue is the declared intended purpose (one paragraph, one hash, identical in register row, manifest, A2A card, Gemini Enterprise description and instructions for use, CI-compared); Super Admin is a credential fact in the technical documentation and risk register, never in the purpose; the hard-denied list and band structure are the Art. 6(3) boundary enforced in code with CI ownership outside the agent repository; derogation under Art. 6(3)(a) for the catalogue and band B, 6(3)(b) for F5; Art. 6(4) assessment and Art. 49(2) registration before the first write; Art. 9–15 adopted voluntarily; band-B writes on a natural-person account carry a mandatory `decision_ref`; fallback, if legal reads band B as widening the purpose, `high_risk` with Annex VI internal control by 2027-12-02, held ready ([10](10-eu-ai-act.md) §4.9) | The one classification argument the whole Wall-E set rests on; if the purpose hash differs anywhere the argument is untrue somewhere | adopt; P28 remains the owner/legal signature. Rejected: "any admin action" as purpose with full Chapter III; high-risk from the start (no standard to conform to, and the design's facts support the derogation) | owner and legal | first write (Stage 1 of Wall-E) | [10](10-eu-ai-act.md) §3.1 | supersedes Wall-E 27's breadth question with a purpose; depends on P29 (the lists) and P33 |
| P126 | proposed | F7 split into `F7-suspended` and `F7-inactive`; no AI output selects a person by behaviour: the inactivity report stays an F1 READ artefact to a human, `F7-inactive` targets are explicit human-supplied ids, CI forbids a usage or sign-in report as any write family's target source, the action service refuses report-derived targets with `profiling_boundary_denied`; `F7-inactive` L3 until P18 is answered in writing | The discriminatory-effect risk (Art. 9) exists whether or not legal calls it profiling; the mechanism must hold either way | if P18 answers "profiling", remove `F7-inactive` from the catalogue rather than reclassify Wall-E `high_risk`; relying on the "single timestamp is not profiling" argument rejected (untested; the discriminatory effect is an Art. 9 risk regardless) | DPO and legal (P18); platform owner (mechanism) | F7 above L3; the Art. 6(4) signature | [10](10-eu-ai-act.md) §3.1.3 | Wall-E 12 (playbooks), 7 |
| P127 | proposed | Art. 14 caps and overseers: F5 at most L4 and only on the T2 HR-system event with holds that do not expire outside business hours; F5 on T0/T1 at most L3; `F7-suspended` as F5; `F7-inactive` L3; overseer roles (operator/approver, second operator, band-B approver, on-duty super admin, Eve owner, incident commander) with competence and training records; monthly blind self-grading of each operator's own approvals as the automation-bias control (review-only, never an automatic consequence for a person); the K0 drill as the Art. 14(4)(e) evidence | A scheduled suspension has no human decision fresh enough for L4; automation bias is the Art. 14 risk with no product control | adopt; F5 L4 on any trigger rejected | agent owner, ISMS, Mo owner | Stage 4 of Wall-E (first L4 on F5) | [10](10-eu-ai-act.md) §4.6 | Wall-E 13 (HR system of record), 15 (business hours); M-4 |
| P128 | proposed (already due) | Art. 50 mechanics: the action service (never the model) prepends a fixed disclosure line and appends a signature block to every F2b body and sets a fixed custom Gmail header; F2 templates carry the line; the agent description, A2A card and first session reply open with a fixed AI-system statement; Mo prose carries the agent-authored label; `disclosure_missing` a denial reason; Art. 50(2) marking of model text recorded as provider-side (Google SynthID) and documentation-unverified; signing the Transparency Code as a deployer is legal's *tbd* | Art. 50 has applied since 2026-08-02; disclosure by system instruction is not enforcement | adopt now | platform owner (injection); agent owner (block); legal (text) | Stage 1 of any `limited_art50`+ agent; already due for anything that sends | [10](10-eu-ai-act.md) §4.7 | M-8 (the narrator's label) |
| P132 | proposed (continuous) | A reclassification trigger list (family/ceiling/trigger change, purpose-hash change, P18 "profiling", a model client in Eve or `eve-advisor` input to the gate, any model modification, final Art. 6 guidelines, a harmonised standard cited in the OJ, 2027-12-02, an Art. 73 serious incident, a substantial modification) with machine signals in CI and calendar items owned by the AI compliance owner; a triggered row is `suspended` until re-signed | The guidelines and the deferral date are dated events; an annual review would miss them by up to a year | adopt | AI compliance owner, platform owner | continuous (first armed at Wall-E's Stage 1) | [10](10-eu-ai-act.md) §3.1.6 | depends on P74 (`suspended`) |

---

## 6. Later

Gates that come after the grant or have no date: the TISAX assessment order (the ISMS orders
it when the label, scope and risk acceptances exist), the VPC-SC backstop after spike 2, Tier
X (closed until HLD §11.5's conditions hold), and standing revisit triggers.

| Id | State | Decision | Why it matters | Recommendation / value | Owner | Gate it blocks | Recorded in | Agent-set decisions |
|---|---|---|---|---|---|---|---|---|
| P6 | spike (protocol P57) | Tenant app bound to `gemini-egress` in `eu` | The register is publication enforcement only if the front door cannot reach an unregistered engine | yes — the binding is documented (an `eu` app binds a `europe-west1` gateway); P57 is the staged protocol (throwaway-app spike → 30-day dry-run → enforce) and the row stays here until the enforce step | Gemini Enterprise admin | the register as publication enforcement (HLD §2.1) | HLD §17, §2.1; [03](03-gemini-enterprise-environment.md) §11 | topology 52; Wall-E 13 §7 |
| P12 | open (revisit trigger) | Assured Workloads revisit trigger | P110 says no for verified reasons; the "no" must have a date on which it is re-asked | when Agent Gateway, Registry and Identity join the package, or the label becomes Strictly confidential; the gap raised with Google (P32) | platform owner | — | HLD §17, §8.2; [08](08-data-logging-retention-sovereignty.md) §7.1 | — |
| P15 | proposed (dated decision now; build later) | Relocation of Eve's control path to the witness as the end state; the cross-organisation spike (VPC-SC across organisations, `run.invoker` both ways, `allowedPolicyMemberDomains` exceptions); no standing cross-organisation `orgpolicy.policyAdmin` | HLD §16's first line: the platform does not make Eve's control path unreachable by an inside super admin; this row is the honest end state and its date | dated decision now; build when IT security operates the witness | owner, IT security | Tier X; not the grant | HLD §17, §13.2 | E-1 (Eve's project), E-2; P16 revisits with it |
| P20 | proposed (P133); ISMS confirms | TISAX label, assessment level, scope location, NIS2 applicability | The label sets the CMEK exceptions (P119), the Assured Workloads position (P110) and AL; the scope location names the site assessed | Confidential, AL2, no availability label; scope location *tbd*; NIS2 to the legal register (P141) | ISMS | assessment order | HLD §17, §14.2; [11](11-tisax.md) §2 | — |
| P23 | open (legal) | Legal entity holding provider and deployer roles | Every Art. 26 obligation, the Art. 49 registration and the Art. 27/26(8) determination name an entity | — | legal | [10](10-eu-ai-act.md) §2 (the page the HLD called `ai-act.md`); P131's determinations | HLD §17; [10](10-eu-ai-act.md) §2 | — |
| P26 | open | Second model family for advisory monitors | Tier X's advisory monitoring must not share the monitored model's failure modes | which family; EU residency required (P113's allow-list) | platform owner | Tier X | HLD §17, §11.5 | M-8 (narrator model) |
| P27 | proposed (stage met; tier closed) | GKE Agent Sandbox as the EU code-execution tier | Whether Tier X can ever open in the EU; Agent Runtime Code Execution is EU-resident but not adopted: launch stage, isolation and egress model unverified | adopt for Tier X only; GA recorded (Google's post of 2026-05-21, P122); every other §11.5 condition still unmet | platform owner | Tier X | HLD §17, §11.5; [09](09-supply-chain-secrets-recovery.md) §4 | — |
| P32 | open | Art. 25 written position with the model provider, and Google's per-service TISAX coverage | Art. 25(4) allocation and the ISA 5.3.x supplier rows both need a document Google has not yet produced for this platform | request from Google; the ENX result share for SYN0NK requested by the ISMS (P135) | platform owner, ISMS | Stage 1 (Art. 25); assessment order (coverage) | HLD §17, §14; [11](11-tisax.md) §4 | — |
| P91 | proposed (the (a) backstop) | Under model (a), a perimeter per tier folder (prod and nonprod separately), with the finite cross-perimeter flow table of [06](06-gateways-model-armor-perimeter.md) §4.4 (tenant service-agent query ingress, peer `principal://agents` ingress, Eve's reconciler and control-plane calls, Mo's BigQuery views, the aggregated sink, `eve-export` to the witness, CI through WIF, humans on IAP with access levels, Workspace API calls outside the perimeter's reach) as the generated source of every ingress and egress rule; anything not in the table refused; 30 days of dry-run violation logs before enforcement | One perimeter over the whole folder would put the verifier and the verified inside the same boundary | as §4.4 | platform owner; security reviewer | the (a) backstop, after P3 spike 2 | [06](06-gateways-model-armor-perimeter.md) §4.4 | supersedes topology §4 ("what crosses nothing") with a generated table; M-11 |
| P119 | proposed (narrowed on 2026-09-13 — §7 row 2) | CMEK exceptions recorded once: Firestore stays Google-managed (its CMEK is behind an access-request form on 2026-09-13 and creation-time only); of the Cloud Logging buckets, the organisation `_Default`/`_Required` buckets and the Tier R content buckets stay Google-managed, while the two central buckets and Tier W+ content buckets take explicit HSM keys (P112); both exceptions re-opened if the TISAX label becomes Strictly confidential (P20) | An exception recorded once, dated, with its reopening condition is a finding an assessor accepts; the same exception discovered per project is not | as stated for Firestore; for the central log buckets P112 applies (per-bucket CMEK verified available) | platform owner; ISMS | TISAX assessment order | [09](09-supply-chain-secrets-recovery.md) §2.2, §2.4; [11](11-tisax.md) R-08 | — |
| P122 | proposed | The Tier X sandbox design: GKE Autopilot in `europe-west1` per Tier X project, Agent Sandbox add-on with gVisor, per-call `SandboxClaims`, namespace egress to the agent's gateway only, no identity in the sandbox, attested base image, KF-1 as the kill; otherwise "no code execution in the EU"; Agent Runtime Code Execution (EU-resident: `europe-west1`, `eu` endpoint) is the second candidate once its stage, isolation and egress model are verified. Records GKE Agent Sandbox as GA (2026-05-21), meeting P27's stage condition and none of §11.5's others; Tier X stays closed | A future AGI agent is a tier, not a new design (HLD §11); the tier's containment must be written before anyone asks to open it | GKE as decided; Cloud Run sandboxes rejected for now (Pre-GA, regions unstated); Agent Runtime Code Execution not adopted now (stage, isolation, egress model unverified; residency no longer the reason) | platform owner | Tier X (not before every other §11.5 condition) | [09](09-supply-chain-secrets-recovery.md) §4.3, §5 | — |
| P123 | proposed | Re-qualification extends to the reporting path: a model-pin change on `eve-advisor` (or any advisory monitor) resets its paging thresholds to defaults and re-runs the seeded-fault set before it may page at severity 2 again | D9 (re-qualified on every model pin change) applied to the one model-permitted path; otherwise a model swap silently changes what pages a human | as stated | Eve owner | `eve-advisor` build | [09](09-supply-chain-secrets-recovery.md) §3.5 | depends on P34; E-18 (thresholds); M-8 |
| P124 | proposed | `capability_eval_ref` per `model_pin` in the register row: a pointer to the provider's evaluation artefact for that model (FSF-derived report or model card); optional below Tier X, required for Tier X; the platform records that capability evaluation is provider-side and gates on measured behaviour within a catalogue | HLD §16: the platform does not evaluate model capabilities; the Art. 9 risk file must say so honestly and point at who does | as stated; the Art. 9 risk file carries the honesty line now | platform owner; AI-safety reviewer when the role exists | Tier X; the Art. 9 risk file | [09](09-supply-chain-secrets-recovery.md) §5.4 | Wall-E 6 (model pin) |
| P133 | proposed (value of P20) | TISAX target: label Confidential (`Assumption:`; Strictly confidential only if the ISMS classes `walle_audit`, content logs, `platform-identity-logs` or conversation history as secret — which also raises AL3 and reopens P12, P119, P110); no availability label; AL2 (AL2.5 accepted if proposed); scope location *tbd* from the ISMS, with Google, the witness's Google organisation and every third party as external IT services of that scope; mapping against ISA2027 with the 6.0.3 cross-reference, mandatory column for high protection need; NIS2 applicability delegated to the legal register | Strictly confidential + AL3 now would gate the whole platform on a physical assessment of a site not yet named | adopt; ISMS confirms label and scope before the order. Rejected: Strictly confidential + AL3 now (no partner requirement known); an availability label (not critical IT services) | ISMS (confirms); platform owner | assessment order | [11](11-tisax.md) §2 | — |
| P134 | proposed | Module scope: Information Security in full; the Data Protection module (9.x) not an assessment objective (controller-side internal platform) but the DPO's checklist; it becomes an objective for the scope of any hosted agent processing customer or OEM data as processor, enforced by a new register field `tisax_dp_scope` plus a dated DPO entry at the admission gate; Prototype Protection not applicable, dated negative determination | "Hundreds of agents" makes a processor-role agent likely; the rule must exist before the first one, and the register field is what enforces it | adopt; treating 9.x as applicable now rejected (wrong role, wrong label); [05](05-registry-and-autonomy-contract.md) adds the field | DPO; ISMS; platform owner (the field) | assessment order; the first processor-role agent | [11](11-tisax.md) §3 | — |
| P135 | proposed | [11](11-tisax.md) is the TISAX compliance-mapping page the HLD named `0x-compliance-mapping.md`, and its §4 is the supplier file until a separate page exists; the Google/platform/ISMS split kept per control and re-dated on every product-stage change; the ENX result share for SYN0NK requested by the ISMS and filed; per-service coverage *tbd* per item until closed from the share or Compliance Reports Manager, never assumed; the 5.3.4 segregation note is §4.4 | Treating Google's public compliance page as evidence is the shared-responsibility mistake an assessor catches first | adopt; the supplier file may later split into its own page | platform owner; ISMS | assessment order | [11](11-tisax.md) §4, §14 | closes HLD §14.3's page-name placeholder |
| P140 | proposed | Platform risk register as a page kept by the security reviewer (or an export of the ISMS's tool), three-point L/I until the ISMS maps its scale, treatment and a signed acceptance file per accepted row, row one R-01 the super-admin deviation, rows R-02..R-17 as [11](11-tisax.md) §10, refreshed at every stage decision and management review; an accepted row without its acceptance at the assessment order blocks the order | 1.4.1 wants a register with owners and acceptance; residual tables inside each page are not one | adopt | security reviewer; ISMS | assessment order | [11](11-tisax.md) §10 | R-01 = P33; Wall-E 41 (single region) as an accepted row |
| P141 | proposed | Legal and contractual register: GDPR; EU AI Act 2024/1689 as amended by Regulation (EU) 2026/1744; national labour and works-council law (country *tbd*); NIS2 (applicability *tbd*; ENX crosswalk of 2025-06-29 if it applies); Google Cloud and Workspace agreements and data-processing terms (titles *tbd*); Preview-product terms; TISAX participation terms (2023-07-16); customer/OEM security clauses; internal policy ids; owned by the ISMS with legal, platform rows by the platform owner, audited annually | Every Preview product in the design (custom constraints, Cloud Run Agent Identity, sandboxes) runs under terms that differ from GA terms; the register is where that is admitted | adopt | ISMS; legal; platform owner | assessment order | [11](11-tisax.md) §11 | Wall-E 8 (jurisdiction) |

---

## 6a. Proposed by the setup procedures (P144–P191)

Added 2026-09-15. The human-executed setup procedures ([setup/README.md](setup/README.md)) answer
[13-setup-procedure-review.md](13-setup-procedure-review.md) and, in doing so, resolve 48
contradictions between the design and what can be built. Each resolution is a decision the
procedures are written to, listed here in the procedures' order (SD-01 to SD-48 of
[setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48)). Every
row's state is `proposed by the setup procedures, pending the owner's signature`: it stands, and
the gated step refuses (`tools/decision-need.sh`) until the dated record named in "Recorded in"
is signed by the signatories setup 03 lists; the "Gate it blocks" column is the setup file and
step that refuses without the record, and the links go to those steps. The rows sit in their own
group because their gates are procedure steps rather than tiers; a row that also moves a tier
gate says so. P153–P155 (SD-10 to SD-12) are the scope delta of 2026-09-15 — Eve monitors every
human super admin from its first run — recorded in
[../eve/01-hld.md](../eve/01-hld.md#scope-from-the-first-run-the-human-super-admins-2026-09-15)
and [../eve/03-lld.md §14.1](../eve/03-lld.md#141-scope-from-the-first-run-the-human-super-admins-2026-09-15).
Rows are compressed: the full contradiction and resolution are in setup 03 §7–§10 and the
setup file each row links to.

| Id | State | Decision | Why it matters | Recommendation / value | Owner | Gate it blocks | Recorded in | Agent-set decisions |
|---|---|---|---|---|---|---|---|---|
| P144 | proposed by the setup procedures, pending the owner's signature | Bootstrap deviation from the factory: until the factory repository is committed and its negative test passes, the platform owner performs each module's steps by hand from setup 17 (FM-AGENT, FM-VERIFIER, FM-IMPROVER, FM-TENANT-APP, FM-REVOKE, platform-core rows), producing exactly the module's folder placement, labels, tags, APIs, policies, grants, budget, sinks, deny and PAB entries, proven by the zero-diff checker; each run is a bootstrap deviation register entry, superseded by a `terraform import` plus an empty plan when the factory exists | HLD §3.1–§3.2 and 02 §3 say nothing under `fld-agentic-platform` is made by hand after Stage 0, but no factory code exists, every agent runbook's fallback builds the wrong thing and the first privileged acts cannot go through PAM before a folder or entitlement exists (S001, S002, S019, S027, S042) | as stated; the register entry carries module, inputs, checker output, date and approver | platform owner; IT security; ISMS | setup 06 first step; every module equivalent in setup 17 | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-01, record slug `setup-conventions-and-bootstrap-deviation`; [setup 06 OB-1.1](setup/06-organisation-bootstrap-and-roster.md#ob-11-preflight-variables-decisions-people-and-the-shell); [setup 17 FM-1.2](setup/17-factory-module-equivalents-and-tier-r-gate.md#fm-12-commit-the-zero-diff-checker) | S001, S002, S018, S019, S027, S042, S059; HLD §3.1–§3.2; [02](02-landing-zone-and-tiers.md) §3; [04](04-identity-and-privileged-access.md) §5.2 |
| P145 | proposed by the setup procedures, pending the owner's signature | The super-admin gate is split: the P-SA `env=prod` register row merges with `gate_checklist: pending` and `privilege: super_admin_pending`, so `WALLE_PROJECT` and every pre-grant deployment may be built; the gate refuses only the Workspace Super Admin assignment to `walle@` and the Stage 0 decision record; PSA1 counts only `env=prod` rows and the twin's `env=nonprod` P-SA row merges without a checklist | 02 §1.3 P1 and the old SETUP refused `WALLE_PROJECT` until every gate line was green, while G10, G12, G13 and G14 need Wall-E's services deployed; the twin's row was refused as a second P-SA row although only the twin can produce G10, G11 and G14 (S008, X-ORG-13) | as stated; "twin first" rejected because it loops | platform owner; IT security; ISMS | setup 16 register CI rules | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-02, record slug `platform-model-and-privilege`; [setup 16 RG-2.2](setup/16-register-and-shared-registry.md#rg-22-write-the-register-row-schema); [setup 31 WD-1.3](setup/31-wall-e-project-and-data-plane.md#wd-13-write-and-merge-the-p-sa-production-row-and-the-manifest) | S008, X-ORG-13; [02](02-landing-zone-and-tiers.md) §1.3 P1, PSA1; [11](11-tisax.md) §6.3; supersedes P1's "refuse the project" reading |
| P146 | proposed by the setup procedures, pending the owner's signature | Eve's G-4 is green on pre-grant evidence: a seeded admin change by a human super admin on the production tenant from the second human's independent proof, present in `eve_workspace_logs` and `eve_workspace_reports` within the lag budget; a seeded change by the sandbox twin robot in the nonprod datasets; the production robot's consent login event; `walle@` admin-change rows move to the post-grant verification | eve/05 G-4 asked for `walle@` shadow-run rows that need the grant, and the admin audit log records changes only, so a shadow run leaves no row (S039, S141) | as stated; eve/07 Phase 7 check 4 split into a pre-grant and a post-grant half | platform owner; second human; IT security | setup 28 the proof | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-03, record slug `eve-h-scope-and-independence`; [setup 28 EV-2.5](setup/28-eve-independent-proof-and-sandbox-drills.md#ev-25-the-eve_workspace_reports-row); [setup 28 EV-2.4](setup/28-eve-independent-proof-and-sandbox-drills.md#ev-24-the-eve_workspace_logs-row) | S039, S141; [../eve/05-stages.md](../eve/05-stages.md) G-4; supersedes eve/07 Phase 7 check 4 |
| P147 | proposed by the setup procedures, pending the owner's signature | The two witness super admins are IT security people who are neither tenant super admins nor the platform owner, the second human or the second operator; the second human keeps `sa-2-admin@`, holds a non-administrator witness account as owner of record and receives witness alarms; the grant needs five named humans, or four with a dated ISMS exception; witness recovery: self-recovery Off, reset by the other witness administrator, support-assisted recovery through domain ownership | Topology §1.2 and §2 exclude tenant super admins from administering the witness, yet §7.5, HLD §13.2 and eve/05 G-1 named the second human, a tenant super admin, as witness administrator (X-ORG-05) | as stated | IT security; ISMS; finance; both witness administrators; second human; platform owner | setup 04 witness purchase; setup 08 W-1 | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-04, record slug `witness-organisation`; [setup 04 PU-2.4](setup/04-purchases-and-lead-times.md#pu-24-witness-domain-and-billing-path-row-4-p14-sd-28); [setup 08 WO-1.1](setup/08-witness-organisation.md#wo-11-check-the-gates-and-the-separation-of-people) | X-ORG-05, X-ORG-09; P137; [11](11-tisax.md) §7.1–§7.2; [../project-topology.md](../project-topology.md) §1.2, §2, §7.5; HLD §13.2; eve/05 G-1 |
| P148 | proposed by the setup procedures, pending the owner's signature | The twin's OAuth clients (Wall-E narrow, Wall-E broad, Eve) live in the nonprod projects and are External, publishing status In production, each marked Trusted in the sandbox Admin console before consent; production clients stay Internal; G13 asserts both forms | Google refuses a sandbox-tenant user on an Internal client of the tenant's organisation, and Testing status expires refresh tokens after 7 days, so no twin token could be stored and G10, G11, G14, G-5 and G-7 could not go green (X-ORG-01) | as stated; the gate record states that the `org_internal` refusal is not reproduced by the twin | platform owner; IT security; ISMS | setup 24 twin consent | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-05, record slug `platform-model-and-privilege`; [setup 24 EW-5.1](setup/24-eve-workspace-identity-and-audit-feeds.md#ew-51-confirm-the-clients-audience-and-publishing-status); [setup 21 SB-6.6](setup/21-sandbox-tenant-and-nonprod-foundation.md#sb-66-read-the-sandbox-api-controls-and-record-the-twin-oauth-client-rule) | X-ORG-01; [02](02-landing-zone-and-tiers.md) §3.5 row 4 |
| P149 | proposed by the setup procedures, pending the owner's signature | The sandbox organisation gets its own logging: a sandbox super admin turns on "Share data with Google Cloud services"; sinks created in the sandbox organisation route to the nonprod destinations (Eve's twin `eve_workspace_logs`, Wall-E's twin trigger topic); writer identities are granted on those destinations and admitted by the sandbox customer id; both sinks join the drift inventory; SIEM fixtures are synthetic events with production values | G-5, G-7, the Eve drill and the SA fixtures expected sandbox events in `S-org`, the SIEM and Eve's sink, but Workspace audit logs land at the sandbox tenant's own Cloud organisation (X-ORG-02) | as stated | platform owner; IT security; ISMS | setup 21 sandbox logging; setup 24 the twin sink | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-06, record slug `platform-model-and-privilege`; [setup 21 SB-6.4](setup/21-sandbox-tenant-and-nonprod-foundation.md#sb-64-turn-on-share-data-with-google-cloud-services-in-the-sandbox); [setup 24 EW-1.5](setup/24-eve-workspace-identity-and-audit-feeds.md#ew-15-create-the-sink--irreversible-as-history) | X-ORG-02, X-ORG-03, X-ORG-15; eve/05 G-5, G-7; [07](07-monitoring-detection-incident-response.md) §6.2; [08](08-data-logging-retention-sovereignty.md) §3 |
| P150 | proposed by the setup procedures, pending the owner's signature | `eve-export@` writes an hourly heartbeat row to the witness carrying the last hour's row counts, so feed silence becomes a value condition, plus a sub-daily export (`Assumption:` every 6 hours) under the same two grants; in the witness, log-based metrics on the witness table's resource name, absence windows of about 90 minutes for the heartbeat and at most 23.5 hours for the export, policies created only after the first heartbeat has landed | 07 §7 H-4 and eve/03 §14 set a 26-hour window on a daily push, but a Cloud Monitoring metric-absence condition allows at most 23.5 hours and never fires before its first data point, and a BigQuery row metric is visible only after up to about 6 hours (X-ORG-06) | as stated; until the policies exist a witness administrator checks by hand | platform owner; second human; IT security | setup 26 export jobs; setup 27 absence policies | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-07, record slug `eve-h-scope-and-independence`; [setup 26 ER-4.5](setup/26-eve-reporting-and-witness-export.md#er-45-deploy-the-hourly-heartbeat-job-push-disabled-blocked); [setup 27 WG-3.5](setup/27-witness-grants-and-alarms.md#wg-35-the-heartbeat-absence-policy-about-90-minutes) | X-ORG-06, X-RQB-06; [07](07-monitoring-detection-incident-response.md) §7; supersedes the windows of [../eve/03-lld.md](../eve/03-lld.md) §14 and P97's 26 hours |
| P151 | proposed by the setup procedures, pending the owner's signature | Severity 1 is routed twice: route 1, minutes, the organisation-owned paging service of 07 §9.2 with the second human as a parallel recipient plus email; route 2, backstop, an incident-mode push by `eve-export@` to the witness and a log-based policy there notifying the second human and both witness administrators; reports about the administrator use route 1 to their sole recipient only, plus route 2; if the paging service is not contracted when Eve goes live, route 1 is a dated interim on Cloud Monitoring channels | eve/03 §15 and 07 §8 routed severity 1 "through the witness channels", which only `eve-export@`'s daily push can reach, so the witness would learn of a severity 1 up to a day later (X-ORG-07) | as stated; G-6 evidence covers both routes | platform owner; second human; IT security; incident commander | setup 15 part A | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-08, record slug `eve-h-scope-and-independence`; [setup 15 PS-2.4](setup/15-pager-siem-and-detections.md#ps-24-the-subject-report-escalations); [setup 26 ER-3.5](setup/26-eve-reporting-and-witness-export.md#er-35-prove-route-1-for-a-platform-event) | X-ORG-07; [07](07-monitoring-detection-incident-response.md) §8, §9.2; supersedes the channel column of [../eve/03-lld.md](../eve/03-lld.md) §15 |
| P152 | proposed by the setup procedures, pending the owner's signature | Wall-E decision 6 closes before setup 35 and Mo-11: a GA model on the `eu` multi-region endpoint retiring at least 6 months after Stage 1 (candidate on 2026-09-15: `gemini-3.5-flash`, re-read on the pin date); the model client is built with location `eu` in the agent code, `GOOGLE_CLOUD_LOCATION` is never set, CI refuses an ambient-location client and a pin retiring within 90 days; setup 35 verifies one `generateContent` to the `eu` endpoint or stops; no step uses a Gemini 2.5 model | No procedure named a model; `europe-west1` serves only the Gemini 2.5 family, retiring in October 2026, and every GA successor is on `global`, `eu` and `us` only (X-RQB-01) | as stated | platform owner; IT security; DPO | setup 35 the engine; setup 40 Mo-11 | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-09, record slug `model-pin`; [setup 35 WE-5.1](setup/35-wall-e-engine-registration-and-gateways.md#we-51-read-the-pin-and-its-retirement-date-on-the-day); [setup 40 MA-10.1](setup/40-mo-after-stage-0.md#ma-101-the-s4-entry-record-and-the-model-pin) | X-RQB-01; Wall-E 6 (model pin) closed; [../wall-e/03-lld.md](../wall-e/03-lld.md) `model_pin`; [02](02-landing-zone-and-tiers.md) §3.6; [08](08-data-logging-retention-sovereignty.md) DL-7.3 |
| P153 | proposed by the setup procedures, pending the owner's signature | Eve is built in two halves. Eve-H (setup 23–28), before Wall-E exists: the six-stream sink, `eve@` and its consent, the Reports API poll by actor for every account on the committed roster and every live admin-role holder, the roster check, the tenant-integrity and posture rules `SA-01`…`SA-10` with the actor set widened to any roster human, the self-integrity rules `SI-01`…`SI-13`, H-1 and the Eve credential check, `eve.incidents` and `eve.pages`, the reporting contract and the witness export; no halt target exists yet, so every halting rule pages severity 1 and records `halt_target_pending`. Eve-W (setup 36, 41) adds the Wall-E side and the gate limb | The owner asked on 2026-09-15 that Eve monitor every human super admin from its first day; the design scoped Eve's observe-and-report layer to a super-admin Wall-E, minted `eve@` at the grant sitting and aimed the catalogue at `walle@` (HLD §13.2, eve/03 §13–§15, eve/05); the Eve runbook both held and ran Phases 8–11 before the grant (S140) | as stated; recorded in [../eve/01-hld.md](../eve/01-hld.md) and [../eve/03-lld.md](../eve/03-lld.md) §14.1 | platform owner; **second human**; IT security | setup 23 first step | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-10, record slug `eve-h-scope-and-independence`; [setup 23 EP-0.1](setup/23-eve-project-and-evidence-stores.md#ep-01-open-the-sitting-and-check-the-gates); [setup 25 EH-2.3](setup/25-eve-human-super-admin-detections.md#eh-23-the-detection-catalogue-sa-01sa-10-with-actor--any-roster-human-and-si-01si-13) | Owner's request of 2026-09-15; HLD §13.2; [../eve/03-lld.md](../eve/03-lld.md) §13–§15; [../eve/05-stages.md](../eve/05-stages.md); S140; generalises P68 and P98 RP-3 to human actors |
| P154 | proposed by the setup procedures, pending the owner's signature | Monitoring named administrators is employee monitoring: setup 25's reconciler and poll jobs are BLOCKED until a DPO record covers monitoring of administrator accounts (purpose: detection of misuse of tenant-wide privilege; data: admin, login, token, SAML, groups and Reports activity metadata, no content; retention: `eve_workspace_*` 400 days and the witness copy; recipients: the second human, the security reviewer, the incident commander) and works-council information is given where required; the D7 letter of week one includes it | D7 and P129 were framed around Wall-E's processing; Eve-H processes the activity metadata of named human administrators from its first day and no page recorded a legal basis, purpose or recipients | as stated | DPO; platform owner; **second human**; HR | setup 25 reconciler and poll jobs (BLOCKED until signed) | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-11, record slug `monitoring-of-administrators`; [setup 25 EH-0.2](setup/25-eve-human-super-admin-detections.md#eh-02-the-sd-11-gate-the-dpo-record-for-monitoring-named-administrators); [setup 25 EH-4.1](setup/25-eve-human-super-admin-detections.md#eh-41-deploy-eve-reports-poll--blocked) | Owner's request of 2026-09-15; PREREQUISITES D7; P129; [10](10-eu-ai-act.md) §4.10; [07](07-monitoring-detection-incident-response.md) §12.2 |
| P155 | proposed by the setup procedures, pending the owner's signature | The monitored administrator installs Eve, so: `eve-owners@` is owned by the second human from setup 06; `EVE_PROJECT` is built under `ent-factory-singleton` for `fld-controllers` approved by the second human, never the platform owner, and every later elevation on it needs the second human's approval; the platform owner's standing `actAs` on Eve accounts is removed; `eve/config` has branch protection with the second human as required reviewer; findings, pages and the heartbeat are copied to the witness with the absence alarms there; the second human independently proves detection on a seeded super-admin action | HLD §13.2 says nothing inside the organisation is out of a super admin's reach and Eve's independence there is detective only; the platform owner, a subject of Eve-H, holds the bootstrap exception, builds Eve and was custodian of key A for `eve@` (04 §8.1, S143, X-ORG-12) | as stated | platform owner; **second human**; IT security | setup 06 `eve-owners@`; setup 23 first step | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-12, record slug `eve-h-scope-and-independence`; [setup 06 OB-5.2](setup/06-organisation-bootstrap-and-roster.md#ob-52-write-control_groups_file); [setup 23 EP-1.2](setup/23-eve-project-and-evidence-stores.md#ep-12-confirm-eve-owners-exists-and-that-the-platform-owner-does-not-own-it); [setup 28 EV-3.3](setup/28-eve-independent-proof-and-sandbox-drills.md#ev-33-the-platform-owner-holds-no-standing-role-that-can-edit-eve) | Owner's request of 2026-09-15; HLD §13.2; [04](04-identity-and-privileged-access.md) §5.2, §8.1; S143; X-ORG-12; P98 |
| P156 | proposed by the setup procedures, pending the owner's signature | The Gemini Enterprise baseline (setup 19–20) runs after the Tier R record (setup 17); the Tier C gate is re-scoped to SCC findings routed with a test finding; SIEM rules and the penetration test move to Tier P; the README's builder order is corrected | HLD §0.4 opened Tier C before Tier R, but GE-2 needs the tenant-app module and the Tier C gate needed a SIEM, a security reviewer and SCC Premium (S047, S050) | as stated | platform owner; IT security; ISMS | setup 19 first step | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-13, record slug `setup-conventions-and-bootstrap-deviation`; [setup 19 GE-0](setup/19-gemini-enterprise-import-and-baseline.md#ge-0-the-sitting-and-the-entry-gate); [setup 20 GG-8](setup/20-gemini-enterprise-gateway-and-tier-c-gate.md#gg-8-the-tier-c-gate-ge-14) | S047, S050; HLD §0.4; [03](03-gemini-enterprise-environment.md) §16 |
| P157 | proposed by the setup procedures, pending the owner's signature | P22 (the git host) closes in setup 03, before setup 10, and its gate moves to before Tier R; `GIT_HOST` and `GIT_OIDC_ISSUER` are set there; setup 03 also creates the platform repository with branch protection and CODEOWNERS (the second human required on the roster, control groups, `eve/`, `oncall.yaml` and the ladder); commits made before the remote exists are local with a signed review record and are pushed on creation | The factory's WIF provider (B19) needs the git host's issuer and repository ids before Tier R, but P22's gate was Tier W (S051); setup 06, 09, 12 and 13 commit under two-person review before setup 16 | as stated | platform owner; IT security | setup 03 §12; setup 10 WIF provider | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-14, record slug `git-host-and-repository`; [setup 03 §12](setup/03-decisions-and-people.md#12-the-platform-repository); [setup 10 CP-4.1](setup/10-core-projects-and-ci-identities.md#cp-41-check-the-git-host-gate-and-read-the-repositorys-numeric-ids) | S051; P22's gate moved; [02](02-landing-zone-and-tiers.md) B19 |
| P158 | proposed by the setup procedures, pending the owner's signature | P11 closes at stage 1 by IT security and finance choosing SCC Premium pay-as-you-go or the subscription, with the payer written; setup 09 activates Premium with `eu` residency before any location policy; setup 13 applies the first location policy to a nonprod folder and reads SCC tier and residency before and after; a billing-export query on the SCC SKUs is added; the 2 million monthly Model Armor tokens included are recorded against `GEMINI_PROJECT` | HLD §0.5's interim platform budget line cannot hold organisation-level pay-as-you-go Premium, which bills every project's billing account; Google warns that a location policy deployed after an automatic Standard activation can deactivate SCC (X-RQB-04) | as stated | IT security; finance; platform owner | setup 04 SCC purchase; setup 09 activation | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-15, record slug `billing-scc-and-siem`; [setup 04 PU-2.3](setup/04-purchases-and-lead-times.md#pu-23-scc-premium-payer-or-contract-row-3-p11-sd-15); [setup 09 FS-7.3](setup/09-folders-and-security-command-center.md#fs-73-activate-scc-premium-with-eu-data-residency-branch-not-activated) | X-RQB-04; P11 closed at stage 1; P94; HLD §0.5; [07](07-monitoring-detection-incident-response.md) §3 |
| P159 | proposed by the setup procedures, pending the owner's signature | A dedicated standard billing account in EUR (not a reseller sub-account) with a named billing administrator from finance; Billing Account User and Costs Manager on that account only, to `sa-1-admin@` with a recorded expiry and to `factory-apply@`; an alert on self-grants through Organization Administrator; a linking test and a quota request for about 20 projects filed early; standard and detailed export to an EU dataset in `LOGGING_PROJECT`; if a shared account is imposed, spend is read only through a label-filtered view | The account and its administrator were *tbd*; a shared account would put the organisation's whole spend into `LOGGING_PROJECT`'s export; project quota increases take days (X-RQB-05) | as stated | finance; billing administrator; platform owner | setup 07 first step | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-16, record slug `billing-scc-and-siem`; [setup 07 BA-1.1](setup/07-billing-account.md#ba-11-identify-the-account-and-confirm-it-is-standard-open-and-owned-by-the-organisation) | X-RQB-05; P31; [02](02-landing-zone-and-tiers.md) §3.7 |
| P160 | proposed by the setup procedures, pending the owner's signature | No Cloud Logging folder default storage location. The observability default storage location `europe-west1` is set on `fld-agentic-platform` before the first project; every project gets a user-defined `europe-west1` bucket with the `_Default` sink redirected to it, keeps `_Required` in `global`, and gets an explicit `_Trace` bucket before traffic | R9 wanted regional logs; new projects get `_Required` and `_Default` in `global`, a bucket's location cannot change, and a Cloud Logging folder default would also move `_Required` and blind Sensitive Actions, on which 07 §3 relies (X-RQB-03) | as stated; 08 R9, S11 and wall-e/ARCHITECTURE corrected | platform owner; IT security; ISMS | setup 09 location step | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-17, record slug `setup-conventions-and-bootstrap-deviation`; [setup 09 FS-2.1](setup/09-folders-and-security-command-center.md#fs-21-set-the-observability-default-storage-location-to-europe-west1); [setup 10 CP-1.7](setup/10-core-projects-and-ci-identities.md#cp-17-route-_default-to-a-regional-bucket-leave-_required-global) | X-RQB-03; supersedes [08](08-data-logging-retention-sovereignty.md) R9 and S11 as written |
| P161 | proposed by the setup procedures, pending the owner's signature | Missing makers named: topology rows 36, 41 and 44 are made in setup 16 and row 40 in setup 14, each as a named step; `ent-org-sink` joins the PAM catalogue (organisation `roles/logging.configWriter`, 1 hour, approver the second human, the security reviewer added as a second eligible approver once appointed, never a no-approval entitlement); `ge-admins@` is created by hand by a super admin in setup 06 and `factory-groups@` in setup 10 | `platform-core` is not one of the five factory modules, so four topology rows had no maker (S048); an organisation sink needs `logging.configWriter` at the organisation and no entitlement granted it (S142); two groups had no maker (S049) | as stated | platform owner; IT security; ISMS; second human | setup 12 `ent-org-sink` | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-18, record slug `platform-model-and-privilege`; [setup 12 PA-3.3](setup/12-privileged-access-catalogue.md#pa-33-create-and-test-ent-org-sink); [setup 14 CL-7.3](setup/14-central-logging-and-billing-export.md#cl-73-grant-the-row-40-readers-or-record-them-pending) | S048, S049, S142; [../project-topology.md](../project-topology.md) §7.6; [04](04-identity-and-privileged-access.md) §2.4, §5.2 |
| P162 | proposed by the setup procedures, pending the owner's signature | P49, 12 §7 and 03 §4 align with 04 §5.2: `ent-ge-admin` at Tier C is "activate without approvals", requester `ge-admins@`, 1 hour, justification required, security-reviewer approval added later as a dated change; the app-level binding and the removal of project-level user bindings are acts of `ge-admins@` through `ent-ge-admin`, CI and the drift job only verify; a grant is obtained before any standing `agentspaceAdmin` is removed | Three pages gave three approval models for `ent-ge-admin`, one with the same person as approver, which PAM refuses (X-GE-12); `discoveryengine.editor` lacks `engines.setIamPolicy`, so CI cannot own the app-level binding (X-GE-06) | as stated | platform owner; IT security; ISMS | setup 12 `ent-ge-admin`; setup 19 | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-19, record slug `platform-model-and-privilege`; [setup 12 PA-5.1](setup/12-privileged-access-catalogue.md#pa-51-create-and-test-ent-ge-admin-tier-c-no-approval); [setup 19 GE-5](setup/19-gemini-enterprise-import-and-baseline.md#ge-5-access-grant-first-fill-bind-test-then-remove) | X-GE-06, X-GE-12; P49 aligned; [03](03-gemini-enterprise-environment.md) §4, §15; [04](04-identity-and-privileged-access.md) §5.2 |
| P163 | proposed by the setup procedures, pending the owner's signature | The Gemini Enterprise baseline never lowers retention: it keeps or raises the value setup 05 recorded; P52 and R8's interim 30 days apply only where they do not shorten it, and any reduction is its own step after P13 with notice to users at least one retention period ahead and rollback "none"; `ge-users@`'s membership source is defined and the group filled and counted before any removal; the allow-list is the union of enabled services and the design list, dry-run before enforcement; `CmekConfig` is its own decided step | GE-6 lowered retention to 30 days with no documented recovery of deleted chats; GE-5 removed access before `ge-users@` was filled; GE-4's allow-list refused services the app uses; GE-10 bound all traffic through an unproven gateway; a default `CmekConfig` changes end-user data with no safe rollback (X-GE-01 to X-GE-05) | as stated | DPO; platform owner; Mo owner; second human; HR | setup 19 first write | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-20, record slug `retention`; [setup 19 GE-6](setup/19-gemini-enterprise-import-and-baseline.md#ge-6-engine-and-assistant-settings-and-the-cmek-decision); [setup 05 GI-3.2](setup/05-gemini-enterprise-inventory.md#gi-32-read-the-current-chat-history-retention-and-set-the-floor) | X-GE-01 to X-GE-05; P50, P52 narrowed; [03](03-gemini-enterprise-environment.md) §5, §11, §16; [08](08-data-logging-retention-sovereignty.md) R8 |
| P164 | proposed by the setup procedures, pending the owner's signature | D8 reads "`eu` only; a global or `us` app stops the build and opens a decision record"; setup 05 stops on any other location, setup 35's registration refuses any location other than `eu`; "Tier C gate closed" becomes gate line G21 | The old SETUP D8 and PREREQUISITES accepted a global app, which GE-0 stops on, and a global app cannot use `gemini-egress` in `europe-west1` or CMEK (X-GE-13) | as stated | platform owner | setup 05 location record | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-21, record slug `gemini-app-location`; [setup 05 GI-2.1](setup/05-gemini-enterprise-inventory.md#gi-21-list-the-apps-in-every-location-and-apply-the-location-stop); [setup 35 WE-0.3](setup/35-wall-e-engine-registration-and-gateways.md#we-03-stop-unless-the-gemini-enterprise-app-is-eu-x-ge-13) | X-GE-13; supersedes PREREQUISITES D8; [03](03-gemini-enterprise-environment.md) §16 GE-0 |
| P165 | proposed by the setup procedures, pending the owner's signature | Deny policies name each agent project's principal as `principal://agents.global.org-${ORG_ID}.system.id.goog/resources/aiplatform/projects/${PROJECT_NUMBER}`, added per project by the module equivalent; the P8 spike in setup 18 proves the spelling with a denied call and Policy Troubleshooter; 04 §3 and the factory use the proven form; a proven fallback is accepted with a dated exception | The old SETUP and 04 §3 used `principalSet://agents.global.org-ORG_ID.system.id.goog/*`, which no Google page documents, and Google's IAM principals page says deny policies do not support agent identity sets (S072) | as stated | platform owner; IT security; ISMS | setup 13 deny policies | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-22, record slug `setup-conventions-and-bootstrap-deviation`; [setup 13 OP-2.5](setup/13-organisation-policies-deny-and-pab.md#op-25-write-the-custom-constraints-the-deny-policies-and-the-pab-rules); [setup 18 KS-3.2](setup/18-model-armor-floor-spikes-and-kill-switch.md#ks-32-p8-part-a-the-deny-entries-for-canary-r-and-a-denied-call-from-a-service-account) | S072; P8; [04](04-identity-and-privileged-access.md) §3 |
| P166 | proposed by the setup procedures, pending the owner's signature | Eve's locked evidence bucket is in `europe-west1`, consistent with 08 S5 and the single-region key `eve-evidence`; a pre-lock location check is part of setup 23; eve/01 corrected | The old Eve runbook created the bucket in the EU multi-region, 08 S5 said `europe-west1` and eve/01 said EU; the location is immutable and the lock irreversible (S135) | `europe-west1` | platform owner; second human; Mo owner; both witness administrators; IT security | setup 23 bucket create (IRREVERSIBLE location) | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-23, record slug `names-register`; [setup 23 EP-7.2](setup/23-eve-project-and-evidence-stores.md#ep-72-create-the-bucket); [setup 23 EP-7.3](setup/23-eve-project-and-evidence-stores.md#ep-73-verify-location-key-access-and-prevention-before-anything-is-written) | S135; [08](08-data-logging-retention-sovereignty.md) S5; [09](09-supply-chain-secrets-recovery.md) §2.4; [../eve/01-hld.md](../eve/01-hld.md) components table |
| P167 | proposed by the setup procedures, pending the owner's signature | Mo reads plan hashes from `walle_audit.plans`; `mo-analyst@` gets no `run.invoker` on `walle-actions`; topology row 8 is retired; MD-3 becomes a denial test that expects refusal | `deny-improvers` forbids `run.invoker` for improver principals on credential holders, but Mo-6 granted `mo-analyst@` invoker on `walle-actions`; once the deny applies, MD-3 and the plan-hash recompute get 403 (S063) | as stated | platform owner; Mo owner; IT security | setup 22 first step; setup 36 Mo-6 | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-24, record slug `mo-decisions`; [setup 22 MO-0.1](setup/22-mo-foundations.md#mo-01-open-the-sitting-and-check-the-gates); [setup 36 WJ-0.1](setup/36-wall-e-joins-to-eve-and-mo.md#wj-01-open-the-sitting-and-check-every-gate) | S063; [02](02-landing-zone-and-tiers.md) §2.2 `fld-improvers`; retires [../project-topology.md](../project-topology.md) row 8; M-rows of [../mo/08-open-decisions.md](../mo/08-open-decisions.md) |
| P168 | proposed by the setup procedures, pending the owner's signature | Every nonprod folder holding a twin component (`fld-agents-p-nonprod`, `fld-agents-p-sa-nonprod`, `fld-controllers-nonprod`; `fld-improvers-nonprod` only if Mo reads sandbox data) admits the sandbox customer id, proven with the first real grant; the twin uses `walle-operators@` on the sandbox domain and the sandbox super admins as band-A and band-B requesters and approvers; the twin approval surface uses custom IAP OAuth credentials, recorded as a G14 difference; cross-customer Directory calls are verified to fail closed | 02 B5 admitted the sandbox customer id on two nonprod P folders only while topology said every nonprod folder, so `fld-controllers-nonprod` refused sandbox principals and IAP's Google-managed client admits only users of the resource's organisation (X-ORG-03) | as stated | platform owner; IT security; ISMS | setup 21 folder admission | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-25, record slug `platform-model-and-privilege`; [setup 21 SB-7.4](setup/21-sandbox-tenant-and-nonprod-foundation.md#sb-74-apply-the-child-policies-under-ent_platform_policy); [setup 21 SB-7.5](setup/21-sandbox-tenant-and-nonprod-foundation.md#sb-75-prove-admission-on-the-nonprod-folders-and-refusal-on-their-production-siblings) | X-ORG-03; [02](02-landing-zone-and-tiers.md) B5, PSA6, §3.5; [../project-topology.md](../project-topology.md) §2 |
| P169 | proposed by the setup procedures, pending the owner's signature | The Eve drill is split: (a) the sandbox part, by nonprod Eve — a tenant-integrity event, a roster diff in both directions, `log_pipeline_silent` on the nonprod sinks, paging through nonprod channels only, no witness contact; (b) the witness part, G-2's drill — production `eve-export@` withholds one push after the first heartbeat and before the grant; a witness administrator records both; no nonprod identity is ever granted on witness resources | The old drill ran on the sandbox tenant by nonprod Eve, which has no witness grant, yet expected the witness alarm and record; only production `eve-export@` holds the two cross-organisation grants (X-ORG-12) | as stated | platform owner; second human; IT security; incident commander | setup 28 drills | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-26, record slug `eve-h-scope-and-independence`; [setup 28 EV-4.2](setup/28-eve-independent-proof-and-sandbox-drills.md#ev-42-withhold-exactly-one-push); [setup 28 EV-6.2](setup/28-eve-independent-proof-and-sandbox-drills.md#ev-62-the-sandbox-seeded-actions--blocked-while-b-08-or-b-09-is-open) | X-ORG-12; eve/05 G-2, G-5, G-7; HLD §13.2; [../project-topology.md](../project-topology.md) rows 32–33 |
| P170 | proposed by the setup procedures, pending the owner's signature | Until W-2 exists, the witnessed paper custody record stays in the safe and a scan goes the same day to the interim evidence location; at W-2 the witness administrators upload every earlier record under `custody/`, `rota/` or `drills/`, named `<date>-<record>-v<n>`, never overwritten, from a record signed by the custodian and the other-line witness; Cloud Storage `DATA_WRITE` audit logs are enabled at W-2 with an upload alert to both witness administrators and the second human; G18 checks the full set | Custody and drill records must reach the witness bucket the same day, but the first keys are enrolled (setup 06) before the witness exists and locked objects cannot be replaced (X-ORG-11) | as stated | IT security; platform owner; second human | setup 06 first key enrolment | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-27, record slug `roster-reduction-and-custody`; [setup 06 OB-2.7](setup/06-organisation-bootstrap-and-roster.md#ob-27-enrol-two-security-keys-on-sa-1-admin); [setup 08 WO-3.3](setup/08-witness-organisation.md#wo-33-upload-the-backlog-and-every-later-record-the-same-day) | X-ORG-11; [04](04-identity-and-privileged-access.md) §7.1, §8.3; SETUP G11, G18 |
| P171 | proposed by the setup procedures, pending the owner's signature | The witness has a billing account parented by `org-witness`, administered only by the witness administrators, no tenant principal on it, with a budget alert to the witness email channel and a `billingEnabled` check in W-4; a Customer Care subscription on the witness organisation, or Access Approval recorded as unavailable after asking Google; a separately registered domain whose registrar and DNS sit outside the tenant's Cloud organisation and the Digital Workplace line, with registrar lock and 2SV; Cloud Identity edition chosen on grounds other than multi-party approval; the managed member constraint set first, the legacy one reset as a dated decision | P14 still offered the platform's billing account, which a tenant Organization Administrator could close; control of the witness domain's DNS is a recovery path; a customer id in `iam.allowedPolicyMemberDomains` admits the whole tenant, and the legacy constraint may stay enforced beside the managed one (X-ORG-08 to X-ORG-10, X-RQB-06, S061) | as stated | IT security; ISMS; finance; both witness administrators; second human; platform owner | setup 04 witness purchase; setup 08 W-1 | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-28, record slug `witness-organisation`; [setup 04 PU-2.4](setup/04-purchases-and-lead-times.md#pu-24-witness-domain-and-billing-path-row-4-p14-sd-28); [setup 08 WO-1.4](setup/08-witness-organisation.md#wo-14-sign-up-for-cloud-identity-on-the-witness-domain); [setup 27 WG-1.3](setup/27-witness-grants-and-alarms.md#wg-13-set-the-managed-member-constraint-at-org-witness) | X-ORG-08, X-ORG-09, X-ORG-10, X-RQB-06, S061; refines P14; [../project-topology.md](../project-topology.md) §7.5 |
| P172 | proposed by the setup procedures, pending the owner's signature | Wall-E decision 29 reads "before the super-admin grant (G10, G11, G14 and Eve G-7 need it)"; the sandbox edition equals production and is at least Enterprise Standard (Enterprise Plus if the Access Transparency stream is drilled); a domain on no other account with TXT and MX; seats; two named sandbox super admins; P59 closed; whether twin robots use hardware-key 2SV is decided before 04 §8.3's count changes; the sandbox SecOps export connected only if live fixtures are wanted | Decision 29 and three pages dated the sandbox "before Stage 1", after the grant that needs it (X-ORG-14); the sandbox had no edition, domain, seats or administrators, while multi-party approval needs Enterprise Standard or higher and two super admins (X-ORG-04) | as stated | platform owner; IT security; finance | setup 04 sandbox purchase; setup 21 | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-29, record slug `sandbox-tenant`; [setup 04 PU-2.5](setup/04-purchases-and-lead-times.md#pu-25-sandbox-workspace-tenant-row-5-decision-29-sd-29); [setup 21 SB-1.2](setup/21-sandbox-tenant-and-nonprod-foundation.md#sb-12-confirm-the-order-the-edition-and-the-keys-in-hand) | X-ORG-04, X-ORG-14; supersedes Wall-E 29's date; closes P59; PREREQUISITES D3; [04](04-identity-and-privileged-access.md) §8.3–§8.4 |
| P173 | proposed by the setup procedures, pending the owner's signature | Tenant-wide super-admin self-recovery Off and multi-party approval On become gate-day steps in setup 38 after a roster-ready check (two admin accounts with two keys each, sealed backup codes, the second human present, a rehearsed recovery); setup 06 creates the roster but does not set them; setup 30 does not set them | The old SETUP Phase 3 applied both early; with one person a lost key is recoverable only through Google Support and every covered change waits for a second super admin who may not exist (S012) | as stated | IT security; platform owner; second human | setup 06 (not set there); setup 38 | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-30, record slug `roster-reduction-and-custody`; [setup 38 GT-5.2](setup/38-super-admin-gate-and-grant.md#gt-52-super-admin-self-recovery-off-at-the-top-organisational-unit-g5); [setup 38 GT-5.4](setup/38-super-admin-gate-and-grant.md#gt-54-multi-party-approval-on-for-every-covered-setting-g4) | S012; P66, P68; supersedes SETUP Phase 3; [04](04-identity-and-privileged-access.md) §8.4 |
| P174 | proposed by the setup procedures, pending the owner's signature | Topology decision 44 or CC-33, and E-16's privilege and scope set, are closed before setup 24's consent; setup 24 checks their records before its first step | Eve's consent happened before a mid-phase stop on decision 44 or CC-33, leaving a live credential and no working Eve (S035) | as stated | platform owner; second human; IT security | setup 24 first step | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-31, record slug `eve-h-scope-and-independence`; [setup 24 EW-0.3](setup/24-eve-workspace-identity-and-audit-feeds.md#ew-03-check-topology-decision-44-or-cc-33-is-closed-sd-31) | S035, S140; depends on [../eve/09-open-decisions.md](../eve/09-open-decisions.md) E-16 and CC-33 / topology 44 |
| P175 | proposed by the setup procedures, pending the owner's signature | Setup 24 is the only creator of `eve@`; Wall-E's procedures and script never create it and only read its state; `eve@` is created in a staging OU without key-only enforcement (or with a one-day enrolment period), its two keys are registered with their custodians, the enrolment is verified, and only then is it moved to the service-identity OU | `walle_setup.py` created a licensed `eve@` with a tenant-wide role months before Eve's runbook (S088); the old Eve runbook created `eve@` in an OU that enforces security-key-only 2SV with no enrolment period, so it could not enrol its key (S038) | as stated | platform owner; second human; IT security | setup 24 `eve@` create | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-32, record slug `eve-h-scope-and-independence`; [setup 24 EW-3.3](setup/24-eve-workspace-identity-and-audit-feeds.md#ew-33-create-eve-in-the-staging-ou--irreversible-as-a-name); [setup 24 EW-3.2](setup/24-eve-workspace-identity-and-audit-feeds.md#ew-32-create-the-staging-ou) | S038, S088; [04](04-identity-and-privileged-access.md) §8.1; supersedes eve/07 Phase 8 |
| P176 | proposed by the setup procedures, pending the owner's signature | Mo's datasets are agent-neutral: `platform_metrics`, `platform_metrics_archive`, `platform_metrics_private` and `platform_metrics_views`, every table keyed on `agent_id`; the drop box `gs://mo-proposals`; one rename pass across topology, 08's store inventory and the Wall-E set before setup 22 | The design requires agent-neutral names before Stage 0, BigQuery datasets cannot be renamed, and Mo-1 still built `walle_metrics*` (S144) | as stated | platform owner; second human; Mo owner; both witness administrators; IT security | setup 22 dataset creates (IRREVERSIBLE) | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-33, record slug `names-register`; [setup 22 MO-6.3](setup/22-mo-foundations.md#mo-63-create-the-four-datasets) | S144; [../mo/04-artefacts-and-proposals.md](../mo/04-artefacts-and-proposals.md) §3.7; HLD §13.3 |
| P177 | proposed by the setup procedures, pending the owner's signature | The ladder publisher is `walle-deployer@` in `CICD_PROJECT` (`Assumption:` following P142's deploy identities), created in setup 10 so that setup 23 can grant it `objectCreator` on `ladder/` before the retention lock; it publishes only a ladder file from a merged commit carrying two human approvals, so the machine publishes but never raises a level; every step guards against an empty variable | `SA_WALLE_CI` was empty on the default path, so Eve's `ladder/` grant went to `serviceAccount:` and failed (S130) | as stated | platform owner; IT security; ISMS; second human | setup 10 identity create; setup 23 `ladder/` grant | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-34, record slug `platform-model-and-privilege`; [setup 10 CP-5.5](setup/10-core-projects-and-ci-identities.md#cp-55-create-walle-deployer-the-ladder-publisher); [setup 23 EP-7.6](setup/23-eve-project-and-evidence-stores.md#ep-76-grant-walle-deployer-create-only-on-the-ladder-prefix--before-the-lock) | S130; P142; supersedes the `CI_DEPLOYER` of `WALLE_PROJECT` in [../eve/01-hld.md](../eve/01-hld.md) components table and eve/07 Phase 9 |
| P178 | proposed by the setup procedures, pending the owner's signature | Mutating tests run only on the sandbox twin (setup 37); production runs only non-mutating tests with a `no_writes` halt and an audit re-read (setup 39); a `walle-denialtest@` fixture account and time-boxed PAM on `walle-agent@` only; `testIamPermissions` or Policy Analyzer for credential holders | The old SETUP §4 fixtures assigned admin roles in production, firing SA-06 and breaking G3, and a failing control turned a test into a real Super Admin write (S091); the tester could impersonate only `walle-operators-caller@` (S124) | as stated | platform owner; IT security; ISMS | setup 37; setup 39 | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-35, record slug `setup-conventions-and-bootstrap-deviation`; [setup 37 WR-0.3](setup/37-wall-e-sandbox-rehearsal.md#wr-03-refuse-a-production-shell); [setup 39 S0-7.1](setup/39-wall-e-stage-0.md#s0-71-halt-writes-on-both-services-before-the-run-s091) | S091, S124; supersedes SETUP §4 |
| P179 | proposed by the setup procedures, pending the owner's signature | Two milestones, Stage 0-pre (everything reachable before the grant) and Stage 0; the gate lines are G1–G18 plus G19 (Tier W rows), G20 (a K7 drill on `fld-agents-p-sa-nonprod` younger than 30 days) and G21 (Tier C gate closed); CI parses the checklist and refuses the register while any line lacks a date and signer; because the assignment happens in the Admin console, which no CI can refuse, the enforceable gate is the second human's refusal on the day; D13 reads G1–G21; 11 §6.3 gains the rows | The old SETUP promised a Stage 0 no written procedure reached (S010); nothing checked the Tier W rows or a recent K7 drill before the grant (S085); the grant was gated on a file's existence and D13 listed G1–G11 (S092) | as stated | platform owner; second human; IT security; incident commander; ISMS | setup 38 the assignment | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-36, record slug `super-admin-gate`; [setup 38 GT-0.2](setup/38-super-admin-gate-and-grant.md#gt-02-refuse-without-the-signed-decisions-s092); [setup 38 GT-3.1](setup/38-super-admin-gate-and-grant.md#gt-31-fill-the-remaining-twenty-lines-from-the-map) | S010, S085, S092, X-GE-13; supersedes SETUP Phase 2; extends [11](11-tisax.md) §6.3 |
| P180 | proposed by the setup procedures, pending the owner's signature | The `setup/` procedures are the canonical manual path; `walle_setup.py` may be used for a step only after its blocking defects for that subcommand are fixed and its self-test gains a live read-only mode; until then a step that cites the script is BLOCKED for the script and gives the manual commands | Nothing said which path to use per phase; the literal script path failed at Phase 12 and 13b and mixing paths created `eve@` twice (S082); the 220/220 self-test ran against stubs (S177) | as stated | platform owner; IT security; ISMS | setup 30 first step | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-37, record slug `setup-conventions-and-bootstrap-deviation`; [setup 30 WW-0.1](setup/30-wall-e-workspace-side.md#ww-01-open-the-sitting-and-check-the-gates) | S082, S177; supersedes wall-e/setup/README's path choice |
| P181 | proposed by the setup procedures, pending the owner's signature | Evidence has a home during the build: text records go to the build-log repository (local git, then the git host after P22) under the step id; scans and signed documents go to the interim evidence location (`Assumption:` a restricted shared drive owned by `platform-security@` with the second human as manager), same day; both are copied to the platform evidence bucket after setup 14 and custody records to the witness at W-2; every step's EVIDENCE line names the 10 §5 E-xx id, the 11 §13 control id and the path | Decisions, drill and custody records and the stage snapshot had no defined location or mapping to E-xx or TISAX ids, and the evidence and witness buckets do not exist during most of the build (S067) | as stated | platform owner; IT security; ISMS | setup 06 first evidence | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-38, record slug `setup-conventions-and-bootstrap-deviation`; [setup 06 OB-1.1](setup/06-organisation-bootstrap-and-roster.md#ob-11-preflight-variables-decisions-people-and-the-shell); [setup 14 CL-10.1](setup/14-central-logging-and-billing-export.md#cl-101-set-the-signed-retention-values-and-read-everything-back-before-locking) | S067; [10](10-eu-ai-act.md) §5; [11](11-tisax.md) §13; X-ORG-11 |
| P182 | proposed by the setup procedures, pending the owner's signature | The toil baseline starts on day one (setup 02, the first step of the set); the CSV is committed to the platform repository under `metrics/`, never the wiki; setup 22 loads it with an explicit `bq load` on merge, no expiry, with a copy in the archive dataset | A scheduled query cannot read a CSV in git and partition expiry would delete the baseline (S150); artefacts under the wiki are pushed to Google Drive (S064); the ordered list omitted starting the baseline (S084) | as stated | platform owner; Mo owner; IT security | setup 22 baseline load | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-39, record slug `mo-decisions`; [setup 22 MO-8.2](setup/22-mo-foundations.md#mo-82-load-the-merged-csv-an-archive-copy-named-by-commit-then-the-keyed-table) | S064, S084, S150; Mo-0; PREREQUISITES D12 |
| P183 | proposed by the setup procedures, pending the owner's signature | The platform sinks are built in setup 14 before any agent; `to-triggers-walle` is made in `LOGGING_PROJECT` by the module equivalent in setup 31; no agent procedure creates an organisation sink; interim sinks are never created; setup 39 verifies none exist | `walle deploy` added a second organisation sink once `to-triggers-walle` existed, doubling every trigger (S094); retired sinks and rows were still rebuilt (S157) | as stated | platform owner; IT security; ISMS | setup 14 sinks | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-40, record slug `setup-conventions-and-bootstrap-deviation`; [setup 14 CL-6.2](setup/14-central-logging-and-billing-export.md#cl-62-create-s-org); [setup 14 CL-6.6](setup/14-central-logging-and-billing-export.md#cl-66-take-the-sink-census-no-interim-or-agent-organisation-sink-s094-s157) | S094, S157; [08](08-data-logging-retention-sovereignty.md) §3.2; [../project-topology.md](../project-topology.md) §7.1 Phase 11 |
| P184 | proposed by the setup procedures, pending the owner's signature | The procedures state Google's rule: a project floor overrides a conflicting folder floor. Every project that calls `generateContent` gets a full project floor (filters, enforcement, `VERTEX_AI` integration, Cloud Logging) and the Agent Platform service agent gets `modelarmor.user`; agent procedures only read folder floors and fail when either floor is looser than the tier standard; folder floor writes happen only in setup 18 under `ent-folder-admin` | The old SETUP 12c said a stricter project floor wins, but Google says a project floor overrides a conflicting folder floor (S073); `walle armor` wrote the platform-owned folder floor (S096); screening `generateContent` needs a project-level floor with `VERTEX_AI` and `modelarmor.user` for the service agent (X-RQB-08) | as stated | platform owner; IT security; ISMS | setup 18 the floor | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-41, record slug `setup-conventions-and-bootstrap-deviation`; [setup 18 KS-2.5](setup/18-model-armor-floor-spikes-and-kill-switch.md#ks-25-write-the-platform-floor-on-fld-agentic-platform); [setup 18 KS-2.1](setup/18-model-armor-floor-spikes-and-kill-switch.md#ks-21-read-every-floor-before-writing-any) | S073, S096, X-RQB-08; corrects [06](06-gateways-model-armor-perimeter.md) §3.2 |
| P185 | proposed by the setup procedures, pending the owner's signature | `fld-agents-p-sa-nonprod` gets a dated no-approval variant of `ent-factory-singleton` with mandatory justification, so the twin can be built early; `fld-agents-p-sa-prod` waits for two named approvers (the security reviewer and the second human), and setup 31 is ordered after their appointment; `fld-controllers-*` is approved by the second human | `ent-factory-singleton` needed two approvers, one a security reviewer who does not exist before Tier W, while the fallback was allowed only while the factory does not exist (S011) | as stated | platform owner; IT security; ISMS | setup 12 `ent-factory-singleton-*` | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-42, record slug `platform-model-and-privilege`; [setup 12 PA-4.5](setup/12-privileged-access-catalogue.md#pa-45-create-and-test-ent-factory-singleton-psa-nonprod-dated-no-approval); [setup 12 PA-4.7](setup/12-privileged-access-catalogue.md#pa-47-create-and-test-ent-factory-singleton-psa-prod-two-named-approvers) | S011; refines [04](04-identity-and-privileged-access.md) §5.2; P142 |
| P186 | proposed by the setup procedures, pending the owner's signature | BigQuery has no insert-only permission (`bigquery.tables.updateData`, which streaming and load writes need, also permits DML `DELETE` and `UPDATE`), so Eve's writer identities get custom roles with `updateData` and without `tables.delete`, `tables.update`, `tables.setIamPolicy` or `datasets.update`, and row tampering is made detectable rather than impossible: a severity-1 rule on BigQuery `DATA_WRITE` audit entries for DML against Eve's tables (SI-07) and cumulative per-table row counts in the hourly witness heartbeat; the accepted limit is signed and the security reviewer ratifies it before S2 | WRITER mapped to `dataEditor`, which can delete and rewrite the mirror, findings and `grades_blind`, and tables expire at 400 days with no locked export until late in the old runbook (S138) | as stated; verified against the BigQuery access-control page on 2026-09-15 | platform owner; IT security; ISMS; second human; security reviewer ratifies | setup 23 writer roles; security reviewer ratifies before S2 | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-43, record slug `platform-model-and-privilege`; [setup 23 EP-6.1](setup/23-eve-project-and-evidence-stores.md#ep-61-create-the-two-writer-custom-roles); [setup 25 EH-1.2](setup/25-eve-human-super-admin-detections.md#eh-12-bind-the-sd-43-writer-roles-by-condition-on-the-three-tables-eve-writes); [setup 26 ER-4.10](setup/26-eve-reporting-and-witness-export.md#er-410-the-accepted-limit-signed) | S138; refines [../eve/03-lld.md](../eve/03-lld.md) §9 and [08](08-data-logging-retention-sovereignty.md) §5.4 |
| P187 | proposed by the setup procedures, pending the owner's signature | Every cross-project grant is wrapped in `exists_or_pending`, which prints PENDING and appends the grant to the re-run index; only S0 principals are granted at setup 31 and S3 principals in setup 36; every command passes `--project` and the shell refuses a gcloud default project | Run before Eve and Mo, each foreign grant was refused and the bash had no PENDING handling; run after Eve, it granted S3 identities READER at S0 (S070, S107); the gcloud project default silently redirects resources (S071) | as stated | platform owner; IT security; ISMS | setup 10 first grant | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-44, record slug `setup-conventions-and-bootstrap-deviation`; [setup 10 CP-0.1](setup/10-core-projects-and-ci-identities.md#cp-01-open-the-sitting-and-check-the-gates); [setup 31 WD-1.1](setup/31-wall-e-project-and-data-plane.md#wd-11-two-schema-amendments-the-p-sa-row-needs) | S070, S071, S107; [../project-topology.md](../project-topology.md) §7 |
| P188 | proposed by the setup procedures, pending the owner's signature | The procedures follow the owner's order — the platform, then Mo and Eve, then Wall-E — and record each move against the review's master order: organisation bootstrap before billing; the paging service and the subject-report escalation before Eve; the sandbox tenant before Eve; Mo's foundations and Eve-H run in parallel after the platform, Eve-H never waiting on Mo; Mo's Eve pack follows both; Wall-E starts only on `EVE_H_LIVE_RECORD` | The review's master order built Wall-E's data plane before Eve and Mo and Eve's observe-and-report sitting after Wall-E's services; the owner asked for the platform first, then Mo and Eve, then Wall-E | as stated | platform owner; IT security; ISMS | setup 06 first step; the README's order | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-45, record slug `setup-conventions-and-bootstrap-deviation`; [setup 06 OB-1.1](setup/06-organisation-bootstrap-and-roster.md#ob-11-preflight-variables-decisions-people-and-the-shell); [setup README §3](setup/README.md#3-the-order) | Owner's request of 2026-09-15; [13-setup-procedure-review.md](13-setup-procedure-review.md) §2; [11](11-tisax.md) §6.3 |
| P189 | proposed by the setup procedures, pending the owner's signature | `ent-bootstrap-module`, one entitlement per non-singleton folder that a hand module equivalent needs before the factory exists (`fld-agents-r-nonprod`, `fld-improvers-prod`, `fld-improvers-nonprod`; others when a register row needs them), with the `ent-factory-singleton` role bundle, 1 hour, requester `platform-owners@`, approver the second human, justification naming the register row; each use is in the deviation register and the entitlements are deleted when the factory's first empty plan supersedes the runs, at the latest at the Tier W gate | No entitlement let a human build a project in a non-singleton folder before the factory exists, yet `canary-r` (setup 18) and `MO_PROJECT` (setup 22) must be made by hand after the organisation exception that includes Project Creator is withdrawn in setup 12 | as stated | platform owner; IT security; ISMS; second human | setup 12 entitlements; setup 18; setup 22 | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-46, record slug `platform-model-and-privilege`; [setup 12 PA-4.8](setup/12-privileged-access-catalogue.md#pa-48-create-and-test-the-three-ent-bootstrap-module--entitlements); [setup 22 MO-2.2](setup/22-mo-foundations.md#mo-22-run-fm-improver-for-mo-prod) | [04](04-identity-and-privileged-access.md) §5.2 `ent-factory-singleton`; P144 (SD-01); S002, S042, S059 |
| P190 | proposed by the setup procedures, pending the owner's signature | Two explicit HSM keys in `EVE_PROJECT`, neither from Autokey: `eve-evidence` in ring `eve` (`europe-west1`) for the locked bucket and `eve-evidence-eu` in ring `eve-eu` (location `europe`) for the `eve.*` datasets; key rings are permanent, so both ring creates are IRREVERSIBLE steps gated on the signed key table; 09 §2.4 and 08's store inventory corrected | 09 §2.4 gave one key `eve-evidence` in `europe-west1` for both the bucket and the `eve.*` datasets, which sit in the EU multi-region; BigQuery requires an EU dataset to use a key from a `europe` multi-region key ring (BigQuery CMEK page, read 2026-09-15) | as stated | platform owner; second human; IT security | setup 23 ring creates (IRREVERSIBLE) | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-47, record slug `key-table`; [setup 23 EP-3.3](setup/23-eve-project-and-evidence-stores.md#ep-33-create-key-ring-eve-in-europe-west1); [setup 23 EP-3.5](setup/23-eve-project-and-evidence-stores.md#ep-35-create-key-ring-eve-eu-in-europe) | corrects [09](09-supply-chain-secrets-recovery.md) §2.4 row `eve-evidence` and [08](08-data-logging-retention-sovereignty.md) S4, S5; P166 (SD-23) |
| P191 | proposed by the setup procedures, pending the owner's signature | The robot never approves: no Multi-party approval admin role is delegated to `walle@`; `walle@` never signs in to the Admin console (setup 39 verifies); Eve raises severity 1 on any approval action whose actor is `walle@` or any service identity (SI-09, live before setup 38); if Google exposes an approval API it joins `HARD_DENIED` and the denial suite; `Assumption:` approvals are made in the Admin console only, checked on the day in setup 38 | Once `walle@` holds Super Admin it is an eligible approver of multi-party approval requests, so the robot could complete a second-person check the design reserves to humans | as stated | platform owner; security reviewer or IT security; ISMS; second human | setup 25 catalogue; setup 38 | [setup 03 §11.1](setup/03-decisions-and-people.md#111-setup-decisions-sd-01-to-sd-48) SD-48, record slug `wall-e-scope`; [setup 25 EH-2.3](setup/25-eve-human-super-admin-detections.md#eh-23-the-detection-catalogue-sa-01sa-10-with-actor--any-roster-human-and-si-01si-13); [setup 38 GT-5.6](setup/38-super-admin-gate-and-grant.md#gt-56-record-that-no-approval-role-is-delegated-to-a-machine-sd-48) | Owner's constraint (the model holds no credential and cannot approve); refines SETUP G4 and [04](04-identity-and-privileged-access.md) §8.4 |

---

## 7. Values that differ between pages

Each row is one element for which two pages state different values. Each is recorded here
rather than silently resolved, because the resolution is a decision with an owner. Rows 1–9 are resolved and applied on the pages named; the rows stay so the
resolution is not lost. Values aligned across pages outside this table: page 04's decision ids
P65/P67/P69 are this register's; K7 lever KF-1 is a policy replacement (P44) on the HLD and pages
04, 07 and 09; the `ent-ge-admin` entitlement is the HLD's 1 h with security-reviewer approval;
the drift-job absence window is page 04's 26 h, page 03 included; page 09's K2/K4 names follow
page 04's K-table; page 04's SIEM K7 triggers are page 07's auto-K7 subset; `aiplatform` is not on
page 02's Gemini folder allow-list (page 03 §3); the page the HLD called `ai-act.md` is
[10](10-eu-ai-act.md). Rows 10–14 are open: values pages 02, 04, 07, 08 and 10 set that the named
pages did not yet carry on 2026-09-14; they stay open until the named page carries them, most of
them through the propagation stage (HLD §18, P143).

| # | Pages | What differs | Resolution proposed | Owner | Rows |
|---|---|---|---|---|---|
| 1 | [03](03-gemini-enterprise-environment.md) §5.4 vs [08](08-data-logging-retention-sovereignty.md) §5.2 R8 | Gemini Enterprise conversation history: 90 days vs 30 days, both `Assumption:` | The DPO decides under P13 before Stage 1; until then 30 (the content ceiling) is the one interim number runbook step GE-6 of page 03 sets, 60 stands where the edition hides the setting | DPO | P13, P52, P113 |
| 2 | [09](09-supply-chain-secrets-recovery.md) §2.4 vs [08](08-data-logging-retention-sovereignty.md) §9 | Cloud Logging buckets: Google-managed keys (exception 2) vs CMEK with an explicit HSM key on the two central buckets | Page 08's position stands for `platform-evidence-logs` and `platform-identity-logs` (per-bucket CMEK at creation is documented and cannot be added later); page 09's exception 2 narrows to "Cloud Logging buckets other than the two central ones"; page 09's key table gains the two rows | platform owner | P112, P119; [11](11-tisax.md) R-08 wording |
| 3 | HLD §3.4 vs [05](05-registry-and-autonomy-contract.md) §3 | "Labels on every registry card" — the registry `Service` resource has no labels | The description first line of P72 replaces the label claim on HLD §3.4 | platform owner | P72 |
| 4 | HLD §7.1 vs [08](08-data-logging-retention-sovereignty.md) §3 | One organisation-level aggregated sink vs two (organisation without children; folder with children, intercepting) | Page 08's topology, on HLD §7.1 as well; [02](02-landing-zone-and-tiers.md) §3 topology rows 22–23 retired | platform owner | P104 |
| 5 | HLD §9 vs [09](09-supply-chain-secrets-recovery.md) §1 | Binary Authorization constraint on "W and P folders" vs also `fld-controllers` and `fld-platform-core` | Page 09's extension (the authority and kill paths are Cloud Run) | platform owner | P115 |
| 6 | HLD §3.1 vs [09](09-supply-chain-secrets-recovery.md) §2 and [08](08-data-logging-retention-sovereignty.md) §8 | Four core projects vs five (`KMS_PROJECT` added) | Five; [02](02-landing-zone-and-tiers.md) §2's folder table and §4.2's allow-list gain `KMS_PROJECT` | platform owner | P118, P109 |
| 7 | [11](11-tisax.md) §3 vs [05](05-registry-and-autonomy-contract.md) §3 | `tisax_dp_scope` is a register field page 11 adds and page 05's schema does not yet carry | Page 05 adds the field, optional below the first processor-role agent | platform owner | P134 |
| 8 | HLD §17 P4 vs [09](09-supply-chain-secrets-recovery.md) §4 | The `identityType` custom constraint on `ReasoningEngine` as a spike vs `ReasoningEngine` absent from the custom-constraint resource-type list on 2026-09-13 | P4's remaining half stays a spike re-run at each factory release; until it passes, D2's engine half is a CI check (detection) and the HLD's grade line says so | platform owner | P4, P42, P121 |
| 9 | [07](07-monitoring-detection-incident-response.md) §4 vs [05](05-registry-and-autonomy-contract.md) §5 | Silence budgets stated on both pages (24 h / 4 h / 15 min) | One value, in page 07 §4 (the continuous invariant: desk, budget, finding and action on breach per tier), that page 05 §5 cites | platform owner | P74, P95 |
| 10 | [02](02-landing-zone-and-tiers.md) §4.1, §2, §3.6 vs `wall-e/13` §2.2 and §10(c)-1, `wall-e/SETUP.md` Phase 13b, `wall-e/setup/README.md`, `wall-e/PREREQUISITES.md` §4.2, `wall-e/12`, Eve's and Mo's staging pages, [../gcp-projects.md](../gcp-projects.md) | Open on 2026-09-14: the Wall-E set still writes `iam.managed.disableAccessPolicyBindings` (plural; Google's set-up page uses the plural, the constraints reference the singular — [03](03-gemini-enterprise-environment.md) §1, [06](06-gateways-model-armor-perimeter.md) §2.2) and the legacy `iam.disableServiceAccountKeyUpload` (both key-upload names exist); Eve's and Mo's staging pages do not name `fld-controllers-nonprod` / `fld-improvers-nonprod`; the inventory does not show the `agp-<tiercode>-<agent>-<env>` id pattern | Page 02 stands: the reference spelling `iam.managed.disableAccessPolicyBinding`, confirmed at the first factory run (06 §8 rows G3, O1); the managed `iam.managed.disableServiceAccountKeyUpload` as the fleet spelling; the two nonprod folders; `agp-` ids with the existing handles kept as variables. Agent-set edits in the propagation stage (HLD §18) | platform owner; agent owners (P143) | P37, P40, P41 |
| 11 | [04](04-identity-and-privileged-access.md) §3, §8 vs Eve's pages and `wall-e/12` §1.4 | Open on 2026-09-14: Eve's pages do not carry the severity-1 rows the HLD §7.3 table has (break-glass sign-in, control-group change without a merge, multi-party approval setting change, a fourth super admin, any K7 lever present outside an incident, deny-policy or PAB edit outside the pipeline); `wall-e/12` §1.4 gives only the IAM spelling of the per-project agent principal set (`attribute.platformContainer/aiplatform/projects/N`), not the PAB spelling (`attribute.container/projects/N`), while Google's deny example uses a third | Page 04 stands: the six severity-1 rows join Eve's rules; the factory emits the first two spellings until P8's spike | Eve owner; agent owner (Wall-E) (P143) | P8, P61, P66, P69 |
| 12 | [07](07-monitoring-detection-incident-response.md) §10 vs `wall-e/05-autonomy-ladder.md` and `mo/04-artefacts-and-proposals.md` | Open on 2026-09-14: both still place the incident note under `platform/wall-e/incidents/`, a path that does not exist | The incident record lives in the SIEM's case system; "write an incident note" becomes "open or update the case" | agent owner (Wall-E); Mo owner (P143) | P100 |
| 13 | [08](08-data-logging-retention-sovereignty.md) §5–§7 vs [06](06-gateways-model-armor-perimeter.md) §3.3, `wall-e/11` §4 and §6, `wall-e/ARCHITECTURE.md` §9, `eve/07-build-runbook.md` Phase 9, `mo/02` | Open on 2026-09-14: page 06 does not name the de-identify templates `platform-log-deid-v1` / `platform-log-inspect-v1` (`CORE_PROJECT`, `europe-west1`) the tier templates reference; `wall-e/11` still carries the advanced SDP de-identify as *tbd* and names neither `content_raw` nor `content_deid`; `wall-e/ARCHITECTURE.md` §9 still lists "two known residency exceptions", one of them "Agent Runtime Code Execution has no EU at-rest residency", which P27 and P122 record as EU-resident; Eve's Phase 9 locks the evidence bucket at creation instead of at P106's trigger; `mo/02` does not name `content_deid` as the only content store graders read | Page 08 stands: P108's templates and `content_raw` → `content_deid`; ARCHITECTURE §9 takes DL-7.2's six rows and DL-7.3's CI rule; Eve's lock moves to the day the DPO's ceiling is recorded, with the interim `UpdateBucket` alert; graders read `content_deid`, never `content_raw` | platform owner; agent owner (Wall-E); Eve owner; Mo owner (P143) | P106, P108, P110, P113, P122 |
| 14 | [10](10-eu-ai-act.md) §3.1.3, §4.6, §4.7, §4.10 vs `wall-e/05-autonomy-ladder.md` §5, `wall-e/03-lld.md`, `eve/07-build-runbook.md`, [08](08-data-logging-retention-sovereignty.md) §5.2 | Open on 2026-09-14: the Wall-E ladder still reads F5 "L4, and not before Stage 5" with no T2-HR-event limit and no F7 split; `wall-e/03` has no F2b wrapper (disclosure line, signature block, header), no `disclosure_missing` refusal and no `explanation_requests` export; Eve's runbook does not name the "no model client" CI check as the Art. 3(1) invariant; page 08's retention schedule has no `explanation_requests` store (400 days, personal data) | Page 10 stands: F5 L4 only on the T2 HR event, T0/T1 at most L3, `F7-suspended` / `F7-inactive`, holds not expiring outside business hours; the Art. 50 wrapper in the action service; the Art. 3(1) invariant named; page 08 adds the store | agent owner (Wall-E); Eve owner; platform owner (P143) | P126, P127, P128, P129 |

---

## 8. Index: agent-set decisions and the platform rows that touch them

The propagation stage (HLD §18) adds a dated pointer line to each of these in the agent set.
A decision not listed is untouched by the platform.

**Wall-E 1–52** ([../wall-e/09-open-decisions.md](../wall-e/09-open-decisions.md); 42–52 in
[../project-topology.md §8](../project-topology.md#8-open-decisions-this-topology-adds))

| Wall-E | Platform rows | Relation |
|---|---|---|
| 2 (names) | P37, P65 | superseded for new resources; existing handles kept as variables |
| 3 (OAuth scope list) | HLD §13.1 item 3 | unchanged; two clients (HLD §18 item 1) |
| 4 (must-never list) | P29, P33, P66, P125, P136 | re-ratified with the two lists; "MPA off" added |
| 5 (OUs, sandbox OU) | P40, P55 | sandbox is a tenant, never an OU |
| 6 (model pin) | P113, P124 | a fleet allow-list of EU-resident models; `capability_eval_ref` |
| 7 (retention after licence removal) | P13, P18, P126 | the DPO's ceiling; F7's profiling boundary |
| 8 (DPIA, employee representatives) | P101, P129, P141; HLD §7.5 | moved to before Stage 1 |
| 9 (Cloud Run ingress) | P3, P90 | superseded by the folder rule |
| 10 (Eve's own credential) | P17 | unchanged; the witness export is the open question |
| 11 (operators, second approver) | P25, P68, P99, P137 | counted minimums per stage |
| 12 (playbooks) | P126, P127 | F7 split; F5 caps |
| 13 (HR system of record) | P127 | F5 L4 only on the T2 HR event |
| 14 (approval surface) | P46, P63; HLD §13.1 item 4, §13.2; RB-09 of [07](07-monitoring-detection-incident-response.md) §11 | per agent, behind IAP with access levels; the IAP surface is the band-B and reporting surface; decision 14's signed assertion is exercised in runbook RB-09 |
| 15 (business hours) | P99, P127 | unchanged values; holds never expire outside business hours |
| 16 (budgets and caps) | P31, P39, P77 | fleet defaults; project budgets |
| 17 (audit retention) | P13, P106 | 400-day floor; the lock rule |
| 18 (split control plane) | HLD §13.1 item 3; P62 | two services, two clients |
| 19 (Agent Identity) | P4, P5 | mandatory (D2); Cloud Run half pending GA |
| 20 (operator authentication) | P24, P51, P63 | IAP on Cloud Run with access levels |
| 21 (gateway or perimeter) | P3, P81, P89 | gateway now; perimeter by spike |
| 22 (`walle-actions` as MCP server) | P88, P138 | option A per agent at Stage 3; never at Tier P |
| 23 (A2A) | P78 | the fleet peer rule |
| 24 (Model Armor threshold) | P85 | a measured flip rule |
| 25 (telemetry content) | P108; [08](08-data-logging-retention-sovereignty.md) §5.1 | de-identified copy; restricted fields; the data-protection question feeds §5.1's records-of-processing inputs |
| 26 (keyless SA admin principal) | P33 | closed by fact |
| 27 (catalogue bands) | P1, P28, P125; HLD §13.1 (bands) | the purpose |
| 28 (requester scope) | HLD §13.1 item 4 | unchanged |
| 29 (where a gate change first executes) | P40, P59, P115; HLD §3.1 nonprod row | sandbox tenant before Stage 1 (Super Admin cannot be limited to an OU); promotion by attestation |
| 30 (restore protocol) | P120; HLD §10 R-B (accepted) | a mechanism replaces the runbook step |
| 31 (evidence durability) | P14, P106, P107, P112 | closed by the export and the witness |
| 33 (promotion statistics) | P77 | the Wilson gate is the fleet default |
| 34 (Eve deterministic) | P34, P79 | reopened for the reporting path only |
| 35 (per-person reporting recipients) | P98 | reports about a human go to the verifier owner only |
| 36 (Stage 0 provisioning) | P118 | keys in `KMS_PROJECT` and `EVE_PROJECT` |
| 37 (second pair of hands) | P1, P22, P96, P137, P139; HLD §0.3 | the security reviewer role, counted |
| 38 (value baseline) | P103 | SOC metrics alongside Mo's packs |
| 39 (robot lifecycle) | P7, P16, P68, P69, P102 | roster, custody, rebuild runbooks |
| 40 (contract drift probe) | P97, P116 | pipeline heartbeats; attested images |
| 41 (single region) | P83, P140; HLD §10 (accepted) | accepted; on the risk register |
| 42 (`walleEngineQuery`) | P56 | generalised as `geEngineQuery` |
| 43 (registry viewer) | P71 | extended: humans may hold it, agent principals may not (`eve-owners@` holds viewer on the shared registry, `eve-controller@` does not); `wall-e/13` §2.4's *tbd* "whether Wall-E's registry lists Eve and Mo" closes — Eve's control path and Mo's jobs are not registered (they are not agents a peer may reach), `eve-advisor` is registered with a card that names no skill ([05](05-registry-and-autonomy-contract.md) §1); topology §2/§4 lose `WALLE_PROJECT`'s registry (HLD §18 item 25) |
| 45 (`walle-events` subscriptions) | P45 | in the consumer's project |
| 46 (Eve-side properties) | P73; HLD §4.7 | closed by the drift job |
| 47 (actor-exclusion check) | P105, P107 | reads the authorised view |
| 48 (carve-outs) | P45, P142; HLD D7 | three carve-outs per verified agent; the named folder-level exceptions, plus the time-boxed `factory-apply@` exception under `ent-factory-singleton` (P142, 2026-09-13) |
| 50 (CI ingestion identity) | P45 | the CI reader |
| 51 (evidence copy project) | P107 | closed |
| 52 (ownership, budgets, Gemini project) | P39, P48, P57, P65, P109; HLD §2.1 | owner groups per project; the app project imported |
| topology §5 (constraints and floor, not a numbered decision) | P41, P84, P104, P118 | B7 lifted at the agent and controller folders and on `GEMINI_PROJECT`; managed key-upload spelling; Wall-E's hand-set project floor replaced by the generated project floor; three organisation sinks become P104's two plus Eve's; `KMS_PROJECT` and `EVE_ADVISOR_PROJECT` added — each scheduled in HLD §18 item 25 with owner and gate |

**Eve E-1..E-20** ([../eve/09-open-decisions.md](../eve/09-open-decisions.md))

| Eve | Platform rows | Relation |
|---|---|---|
| E-1 (own project) | P15 | settled; the witness relocation is the end state |
| E-2 (owner) | P1, P14, P98, P137 | outside the Wall-E line; the verifier-owner rule generalised |
| E-3 (signed field list) | P118 | key placement only |
| E-5 (Eve-down timer, L5 demotion) | P70, P74, P97 | generalised to every agent's feed; `log_pipeline_silent` |
| E-6 (observe mode) | P79 | the platform verifier's compensations |
| E-7 (halting bias) | P19, P34 | moot for the control path; the reporting path may reason |
| E-9 (BigQuery grant) | P17, P30 | validator custodian's reader; witness export |
| E-10 (availability measurement) | P83, P95, P97 | probe engines; heartbeats |
| E-11 (outcome code) | P100 | a field of the incident record |
| E-13 (second grader) | P25, P79, P98, P137 | named, counted |
| E-14 (retention) | P13, P52, P93, P100, P106, P112 | the DPO's ceiling; the lock trigger in Phase 9 |
| E-15 (seeded faults, K6 drills) | P40, P67, P79, P120 | nonprod and the sandbox tenant |
| E-16 (credential chapter contingent on 26) | P33, P118, P136 | 26 closed by fact |
| E-18 (thresholds) | P95, P96, P123 | SA-03/SA-04 fill conditions 5–6; budget stays *tbd* at S2 |
| E-19 (gateway egress for Eve) | P3, P81, P89 | closed by P3 |
| E-20 (`walle-events` schema) | P76 | `contract_version` semver |

**Mo M-1..M-11** ([../mo/08-open-decisions.md](../mo/08-open-decisions.md))

| Mo | Platform rows | Relation |
|---|---|---|
| M-2 (fingerprint scoping) | P76 | the N/N−1 window |
| M-3 (who reads `walle_metrics`) | P109 | `walle_metrics_private` is class `secret` |
| M-4 (second grader) | P25, P127, P137 | counted per Tier W agent |
| M-5 (retention floor, evidence copy) | P13, P103, P106, P107 | closed by the export |
| M-7 (git host) | P22, P36 | platform-owned |
| M-8 (narrator model) | P26, P103, P123, P128 | agent-authored label; re-qualification |
| M-10 (`unsure` cap, calendar) | P77 | fleet defaults |
| M-11 (cross-project reads) | P30, P91, P116 | verified per row in the topology |

**Gap-register trace.** Which section answers each blocking or major gap of the objective
review, with the owner and gate of what remains, is traced once in
[00-objective-review.md §4.1](00-objective-review.md#41-where-the-blocking-and-major-gaps-are-answered).

---

## 9. What this page does not do

- It does not decide anything itself. Every row's content is on the page in "Recorded in",
  with that page's sources; this page carries the id, the gate and the cross-reference.
- It does not hold the agent sets' decisions. Wall-E's 1–52, Eve's E-1..E-20 and Mo's
  M-1..M-11 stay where they are; §8 is an index into them.
- It does not replace the dated decision records (`decisions/2026-09-13-*.md`) for P33 and
  P34; those files are the signatures, this page is the register.
- It does not track build status. Nothing is built; when something is, the register of record
  ([05](05-registry-and-autonomy-contract.md)) and the ladder-state page carry state, not this
  page.

---

## Related

- [01-hld.md](01-hld.md) — the parent; §17 is P1–P34 and points here for P35–P143
- [00-objective-review.md](00-objective-review.md) — §5, the 82-item brief the pages answered
- [02-landing-zone-and-tiers.md](02-landing-zone-and-tiers.md) P35–P47; [03-gemini-enterprise-environment.md](03-gemini-enterprise-environment.md) P48–P59; [04-identity-and-privileged-access.md](04-identity-and-privileged-access.md) P60–P70; [05-registry-and-autonomy-contract.md](05-registry-and-autonomy-contract.md) P71–P80; [06-gateways-model-armor-perimeter.md](06-gateways-model-armor-perimeter.md) P81–P91; [07-monitoring-detection-incident-response.md](07-monitoring-detection-incident-response.md) P92–P103; [08-data-logging-retention-sovereignty.md](08-data-logging-retention-sovereignty.md) P104–P114; [09-supply-chain-secrets-recovery.md](09-supply-chain-secrets-recovery.md) P115–P124; [10-eu-ai-act.md](10-eu-ai-act.md) P125–P132; [11-tisax.md](11-tisax.md) P133–P141
- [../wall-e/09-open-decisions.md](../wall-e/09-open-decisions.md), [../project-topology.md](../project-topology.md) §8, [../eve/09-open-decisions.md](../eve/09-open-decisions.md), [../mo/08-open-decisions.md](../mo/08-open-decisions.md) — the agent registers §8 indexes
- [setup/README.md](setup/README.md) and [setup/03-decisions-and-people.md](setup/03-decisions-and-people.md) — the setup procedures and the signing checklist for P144–P191
- [README.md](README.md) — the set's index and reading orders
