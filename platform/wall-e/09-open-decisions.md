# 9. Decisions to make before build

## Status
- Owner: the platform owner
- Last reviewed: 2026-09-14
- Decisions 1–41 are here; 42–52, opened by the four-project topology of 2026-09-13, are
  recorded in [../project-topology.md](../project-topology.md) §8 and listed below. A row that
  says "the project" names which of `GEMINI_PROJECT`, `WALLE_PROJECT`, `EVE_PROJECT` and
  `MO_PROJECT` it means.
- **Objective restated 2026-09-13; see the platform HLD.** Wall-E holds Super Admin on a
  dedicated, licensed user account (P33). Decisions 3, 4, 5, 8, 9, 10, 11, 14, 18, 19, 21, 26,
  27, 28, 29, 30, 31, 34, 37, 39, 41, 46 and 52 are changed, closed or accepted by
  [../agentic-platform/01-hld.md](../agentic-platform/01-hld.md) §17; each row carries a dated
  line naming the deciding section, earlier text stays in the row as dated history, and
  [§"Decisions changed by the objective of 2026-09-13"](#decisions-changed-by-the-objective-of-2026-09-13)
  gives the was/now for each.
- **Platform decisions continue this register as P1, P2, …** — Wall-E's numbering ends at 52.
  The register of record is
  [../agentic-platform/12-open-decisions.md](../agentic-platform/12-open-decisions.md), whose §8
  indexes every Wall-E decision against the platform rows that touch it. A new decision about
  Wall-E that is really a platform choice is opened there as a P row, not here as 53.

Answer these. The reasoning stays on this page and each answer is recorded as a dated,
append-only file in [`../../decisions/`](../../decisions/)
([../agentic-platform/12-open-decisions.md §1](../agentic-platform/12-open-decisions.md#1-how-this-register-works)).
Since 2026-09-13 that directory holds two records,
[P33's](../../decisions/2026-09-13-wall-e-holds-super-admin.md) (Wall-E holds Super Admin,
accepted, its TISAX deviation not yet signed) and
[P34's](../../decisions/2026-09-13-eve-reporting-path-may-reason.md) (Eve's reporting path may
reason, proposed); none of the numbered decisions below has its own file yet, so Wall-E starts
by closing them.

## Decisions changed by the objective of 2026-09-13

The was/now of every decision whose answer the Super Admin objective moved. The row itself, with
its dated line, is the record; "§" references are to the
[platform HLD](../agentic-platform/01-hld.md), and P rows are in
[../agentic-platform/12-open-decisions.md](../agentic-platform/12-open-decisions.md).

| Decision | Was | Now | Decided in |
|---|---|---|---|
| 3 (OAuth scope list) | one client, fourteen scopes, frozen at consent | two clients on the one account: the narrow list for `walle-actions`, a broad list for `walle-actions-super`; both consented in one sitting; `cloud-platform` in neither; adding a scope re-consents client 2 only. The broad list is itself a super-admin-signed decision | §13.1 item 3 |
| 4 (ratify the never-list) | nine absolute rows, unratified | re-opened as **P29**: the two lists of [01-hld.md](01-hld.md) §"The controls that replace role scoping" ([06](06-security-guardrails.md) "The two lists"), owner-signed before the grant; "Multi-party approval off" joins the hard-denied list (P66) | §13.1 item 2; P29, P33 |
| 5 (OUs and sandbox) | a sandbox OU with synthetic accounts plus a small pilot OU; Workspace enforces the OU scope | the pilot OU allow-list survives only as a hard invariant in the action service; the sandbox is a separate Workspace tenant, never an OU (decision 29) | §3.1 nonprod row; P40, P55 |
| 8 (DPIA, employee representatives) | reads now, autonomous writes before Stage 3 | widened: a super-admin robot is a different processing activity again; the record carries the EU AI Act classification and the TISAX scope; worker information and consultation before Stage 1; DPO engagement is a precondition of the grant | §7.5, §14.1, §14.2; P13, P129, P141 |
| 9 (Cloud Run ingress) | IAM as the boundary; PSC interface only if network policy demands | superseded by P3: IAM until P3's engine-reach spike passes, then the folder ingress policy behind an internal load balancer and PSC endpoint ([§"Verified since the first draft"](#verified-since-the-first-draft--these-are-now-closed), Cloud Run ingress row) | §8.1; P3 |
| 10 (Eve's own read-only credential) | yes, one more bootstrap | confirmed: `eve@` holds a read-only custom role and never Super Admin; its privileges and scopes widen to the six Workspace streams, with no content scopes | §13.2, §4.6; E-16 |
| 11 (operators, second approver) | one more Workspace admin in `walle-operators@`; an IT-security second approver | platform RACI roles with counted minimums (3 humans at Stage 1, 4 at the grant); a band-B `SUPER` approver is a human super admin other than the requester; decision 28's requester check before any second operator | §0.3, §13.1 item 4; P137 |
| 14 (approval surface) | Chat app or IAP page | the IAP surface, bound to the canonical request hash, is the band-B two-person surface; Eve's reporting contract is recorded against the same decision | §13.1 item 4, §13.2; P46, P63 |
| 18 (split the control plane) | "before Stage 4" | **now** — the scope split needs two readers: `walle-actions` and `walle-actions-super`, each with its own service account and secret | §13.1 item 3; P62 |
| 19 (Agent Identity) | adopt for the engine, contingent on the one-day spike | a platform rule for every reasoning layer; Agent Identity for Cloud Run is Preview: nonprod now, prod at GA, with dated service-account exceptions until then | §4.1; P4, P5 |
| 21 (gateway or perimeter) | one decision before Stage 1 | closed by P3: gateway now, VPC-SC per tier folder as backstop after the second spike | §8.1; P3 |
| 26 (keyless service account for the admin half) | blocking spike before Phase 9 | **closed for Wall-E by fact**; stays open for Eve as E-16 | "What this reverses", §4.6; P33 |
| 27 (catalogue breadth: bands B and C) | "Wall-E is a narrow operator, not a stand-in for a super admin" | re-cut as "which lane": band A the catalogue, band B the generic lane, band C the handoff | §13.1 "The three bands"; P28, P29, P125 |
| 28 (requester entitlement) | committed operator list; live check 5b deferred | band A unchanged; band B tier `SUPER` runs the live check, fail-closed, requester and approver both human super admins and different people | §13.1 item 4 |
| 29 (where a gate change first executes) | offline harness; a second robot scoped to the sandbox OU in `WALLE_PROJECT`; a separate tenant only if F3/F3b autonomy is wanted | **a sandbox Workspace tenant, before Stage 1**, because Super Admin cannot be limited to an organisational unit, so a second robot "scoped to the sandbox OU" of the production tenant contains nothing once the robot is a super admin; only a separate tenant does | §3.1 nonprod row, §9; P40, P59 |
| 30 (control-plane durability), 41 (single region) | recommendations, unrecorded | accepted as written (R-B row; single region for every tier, with the manual Admin console as Wall-E's continuity plan) | §10; P120, P83, P140 |
| 31 (evidence durability) | off-project copy at Stage 1, Eve's mirror the candidate | closed by the daily JSONL export to the locked evidence bucket plus the witness copy; Eve's mirror stays as the reconciliation source | §7.5, §13.2; P106, P107 |
| 34 (Eve is deterministic) | no LLM produces an approval | the approval half stands; the reporting half is reopened: `eve-advisor` in `EVE_ADVISOR_PROJECT` may reason, writes reports and pages only | §13.2; P34, P19 |
| 37 (second pair of hands during the build) | a security reviewer, a validator custodian and a second person on the Phase 9 re-bootstrap, before Phase 10 | platform RACI roles with separation rules; the Eve owner is a second human outside the Wall-E administration line, named before the grant; "the four owner groups are one person" expires on the grant date | §0.3, §0.4; P137, P139 |
| 39 (robot account lifecycle) | detect other administrators acting on the robot; `role_assignment_missing` pages | widened both ways: the robot acting on other administrators is hard-denied for their security settings and backup codes and pages; `role_assignment_added` also pages; three super admins in steady state; K6 drill; two hardware keys | §13.1 items 5–7, §4.6; P7, P16, P68, P102 |
| 46 (who asserts Eve-side properties) | reassign to Eve's own drift job | closed by the platform drift job with folder-level `roles/iam.securityReviewer`, recorded as a dated exception to the one rule and to decision 48; Eve's own drift job keeps its anti-grant assertions | §4.7; P73 |
| 52 (ownership and budgets) | ownership and budgets of the four projects ([../project-topology.md](../project-topology.md) §8) | re-cut: the app project is a platform project imported in place under `fld-gemini-enterprise`; the per-agent project, owner group and budget are a factory pattern, not a count of four | §2.1, §3; P39, P48, P57, P65, P109 |
| C35 in [14](14-hld-challenge.md) | generic Admin SDK proxy rejected | re-argued as band B, never above L3, so the lost inverse, pre-state predicate and taint declaration are not needed | §13.1 |
| New, platform | — | P7 (Context-Aware Access on the robot), P16 (a machine-invocable account stop — none now), P28 (Wall-E's intended purpose: the catalogue plus bands B/C as execution and instructions, signed with legal), P29 (the two lists), P3 (the perimeter, a precondition of the grant), **P33** — "Wall-E holds Super Admin" ([decision record](../../decisions/2026-09-13-wall-e-holds-super-admin.md)), superseding 26 for Wall-E, entering the residual in the accepted-risks table of [10](10-adversarial-review.md), signed as a TISAX deviation and as row one of the risk register — and **P34**, Eve's report-only reasoning path ([decision record](../../decisions/2026-09-13-eve-reporting-path-may-reason.md)), which overturns the "An LLM Eve — Rejected" row of [14](14-hld-challenge.md) §"Alternatives considered" for reporting only | [../agentic-platform/12-open-decisions.md](../agentic-platform/12-open-decisions.md) |

## Blocking — the build cannot start without these

| # | Decision | Why it blocks | Recommendation |
|---|---|---|---|
| **1** | **Does any other automation already write to the same Workspace objects?** An HR-driven directory sync, a licence-management script, a joiner/leaver tool. | Two writers on one object is the collision this design's pre-state re-read exists to catch, but it is far better to know up front. | **Inventory every existing writer before Stage 1** and give each a named owner. |
| **2** | **Names**: the four project ids (`GEMINI_PROJECT`, which exists already and holds the app; `WALLE_PROJECT`, `EVE_PROJECT`, `MO_PROJECT`), all under `FOLDER_ID` ([../project-topology.md](../project-topology.md) §2); robot account, OU paths, group addresses, BigQuery dataset, OAuth app name | The consent screen shows the app name to the robot and is awkward to change afterwards. A project id can never be reused after a delete. Every script hard-codes the rest. | Decide in one sitting. `walle-`, `eve-` and `mo-` prefixes; robot `walle@<primary domain>`. Ownership and the Gemini project's identity are decision 52. [SETUP.md](SETUP.md) §1.1 D1. |
| **3** | **The complete OAuth scope list** | Scopes freeze at consent. Adding one later means redoing the bootstrap and the Trusted-client step. | Take the list in [02](02-identity-and-auth.md) as the proposal (the same list is [SETUP.md](SETUP.md) §1.3, and §1.2 explains why it is close to irreversible; D2 there). The two live questions: include `admin.directory.user.security` (sign-out, token revocation) for leaver hygiene? And confirm you do **not** want `drive`. **2026-09-13 (platform HLD §13.1 item 3):** reopened by the objective. There are now two OAuth clients on the one account. The narrow client carries the catalogue's scopes and is read only by `walle-actions`; it keeps a Google-enforced ceiling for everything unattended. The broad client carries band B's scopes and is read only by `walle-actions-super`. Both are consented in one sitting on the same hardware key, with `cloud-platform` in neither (checked in CI). Adding a scope re-consents the broad client only. The two live questions above now apply per client; the broad list is itself a super-admin-signed decision. Gated by 27, as before. |
| **4** | **Ratify the "must never happen" list** | It is the ceiling every other control is calibrated against, and I wrote it without you. | Read [06](06-security-guardrails.md) §"blast-radius ceiling". Nine rows. Tell me which you disagree with, before build, not after ([SETUP.md](SETUP.md) §1.1 D4). **2026-09-13 (platform HLD §13.1 item 2; P29, P33):** re-opened. What gets signed is the **two lists**: (1) hard-denied in every lane, including the band-C handoff; (2) reachable only through band B at tier `SUPER`, under the two-person rule. Owner-signed before the super-admin grant. N1 and N2 are enforced as hard invariants in the policy chain ([06](06-security-guardrails.md) "The two lists"). "Multi-party approval off" is added to the hard-denied list (P66). |
| **5** | **Which OUs, and is a sandbox OU possible?** | Every write scope limit references an OU allowlist that is currently `tbd`, the OU allow-list in the policy chain (SETUP Phase 14) needs a unit to name, and Stage 0 shadow plans need somewhere safe to point. | A sandbox OU with synthetic accounts, plus one small real pilot OU. Without a sandbox, Stage 1's first real writes land on real employees, which I would not do. If the tenant genuinely cannot have a sandbox, that is a finding to record before Stage 1, not a detail ([SETUP.md](SETUP.md) §1.1 D3). **2026-09-13 (platform HLD §3.1 nonprod row; P40, P55):** OU scoping is no longer a control Workspace enforces, because Super Admin cannot be limited to an OU. The pilot OU allow-list survives only as a hard invariant in the action service. The sandbox is a **separate Workspace tenant**, never an OU (see 29; platform tier control PSA6 in [../agentic-platform/02-landing-zone-and-tiers.md](../agentic-platform/02-landing-zone-and-tiers.md) §1.3). The robot OU `/Automation/Service Identities` keeps the account-hygiene settings, which can be OU-scoped even though the role cannot (platform HLD §4.6). |

## Needed before Stage 1 (the first real write)

| # | Decision | Why it matters | Recommendation |
|---|---|---|---|
| **6** | **Which model, pinned by id** | Not every current model has EU data residency. The id goes on every audit row so Mo can attribute behaviour changes. | Pick from the per-model EU residency table at build. Do not assume the newest is eligible. |
| **7** | **Data retention after removing a licence** on your tenant's plan | Licence reclaim is the clearest early saving, and on an *active* account it may destroy data. | Ask your Google account team before any active account is in scope. Until answered, reclaim only from suspended accounts. |
| **8** | **DPIA, and employee representative bodies where your jurisdiction has them** | Autonomous admin action is a **different processing activity** from human-requested action. Longest lead time in the plan. Not a blocker for build Phase 1 ([SETUP.md](SETUP.md) §1.1 D7). | Ask in week one, in parallel with build. Frame it in two parts: reads now, autonomous writes before Stage 3. **2026-09-13 (platform HLD §7.5, §14.1, §14.2; P13, P129, P141):** widened. A super-admin robot is a different processing activity again, and the same record now carries the EU AI Act classification (the Art. 6(3) derogation claim for the catalogue, registered under Art. 49(2) before the first write) and the TISAX scope. Worker information and consultation move to **before Stage 1**, not Stage 3. The DPO sets the retention ceiling (the floor is Art. 26(6)'s six months or the organisation standard), and DPO engagement is a precondition of the super-admin grant. |
| **9** | **Cloud Run ingress and network isolation** | Internal-only ingress blocks Agent Runtime, which Cloud Run treats as external traffic ([closed question below](#verified-since-the-first-draft--these-are-now-closed)). | Deploy with IAM as the boundary and test. Add a PSC interface only if your network policy demands it. **2026-09-13 (platform HLD §8.1; P3):** superseded by P3. IAM-only invoke until P3's engine-reach spike passes; then the folder ingress policy, behind an internal load balancer and a PSC endpoint. The spike is a precondition of the super-admin grant, so by the grant `walle-actions` and `walle-actions-super` are never internet-reachable. |
| **10** | **Does Eve get its own read-only Workspace credential?** | Without one, Eve verifies through the thing it is verifying. | **Yes.** One more bootstrap, and it is what makes verification independent. Its OAuth client and refresh token live in `EVE_PROJECT`'s Secret Manager, never in `WALLE_PROJECT` ([../project-topology.md](../project-topology.md) §2). **2026-09-13 (platform HLD §13.2, §4.6; E-16):** confirmed by the objective ("She won't have super admin access"). `eve@` holds a read-only custom role and never Super Admin. Its read privilege and scope set widen to cover all six Workspace streams, the Reports API polled by actor, domain and customer settings, security and API-controls state where an API exists, and context-aware-access policies, with **no content scopes**. They are fixed before the one-sitting consent. |
| **11** | **Who else is an operator, and who is the second approver?** | `walle-operators@` is the group that can halt, demote, veto and approve; a one-person group means every kill switch depends on you being reachable. L4 and L5 promotions need two named humans — that is Stage 4, but the second person should be named now. A one-person rota also means approvals stall whenever you are away. | Name at least one more Workspace admin for `walle-operators@`, and someone from IT security as the second approver for high-risk promotions. **2026-09-13 (platform HLD §0.3, §13.1 item 4; P137):** the RACI makes these platform roles with counted minimums: 3 humans at Stage 1, 4 at the super-admin grant. A band-B `SUPER` approver must be a human super admin other than the requester. With a super-admin robot, the requester check of decision 28 must be in place before **any** second operator is added. |
| **12** | **Which playbooks first?** | The four in this design are my guesses. The ladder does not depend on them, but Stage 0's shadow evidence does. | Tell me the three admin tasks that waste the most of your time. My guesses: licence reclaim from suspended accounts, leaver group hygiene, stale-account reporting. Not blocking for Stage 0, but have an answer before SETUP Phase 14, because these are the playbooks Stage 0 should shadow ([SETUP.md](SETUP.md) §1.1). |

## Needed before autonomy (Stage 3 onward)

| # | Decision | Why | Recommendation |
|---|---|---|---|
| **13** | **Is there an HR system of record** reachable as an event source? | Autonomous suspension at Stage 5 is only acceptable when the trigger is authoritative, never free text. | If there is none, F5 suspend stops at L3 permanently. That is an acceptable outcome. |
| **14** | **Approval and notification surface** | Operators need somewhere to approve, veto and grade, and the surface must authenticate the human itself — the agent must never carry consent. **A Stage 1 blocker.** | Google Chat, **and it needs a Chat app**: under user authentication the Chat API can send text only, so the one-click veto and approval cards this design assumes require app authentication. Budget for that, or use an Identity-Aware-Proxy-fronted approval page instead. **2026-09-13 (platform HLD §13.1 item 4, §13.2; P46, P63):** the IAP-fronted surface is chosen for band B. Its approval is bound to the canonical request hash and records both humans for `SUPER`. Eve's reporting contract is recorded against this decision too: severity-to-channel table, `eve.incidents`, `eve.pages`, `oncall.yaml` secondary and timeouts, severity 1 paged from the witness organisation, and the second human as sole recipient of reports about the administrator. |
| **15** | **Business hours, timezone, freeze windows** | Hard-coded into every autonomous run's gate. | `Assumption:` Europe/Paris, Mon–Fri, last write 16:00. Correct me. Not blocking for Stage 0; have an opinion before SETUP Phase 14 ([SETUP.md](SETUP.md) §1.1). |
| **16** | **Budgets and caps** | The numbers in [05](05-autonomy-ladder.md) are defaults I chose. | Revisit at each stage decision rather than now. |
| **17** | **Audit retention** | 400 days is my default (`Assumption:` 400 days in [SETUP.md](SETUP.md) §1.1). | Align with your retention policy. Not blocking for Stage 0; have an opinion before SETUP Phase 14. |
| **18** | **Split the control plane onto its own service?** | Today every control — credential, policy, approval minting *and* verification, execution, audit — runs in one Cloud Run process, and `run.invoker` is granted per service rather than per path. An in-app allowlist works; a second service is stronger. | Do it before Stage 4. Until then, the per-endpoint allowlist plus Eve's asymmetric key is the minimum. **2026-09-13 (platform HLD §13.1 item 3; P62):** due **now**, not before Stage 4. The scope split needs two readers, so there are two Cloud Run services: `walle-actions` (narrow client, band A) and `walle-actions-super` (broad client, bands B and C). Each has its own service account and its own secret, with one reader each. |
| **19** | **Adopt Agent Identity, and decide it before the first production engine exists** | Generally available, per-agent identity with 24-hour certificates and bound tokens, and the identity every platform governance feature keys on. `identity_type` is fixed at engine creation. One fact is undocumented: whether the agent can present a Google-signed ID token that Cloud Run IAM accepts. | Yes, contingent on the one-day spike in [SETUP.md](SETUP.md) Phase 12b step 3 (checks 12b-a, 12b-b and 12b-c; reasoning in [12-agent-identity.md](12-agent-identity.md) section 1.6), run on a throwaway engine before the production one is created. Fallback is `walle-agent@`, with the spike output attached. **2026-09-13 (platform HLD §4.1; P4, P5):** promoted to a platform rule. Every reasoning layer on the platform runs under a Google-issued agent identity. The Agent Runtime half is unchanged. Agent Identity for Cloud Run is Preview: nonprod now, prod when GA, with dated service-account exceptions until then. |
| **21** | **Gateway or perimeter for the reasoning layer, one decision before Stage 1** | Agent Gateway and VPC Service Controls are not supported together on the engine, so weakness 13's deferred perimeter and the egress-gateway allowlist are the same choice. | The gateway, for the agent: default-deny hostnames, dry-run evidence, no Google-managed tenant network to reason about. Whether a perimeter can still wrap the action service's dependencies while excluding the engine is `tbd` in the same record. [13](13-agent-interconnection.md) section 7.5. **2026-09-13 (platform HLD §8.1; P3):** closed by P3. The gateway now; VPC Service Controls per tier folder as the backstop once the second spike resolves. |
| **22** | **Is `walle-actions` ever exposed as an MCP server?** | It is the only way the egress gateway's Model Armor would screen tool results. It adds a second protocol to the credential holder. | No, unless the injection-precision metric at Stage 2 shows tool-result injections reaching the proposal queue at a rate canonicalisation and the taint bit do not contain. [13](13-agent-interconnection.md) section 6.3. |
| **23** | **When, if ever, does Wall-E serve A2A, and from where?** | Three options: none, the Preview Agent Runtime template, or a Cloud Run `to_a2a` service. Every A2A path is a second ingress to the reasoning layer. | None through Stage 2. Eve uses `reasoningEngines.streamQuery`. Revisit at Stage 3 only if a consumer needs it. [13](13-agent-interconnection.md) section 4.1. |
| **24** | **Model Armor blocking threshold and confidence level** | Templates start inspect-only; the flip to blocking and the confidence level are measured decisions, and the prompt-injection filter changes version on 2026-09-25. | Decide from the Stage 0 regression-suite numbers, in the Stage 1 record. [11](11-prompt-security.md) section 4. |
| **25** | **Telemetry content capture: `EVENT_ONLY` or `NO_CONTENT`** | Grading and forensics need the model's inputs and outputs; those logs then hold employee data and tenant content. Span capture is off regardless. | `EVENT_ONLY` at Stage 0 into a restricted EU bucket; the data-protection assessment decides before Stage 1. [11](11-prompt-security.md) section 6. |
| **20** | **How do operators authenticate to the control plane?** | Without an answer there is no kill switch, no approval and no veto — the entire enablement plan is unexecutable. | Give operators a caller path from day one: no human holds `run.invoker` on an action service directly, so `walle-operators@` members halt, demote and veto through the `walle-operators-caller@` identity and the approval page ([03 §"GCP resource inventory"](03-lld.md#gcp-resource-inventory), item 2 and the per-endpoint allowlist). Build the approval surface before Stage 1's first real write. |

## Verified since the first draft — these are now closed

Recorded here so nobody re-opens them.

| Was open | Answer, verified 2026-09-07/08 |
|---|---|
| Agent Engine in europe-west1? | **Yes.** Agent Runtime, Sessions and Memory Bank are GA there, with EU at-rest residency. |
| Is it still "Vertex AI Agent Engine"? | Now **Agent Runtime**, part of Gemini Enterprise Agent Platform. The API resource is still `reasoningEngines`. |
| How does Gemini Enterprise pass the end-user identity? | As `user_id` = the user's **email**, surfacing as the ADK session user id. Asserted by the caller — the Discovery Engine service agent of the app project — and not cryptographically bound to the user, so lock down `reasoningEngines.query` ([12](12-agent-identity.md) §7) and re-check group membership server-side. |
| Registration flow for a custom agent? | Console: Agents → Add agent → Custom agent via Agent Runtime, with the `reasoningEngines` resource path. Sharing is per-agent, and supports Google Groups. |
| Do Chat methods need an app identity? | **Partly.** The robot's user token can post **text** messages. Cards, buttons and interactive widgets require app authentication, so the one-click approval and veto surface does need a Chat app. |
| Current deployment SDK? | `vertexai.Client(project, location).agent_engines.create(...)`. The old module API, `vertexai.agent_engines.create()`, is deprecated since Vertex AI SDK v1.112.0. |
| Can the action service's Cloud Run ingress be internal-only? | **No.** Agent Runtime egresses from a Google-managed tenant project, which Cloud Run treats as external, so internal-only ingress blocks it. The fixes are a shared VPC Service Controls perimeter, an internal load balancer in front of Cloud Run, or a Private Service Connect endpoint; a PSC **interface** alone does not help, because `run.app` traffic still bypasses the VPC without private DNS peering, and it also disables the agent's internet egress. IAM is the enforced boundary until P3's engine-reach spike passes ([decision 9](#needed-before-stage-1-the-first-real-write); the perimeter itself is [../agentic-platform/06-gateways-model-armor-perimeter.md](../agentic-platform/06-gateways-model-armor-perimeter.md) §4.2). |
| Can the Alert Center API be used? | **No** — it requires domain-wide delegation. Replaced by Workspace audit-log sharing into Cloud Logging, which needs no credential and gives Eve an independent view. |
| Are there Workspace Events for directory changes? | No. The Events API covers Chat, Meet and Drive. Use the Cloud Logging route. |
| Deleted-user restore window? | 20 days, and it needs a spare licence. This is why deletion is not in the catalogue. |
| Can the admin role be OU-scoped? | Yes for a custom role (`scopeType=ORG_UNIT`), still relevant to Eve's `eve@`; Groups and Reports privileges cannot be OU-scoped. **Qualified 2026-09-13:** not for Super Admin, which Wall-E now holds — Google's prebuilt-role page describes Super Admin as having "access to all features in the Google Admin console and Admin API" and offers an OU limit only for roles such as User Management Admin ([prebuilt administrator roles](https://knowledge.workspace.google.com/admin/users/prebuilt-administrator-roles), re-read 2026-09-13) — so for Wall-E, scope is enforced by our code alone (platform HLD "What this reverses"). The privilege facts and their sources: [02 §"Workspace privilege facts"](02-identity-and-auth.md#workspace-privilege-facts). |

## Still to verify in console during build

1. The exact custom-role privilege names covering licence read and assignment. *Qualified
   2026-09-13:* moot for Wall-E, which holds Super Admin. Still needed for Eve's read-only
   `eve@` role, resolved with `privileges.list` before its consent (platform HLD §13.2).
2. Whether your Gemini Enterprise app is in the `eu` multi-region, `us`, or `global`,
   and which project hosts it (`GEMINI_PROJECT`) and that project's number
   (`GEMINI_PROJECT_NUMBER`), from which the service-agent principal
   `service-<GEMINI_PROJECT_NUMBER>@gcp-sa-discoveryengine.iam.gserviceaccount.com` is
   built — never from Wall-E's number. An `eu` app can front a `europe-west1` agent; a
   `global` app can front any region; a `us` app cannot, and then either a new `eu` app is
   created or the region decision reopens. This one is a precondition rather than a build
   verification: a five-minute console lookup (Gemini Enterprise console → app → settings)
   done **before SETUP Phase 6**, because everything after it is regional ([SETUP.md](SETUP.md)
   §1.1 D8; [PREREQUISITES.md](PREREQUISITES.md) §1 D8). The app fronts the agent in
   `WALLE_PROJECT` cross-project (Google:
   [Configure cross-project ADK agent access](https://docs.cloud.google.com/gemini/enterprise/docs/configure-cross-project-adk-agents)).
   Whether the app's project sits under `FOLDER_ID` is decision 52.
3. Your Workspace edition, which determines whether OAuth and SAML audit events can be
   shared to Cloud Logging — and therefore whether the event trigger class is viable at
   all. Note **Drive audit events are not among the shareable types** in any edition.
   Also confirm the edition supports the login reporting rule in Phase 1.
4. Whether your GCP session-control policy would affect the robot's token — it will not
   if `cloud-platform` is never requested, which is the rule.
5. Whether Agent Identity satisfies the Cloud Run hop in practice. It is generally
   available and supported on both ends, so this is a build check, not an unknown —
   [decision 19](#needed-before-autonomy-stage-3-onward).
6. Whether group-management privileges honour OU scoping. Answered 2026-09-13 against Google's
   privilege definitions: Groups privileges cannot be OU-scoped
   ([02 §"Workspace privilege facts"](02-identity-and-auth.md#workspace-privilege-facts)). Moot
   for Wall-E (Super Admin cannot be OU-limited; containment is the sandbox tenant, decision 29).
7. Current Agent Runtime and Sessions unit prices — the pricing page did not render for
   automated reading.

## Raised by the design challenge of 2026-09-11

[14-hld-challenge.md](14-hld-challenge.md) re-examined the high-level design with the set
complete and nothing built. Its verdict is that the architecture is the correct path and that
sixteen further decisions are open. The reasoning, the evidence and the challenge each one
comes from are on that page; only the decisions are repeated here.

Three are blocking, and two of those come due before the Phase 9 consent, which cannot be
undone. *Qualified 2026-09-13:* 26 is closed for Wall-E by fact, and 27 is re-cut as "which
lane". The Phase 9 consent now covers two clients on a super-admin account and waits on 27
and 3.

| # | Decision | Why | Recommendation | Gate |
|---|---|---|---|---|
| **26** | **Is the admin principal a keyless service account holding the custom role, rather than the robot user?** *Rewritten 2026-09-13 — closed for Wall-E by fact (platform HLD "What this reverses", §4.6; P33).* | Google documents that any role except Super Admin can be assigned to a service account with no DWD. If it covers Directory, Licensing and Reports, the password, recovery path, hardware key, consent, frozen scope set and stealable token all disappear. **2026-09-13:** the objective gives Wall-E Super Admin, and the same Google sentence ("You can assign any prebuilt or custom role except Super Admin to a service account", [assign specific admin roles](https://knowledge.workspace.google.com/admin/users/assign-specific-admin-roles), re-read 2026-09-13) excludes a service account from holding it. So Wall-E's admin principal is the licensed user `walle@`, and the costs listed here (password, recovery path, hardware key, consent, frozen scope set, stealable token) are paid and compensated by platform HLD §13.1 items 3, 5–7 and 12. | **Closed for Wall-E, 2026-09-13.** No spike runs for Wall-E. The question survives only for Eve, whose `eve@` holds a read-only custom role that a service account *could* hold: Eve's **E-16** ([../eve/09-open-decisions.md](../eve/09-open-decisions.md)). The decision file is P33's `decisions/2026-09-13-wall-e-holds-super-admin.md`, which supersedes this row for Wall-E. *History, recommended 2026-09-11 by the design challenge ([14](14-hld-challenge.md), C3):* "Spike on a throwaway service account with the reader role through ADC. If it passes, the admin half moves; Gmail, Chat and Calendar stay on a user with no admin role." | *Was:* **Blocking — before Phase 9 consent, which is irreversible.** *Now:* none for Wall-E; E-16's gate for Eve |
| **27** | **Catalogue breadth: ratify bands B and C** — *rewritten 2026-09-13 as "which lane", not "whether" (platform HLD §13.1 "The three bands"; P28, P29, P125)* | The requirement was narrowed and never signed off, and scopes freeze at consent. **2026-09-13:** the objective answers the breadth question the other way. Wall-E may perform "any super admin level action of Google Workspace" on a human's prompt. What is left to decide is the lane each kind of work takes, and what the EU AI Act purpose statement says about it. | **Three bands.** **A**, the typed catalogue on the ladder, is the declared intended purpose and the only band that can climb. **B** is a generic Admin SDK lane `/v1/execute-generic` on `walle-actions-super`: Discovery-validated, permanently L3, chat only, two human super admins at tier `SUPER`. **C** is console-only work, handed to a human with a watch for the matching admin event; the Admin console is never automated under the robot's session. The hard-denied list is refused in all three. Sign: the two lists (P29); the purpose, "the catalogue plus bands B and C as execution and instructions", with legal (P28). The literal reading "any admin action as the purpose" is declined in platform HLD §0.6 R4. Band B is the generic Admin SDK proxy that [14](14-hld-challenge.md) C35 rejected, re-argued: because it never climbs above L3, the per-operation inverse, pre-state predicate and taint declaration it lacks are not needed. *History, recommended 2026-09-11 by the design challenge ([14](14-hld-challenge.md), C1):* "Wall-E is a narrow operator, not a stand-in for a super admin. Read the bands table and say which band-B exclusions you disagree with." | **Blocking — before [decision 3](09-open-decisions.md)**, unchanged; P29 also gates the super-admin grant |
| **28** | **Does a human request or approval have to fall inside the requester's own admin scope?** | Group membership currently grants the robot's whole allowlisted reach. Harmless with one super-admin operator, a privilege-escalation path the day a second operator is added — and [SETUP.md](SETUP.md) Phase 1 already names one. | Yes, but through the committed operator list rather than a live check: record each operator's admin role, families and OU reach before they are added, and reconcile daily against `roleAssignments.list`. Live policy step 5b — privilege-level for Groups and Reports, which Google does not OU-scope, full OU intersection for the rest — is the stronger form and is deferred, because it would put a fail-closed live Directory read on the approval path of a one-person rota and rests on a privilege mapping the set records as unverified. **2026-09-13 (platform HLD §13.1 item 4):** with a super-admin robot, the requester check is the primary containment for T0, not a latent risk. Band A keeps the committed operator list, reconciled daily. Band B at tier `SUPER` makes the live requester check mandatory and fail-closed, with requester and approver two different human super admins; the rule is [03 §"Band B"](03-lld.md#band-b--the-generic-lane-discovery-pinned). **Closed for band B.** | Before S1 and **before any operator who is not a super admin**. *2026-09-13:* before S1 unconditionally, and before the first band-B request |
| **29** | **Where does a change to the gate first execute?** | Today: as the production credential holder, against the production tenant. Group-family writes have no containment but the code under test. | **Replaced 2026-09-13 (platform HLD §3.1 nonprod row, §9; P40, P59):** offline harness by default, **plus a sandbox Workspace tenant before Stage 1**. The gate change first executes there, in `fld-agents-p-sa-nonprod`, with no production credential; promotion to production is by attestation. The reason: Super Admin cannot be limited to an organisational unit, so a second robot "scoped to the sandbox OU" of the production tenant contains nothing once the robot is a super admin. A nonprod OU in the production tenant also shares its super admins. Only a separate tenant contains the change. Eve is drilled against the sandbox tenant before the grant (P40). *Superseded recommendation, 2026-09-11, from the design challenge ([14](14-hld-challenge.md), C50):* "Offline harness by default; a second robot scoped to the sandbox OU with no customer-scoped Reader, in Wall-E's project (`WALLE_PROJECT`). A separate tenant only if F3/F3b autonomy is wanted." | Before Stage 1's first write; the tenant sub-question before any F3/F3b cell leaves L1. *2026-09-13:* the tenant is no longer a sub-question — it is due before Stage 1 |
| **30** | **Control-plane durability: RPO, RTO and the restore protocol** | A restore silently rolls back halts, overrides, nonces, dedup records and counters. | PITR, delete protection and daily backups at Phase 7; a restored control plane starts at `halt_all` and is reconciled against BigQuery before it resumes. **2026-09-13 (platform HLD §10, R-B row; P120):** accepted as written. The restore now boots at `halt_all` **by construction**: the action service pins the Firestore `Database.uid`, and a mismatch forces `RESTORED_UNCLEARED`, which only a human clears. One recorded restore before Stage 1, then one in every promotion gate above L3. | Before Phase 7 completes |
| **31** | **Evidence durability and retention** | `walle_audit` is inside teardown's blast radius, recreation destroys undelete, and grades live only in Firestore. | Write-ahead grades to BigQuery and teardown guards now; an off-project copy at Stage 1 — Eve's `walle_audit` mirror in `EVE_PROJECT` is the natural candidate (decision 51 in [../project-topology.md](../project-topology.md)); the decision itself is unchanged. Moves [decision 17](09-open-decisions.md) earlier and makes it a minimum *and* a maximum. **2026-09-13 (platform HLD §7.5, §13.2; P106, P107):** closed by the daily JSONL export of the audit tables to the locked evidence bucket, plus the append-only copy in the witness organisation. Eve's mirror stays as the reconciliation source. | Before Stage 1 |
| **32** | **Do fully pinned T1/T2 playbooks need a model at all?** | Everything the model would decide is already pinned; keeping it there carries the A3/A11 class and all the platform churn. | Deterministic by default with a typed predicate; `requires_model` per playbook by exception, entering at L0 and tainted. | Before S0 shadow evidence is collected |
| **33** | **Promotion and demotion statistics** | Point thresholds on 20–30 items promote a 90 % playbook about one time in five and demote an on-target one about one window in four. | Wilson interval gates with hysteresis; `unsure` reported separately. The gate forces the sample floor up and cannot be decided apart from it: at n = 30 even a flawless record gives a Wilson 95 % lower bound of 0.886, so [05](05-autonomy-ladder.md) §7's "≥ 30 proposals graded per family" must rise to ≥ 35 or no cell is ever promotable. | Before the first item is graded |
| **34** | **Eve is a deterministic verifier; no LLM produces an approval** | Already decided in [12](12-agent-identity.md) section 1.8 and contradicted in four other pages. Eve's designer will inherit whichever version they read. | State it in 01, 05, 06 and 08. Until an Eve design record exists, "Eve v0" is BigQuery scheduled queries read by a human. **2026-09-13 (platform HLD §13.2; P34, P19):** the approval half **stands**. No model produces an approval, signature, halt, demotion or veto, and the control path is model-free by absence (a project-level `restrictServiceUsage` denylist on `EVE_PROJECT`, and CI). The reporting half is reopened by the objective's "report to the human anything wrong". A separate reporting path, `eve-advisor` in `EVE_ADVISOR_PROJECT`, may reason. It writes reports and pages only; nothing it writes is read by the gate or by any action service. Its AI Act class is P19. | Now for the wording; before S3 for the design record. *2026-09-13:* the `eve-advisor` build for P34 |
| **35** | **Who may receive per-person reporting output?** | `walle-readers@` is specified on the write axis only, and there is no aggregate-only operation to give a non-admin. | Answer inside [decision 8](09-open-decisions.md): aggregate-only family with a minimum cell size (`Assumption:` 5), or no readers beyond admins. | Before the first reader is onboarded |
| **36** | **What does Stage 0 actually provision?** | Phase 15 and the KMS key are now Eve's, provisioned in `EVE_PROJECT` by Eve's setup (2026-09-13); what Wall-E's Stage 0 could still provision early is only the `eve-controller@EVE_PROJECT` allowlist entries, the cross-project bindings on Wall-E's resources, and `walle-events`, none of which has a consumer; a consented Eve token dies after six months unused. | Stage 0 provisions what Stage 0 uses; seams stay in code as contracts, audit columns and a CI-only stub caller. | Before Phase 1 |
| **37** | **Second pair of hands during the build (splits [decision 11](09-open-decisions.md) into 11a)** | Separation of duties, the external validator and credential recovery all need another person before Phase 10, not before S1. | Name a security reviewer with code ownership on the ceiling module, policy chain, catalogue and validator; a validator custodian; and a second person trained and witnessed on the Phase 9 re-bootstrap. **2026-09-13 (platform HLD §0.3, §0.4; P137, P139):** more load-bearing. The security reviewer, validator custodian, second deployer reviewer and Eve owner are platform RACI roles with separation rules. The Eve owner is a second human outside the Wall-E administration line, named before the grant, and "the four owner groups are one person" expires on the grant date. | Before Phase 10; *2026-09-13:* and the second human before the super-admin grant |
| **38** | **Value baseline and stop rule** | No baseline, no stop rule, and a cost table missing its largest line — human time. | Measure four weeks of baseline toil before Phase 1; dated stop-or-continue review at S1 exit. | Baseline before Phase 1; review at S1 exit |
| **39** | **Robot account lifecycle: detection, custody and rebuild** | No detection of another administrator acting on the robot, no paging class for a removed role assignment, no rebuild path past the 20-day deletion window. | Activity rule (Google's current name for a reporting rule) on admin events targeting the robot; `role_assignment_missing` as a paging class; second vault custodian; rebuild checklist. **2026-09-13 (platform HLD §13.1 items 5–7, §4.6; P7, P16, P68, P102):** widened in both directions. Detection of another administrator acting on the robot is still needed. The robot can now also act on **other administrators**: that is hard-denied for their security settings and backup codes, and a second activity rule pages on any admin event whose actor is the robot and whose target is another admin. `role_assignment_missing` and `role_assignment_added` both page. The roster holds exactly three super admins in steady state, with the robot never the recovery one. K6 (a human removes Super Admin from the robot) joins the drills. Custody uses two hardware keys with witnessed records. | Before S0 opens |
| **40** | **Google-side contract drift probe** | Two dependencies fail silently on the autonomous path: the admin audit event names the playbook `trigger` fields key on, and the robot's actually-assigned privilege set for reads other than the admin enumeration. Loud breaks, such as a renamed privilege returning 403, are already caught by the closed error enum, verification and the breaker. | Extend the existing daily drift job: privilege-name and event-name diffs set `no_autonomous`; discovery-revision bumps are informational and freeze promotions. No SKU diff — Google renames SKU display names while the ids stay stable, and the Licensing API takes ids ([C54](14-hld-challenge.md#c54--google-side-contract-drift-is-watched-for-model-armor-but-not-for-the-workspace-apis-stands)). | Before Stage 1 |
| **41** | **Single region, accepted** | Everything but BigQuery is in one region and nothing records that as a decision. | Accept, with RTO equal to Google's regional recovery, because multi-region would cost a residency analysis and duplicated engines for a system that is not business-critical. **2026-09-13 (platform HLD §10; P83, P140):** accepted for every tier, with the manual Admin console as Wall-E's continuity plan, and entered as an accepted row on the platform risk register. | Record now; non-blocking |

## Raised by the four-project topology of 2026-09-13

[../project-topology.md](../project-topology.md) moved Eve, Mo and the Gemini Enterprise
app out of Wall-E's project and wrote down every grant that crosses. Each of decisions 42 to 52
is a grant that would otherwise cross a project boundary implicitly; all are recorded, with
reasoning, recommendation and gate, in [its §8](../project-topology.md#8-open-decisions-this-topology-adds). The was/now of 46 and 52 is in
[§"Decisions changed by the objective of 2026-09-13"](#decisions-changed-by-the-objective-of-2026-09-13).

- **42** — Does the engine-scoped custom role `walleEngineQuery` suffice for the Gemini
  Enterprise app in another project to register and invoke Wall-E, or is the project-level
  `roles/discoveryengine.serviceAgent` on `WALLE_PROJECT` required? A spike on a throwaway
  engine decides it before the SETUP Phase 13 registration, and if the project-level fallback
  is applied the query lock holds only because `WALLE_PROJECT` contains exactly one engine
  ([§8](../project-topology.md#8-open-decisions-this-topology-adds)).
- **43** — Drop `roles/agentregistry.viewer` for Eve and Mo in `WALLE_PROJECT` ([§8](../project-topology.md#8-open-decisions-this-topology-adds)).
- **44** — Replace Eve's project-level `roles/datastore.viewer` in `WALLE_PROJECT` ([§8](../project-topology.md#8-open-decisions-this-topology-adds)).
- **45** — `walle-events` cross-project subscriptions: when, and in whose project ([§8](../project-topology.md#8-open-decisions-this-topology-adds)).
- **46** — Who asserts the Eve-side properties Wall-E's drift job cannot read ([§8](../project-topology.md#8-open-decisions-this-topology-adds)).
- **47** — How Eve checks that Wall-E's `walle_workspace_logs` copy carries no actor exclusion ([§8](../project-topology.md#8-open-decisions-this-topology-adds)).
- **48** — The carve-out list in `EVE_PROJECT`, and the `verdict_receipts` view dataset ([§8](../project-topology.md#8-open-decisions-this-topology-adds)).
- **49** — Whether `mo-metrics@` can read Wall-E's linked `walle_spans` dataset ([§8](../project-topology.md#8-open-decisions-this-topology-adds)).
- **50** — The CI ingestion identity, its home, and its two reads in `MO_PROJECT` ([§8](../project-topology.md#8-open-decisions-this-topology-adds)).
- **51** — Which project holds decision 31's off-project evidence copy ([§8](../project-topology.md#8-open-decisions-this-topology-adds)).
- **52** — Ownership, budgets and the Gemini project's identity ([§8](../project-topology.md#8-open-decisions-this-topology-adds)).

## Sources for the verified rows

Google Cloud documentation, read 2026-09-07 and 2026-09-08: Agent Platform supported
locations for agents; Agent Engine SDK migration guide; register and manage an ADK agent
in Gemini Enterprise; share custom agents; Gemini Enterprise locations; Agent Identity with
Agent Runtime. Google Workspace developer documentation: Admin SDK Reports push
notifications; Alert Center authorization; Admin SDK Directory limits and roles; Chat
authentication and authorization; Enterprise License Manager. Google Workspace admin help:
restoring a recently deleted user. ADK release notes on GitHub for versions 2.5 to 2.8.
